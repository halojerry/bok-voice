# Repository Guidelines

Contributor guide for **Bok Voice**, a local-first voice customer-service assistant (A-line) and real-time interpretation workbench (B-line). Read [RUNTIME_TOPOLOGY.md](docs/RUNTIME_TOPOLOGY.md) before changing runtime behavior, and [AGENT.md](AGENT.md) for LiveKit decisions. S2S（端到端语音模型）评估结论与试点路线见 [docs/S2S_ROADMAP.md](docs/S2S_ROADMAP.md)——粤语输出当前无本地 S2S 可用，勿轻言换架构。

## Project Structure & Module Organization

- `apps/agent` — LiveKit agent runtime (VAD → ASR → LLM → TTS orchestration and providers).
- `apps/control-plane` — FastAPI API (:8000): objects, personas, knowledge, templates, calls, audit, tokens.
- `apps/web` — Next.js static export（纯浏览器 UI：节点 node_agent :3000 / 云 CP 托管）。**AI 工作站 `/studio`（2026-09-19 W1）**：模板列表 → `/studio/?t=<id>` 工作台五 tab（话术流程=共用编辑器 `components/template-editor.tsx`（自 /templates 原样提取、双页共用）/ 意图与问答只读摘要+深链 `/qa/?template=<id>&view=canvas` / 罐头录音状态面 / 通话日志按模板过滤 / 学习报告（reports 键门控））；静态导出零动态段，深链一律 query 参数。**主管台捕获打铃 v0**：`whatsapp_status` 跳变（4s 轮询 diff）→ WebAudio 蜂鸣+可选通知，开关 `bok_supervisor_bell`；已知漏铃窗口（轮询窗内挂断/后台标签节流）由名册兜底，根治=W4 `assist_status` 列（WA 捕获铃与图意图 `notify_human` 铃届时共用呈现链）。**W2 发布两态+流程画布（2026-09-19）**：模板发布=**单列契约 `conversation_templates.published_json`**（冻结九键 steps_json/graph_json/hotwords/tone_override/opening/core/objection/closing/language；空=从未发布，不设 status 列消灭双列漂移）——`POST /api/templates/{id}/publish` 是唯一写入口（闸链同 PUT+审计 `template.publish`）、PUT 永不触碰；**agent 装配恒吃冻结版**（机器通道 GET 详情 overlay，人类通道恒 live 草稿，建单快照+发布快照双层隔离在途通话）；`_ensure_column` 建列当次一次性回填存量（排在 yue→cantonese 之后冻结规范 language；每启回填=未发布模板被误标已发布，勿改）；列表/详情行派生 `published/has_changes` 布标。**流程画布**（工作站话术流程 tab 表单/画布切换）：场景=`FlowStep.scene` 纯数据位（parse 宽容、引擎零消费、stepsToJson 非空才写键），画布=场景泳道+步节点+**答法抽屉**（正稿/分支/注意三件结构化编辑，`lib/flow-canvas.ts` 的 parse/serialize **镜像 flow.py `_BRANCH_LINE_RE`/`_NOTE_LINE_RE` 语法、round-trip 无损**——未知行不丢原样回写；改语法两处必须同步）+意图只读 overlay（jump 边/播快答/判据徽标，编辑深链 qa 画布）；保存=updateTemplate({steps_json}) 单键部分更新。**W3 自学习闭环+变量 tab（2026-09-19）**：`POST /api/qa/cluster`（`_gate_page qa`；dry=挖掘→本地 LLM 三列计划 variants/fresh/junk、apply=按勾选盖章入库与 create_qa_entry 同款 owner 语义+审计 qa_entry.create×N/qa.cluster 汇总；variant payload 补 `cluster_head_id=target` 接轮换簇）——纯函数上移 `packages/core/bok_voice_core/qa_cluster.py`（`scripts/mine_qa.py` re-export，CLI 零变化）；CP runner `control_plane/qa_cluster.py`：LLM=`MLX_LLM_BASE_URL`（**勿用 CLI 的 BOK_LLM_PORT**，CP env 面没有）+/v1/models 发现 4b 优先+`BOK_QA_CLUSTER_MODEL` 覆盖；候选=`mine_qa_pairs(iter_call_conversations)` 与 reports/qa-pairs 同源进程内零重复；**单飞**（锁+运行标志→409）+**dry 计划缓存键=(account,min_calls,limit) TTL 600s**——**apply 带 select 必须命中同参数新鲜缓存否则 409「重新生成」**（下标只在那份计划上有效，静默重算=采错条目；select=None 采纳全部可安全重算）；采纳成功清该账号全部缓存键（防 TTL 内重复采纳）。web 学习 tab=`components/study-tab.tsx`（聚类计划三组勾选采纳，dry/apply **必须同参 limit=30**）+ 变量 tab=`lib/var-panel.ts` 纯函数镜像 `flow.py object_vars/render_template_text`（空串保留占位/digitsToCn 双轨/contact 语言缺省/say 行丢行警示；改 flow.py 变量语义两处必须同步）。**W4 意向规则引擎+notify_human 打铃（2026-09-19）**：两级规则表 `intent_rules`（`account_id=''`=全局行 admin/root 写、账号行话务员写；读=两级行合并 `account_id IN ('',acct)`；端点组挂 `_gate_page("calls")`，by-ID 闸链=`_gate_intent_rule_row` 404→全局行走角色闸/账号行走 deny_cross_account）；**共享契约 `packages/core/bok_voice_core/intent_rules.py`**（INTENT_FACTS 12 键白名单/`eval_intent_rules`（(priority,id) 升序首中、conditions AND、缺键保守不命中）/`validate_conditions`——确定性事实比对无正则无 LLM）；**评估在 agent 挂断单点**（`_schedule_call_end` 内 `_intent_facts_snapshot`（`_facts` 账本：nudge/watchdog/storm/verdict Counter/graph_notifies/t_start_wall）× 规则 → `evaluate_intent_disposition` → 覆盖 disposition（规则字段非空才覆盖）+ intent_code 存 `_end_scheduled`，`_fire_end_call`/`_end_on_shutdown` 同读账面；`/end` 扩 query `intent_code` 落 `call_sessions.intent_code` 列；kill-switch `BOK_INTENT_RULES=0`，已入 `_FORWARD_ENV`）；新意向 disposition 在 dashboard/campaign **天然安全**（非三坏值=算接通、item=done 不重拨，测试钉住，未动 `_ANSWERED_EXCLUDED`/redispatch 白名单）。**打铃=notify_human 不暂停**（用户拍板：AI 零空档照常兜话）：图意图第三动作 `ACTION_NOTIFY_HUMAN`（validate 枚举+agent graph 块第三臂：`_report_notify_once` 上报成功才烧 once（CancelledError/Exception 双回滚）+`provider="graph-notify"`+**不 raise StopResponse**）→ `POST /api/calls/{id}/assist`（幂等 done 不降级 notified+审计 `call.assist_notify`）→ `call_sessions.assist_status`（whatsapp capture 端点顺手置 notified（仅空时）、takeover 顺手置 done）→ supervisor 呈现链合流（pending=WA 态∪assist notified，横幅/徽标/蜂鸣共用，W1 漏铃窗就此根治）。web：/calls 页意向规则折叠卡（条件行编辑器 fact 12 键中文下拉/op/value 数字或布尔）、qa 画布动作三选（通知人工无参数）。**W5 通知骨架+卫生收编（2026-09-19，收官期）**：①**短信 webhook provider 骨架**（真实网关未来对接，webhook_url 即对接点）——settings 新 `sms` 段（`global_settings.sms_json` 列；webhook_url/secret/**enabled=False 默认关**/hangup_enabled/hangup_template；secret 入掩码+PUT 空传保留旧值）；`POST /api/notify/sms`（require_role admin/root 烧真金判据；to 空按 call_id 反查 contact_phone；未配置 503；HMAC-SHA256 签名头 `X-Bok-Signature`；审计 `sms.send` detail.source=api|hangup）；**挂断自动钩子**挂 `_settle_core` 尾部（append_settlement 之后 try/except 全包不破 settle；enabled+hangup_enabled+phone 才发，模板 `{contact}` 替换；幂等靠 settle existing 短路天然不重发）。②**SIP REFER 试点骨架**（trunk REFER 支持未验证）：`POST /api/supervisor/{id}/transfer-sip`（admin/root+跨账号闸；settings sip.mode 非 "real" 一律短路 `{"mocked":True}` 默认安全；真路径 `client.sip.transfer_sip_participant`，participant_identity=`sip-{contact_phone}`；审计 `supervisor.transfer_sip`）。③**web 卫生收编**：`lib/voice-options.ts` 统一音色三源（静态目录+云端克隆+本地预置/克隆：去重/语言槽过滤未知回落全量/匹配语言克隆置顶/两种标签风格）+`resolvePreviewLang`/`previewSampleText`/`decideAuditionPath` 纯函数；`lib/preview.ts` 统一试听（`previewVoice` 缓存优先 cannedEntryId→降级、**全部 fetch 带 authHeaders**（修 canned-audition 垫话与 interpret-console 两处裸 fetch auth-on 401）+`playAudioBlob` 单点 revoke（修两处 objectURL 泄漏））；四处消费点（settings/personas/interpret/qa+canned-audition）行为等价改造；孤儿 API 处置=删 `supervisorJoin`/`updatePersonas`/`allMinimaxVoiceOptions`/`previewLangForVoice`（CP 端点全保留）、`changeMyPassword` 补 topbar 改密对话框（全角色自助入口）、`settle` 补 calls 行「补结算」按钮（ended 行，CP 幂等兜底）。总路线与分期定案：`docs/superpowers/plans/2026-09-19-ai-studio.md`（W1-W5 五期全部落地）。
- `packages/` — shared Python: core models, SQLite repository, knowledge, observability.
- `services/` — local sidecars: Qwen3-ASR (:8787), Qwen3-TTS (:8788), realtime-translation worker (:8790), LiveKit server config.
- `tools/node_agent.py` — 薄节点守护（心跳/commands/UI :3000 托管/全栈拉起）。
- `tools/bok.py` — single orchestrator: `serve`, `status`, `down`, `doctor`, `download`.
- `tests/`, `services/realtime-translation/test`, `scripts/` — Python/Node suites and CI helpers.

## Build, Test, and Development Commands

```bash
./scripts/bootstrap.sh                  # create .venv312 + install Python deps
./scripts/test.sh                       # full pytest suite
cd services/realtime-translation && npm ci && npm test   # B-line Node tests
python tools/bok.py serve               # start the full local stack
python tools/bok.py status | down | doctor --packaged
python tools/bok.py prod install        # generate launchd plist units (KeepAlive auto-restart)
python tools/bok.py prod status         # official health surface (HTTP 健康 + worker :8081-8083 真端点 /worker + 可选 mt/settle)
E2E_ONLY=cantonese .venv312/bin/python scripts/e2e_trilingual_livekit.py  # A-line E2E
cd apps/web && npm run build            # static web export
scripts/build_node_pkg.sh <version>     # 发版：节点代码包（+ build_runtime_pkg.sh 运行时包）
```

## Coding Style & Naming Conventions

- Python: PEP 8, 4-space indent, `from __future__ import annotations`, type hints on signatures; run `python -m compileall -q` after edits.
- TypeScript/React: match surrounding components; no formatter is enforced in CI.
- Naming: `snake_case` Python modules/functions, kebab-case services; model repo dirs use `owner--name` as declared in `tools/bok.py` `MODELS`.
- Bash scripts: `set -euo pipefail`.

## Language / Terminology Rules（跨层约束）

- **粤语规范值 = 小写 `cantonese`，全时空唯一拼写**，语言三态只有 `zh / cantonese / en`。全栈（DB、web、TS 类型、prompt 分支、`LanguageState`、voice-map 键、`speaker_cantonese`）一律 `cantonese`；旧拼写已存量清零（CP 启动迁移 `deps.py build_engine()` 是唯一兼容点），运行时代码**不留任何别名分支**。
- **防复发门禁**：`tests/test_cantonese_terminology.py` 扫描全部跟踪源文件，旧拼写只允许出现在白名单单点，新增即测试失败——字段单轨化，杜绝双轨技术债。
- **外部系统真字面量不改（门禁白名单内）**：Wikipedia 域名 `zh-yue.wikipedia.org`、Volcano dialect 枚举、MiniMax 音色 ID `Cantonese_*`。音色 id 值是不透明标识符，不属语言字段。
- **每通对话语言固定（A 线，取代逐轮语言跟随）**：粤语通话全程粤语、中文全程中文、英文全程英文，中途不切换。会话装配时一次钉死三方：ASR hint 恒钉通话语言（`_call_language` 人设→对象→zh；`PinnedLanguageState`+`pin_language=True` 三语全钉，zh 也下发 `Chinese`；设置 `asr.language_mode=fixed`+显式 `language` 仍优先，**mode=auto 在 A 线=钉到本通语言，不是滞回跟随**——LanguageState 滞回/sticky 机制已在 A 线退役，B 线不变）、LLM【用户语言】规则装配时 `set_user_language` 一次字节静态整通（逐轮钩子不碰语言）、TTS 音色+MiniMax `language_boost`（zh→Chinese、cantonese→Chinese,Yue、en→English，env 按通话注入）按通话语言固定。sidecar 传 `language=cantonese` 等规范名（mlx 大小写不敏感回填模型 config）消除 auto 误判（啱唔啱→难唔难）。**Prompt 语言纯度**：进 prompt 的共享指引文本（【应答准则】/当前步守卫/【新一步】/后备块/收尾/尾部节头）一律**标准书面中文**——这些无条件进每通通话，写成粤语书面语会把 4B 模型的普通话回复带偏成夹粤语（2026-09-06 实证后整批改写，`test_zh_prompt_purity_no_cantonese_marks` 钉死）；方言风格只准出现在按通话语言条件渲染的规则里（`_cantonese_rule`/港式词表，仅 cantonese 通话渲染）；`_zh_rule` 明确禁方言夹杂。

## Architecture Boundaries & Runtime Rules

- **官方组件优先（契约+组件，2026-09-09 拍板）**：除知识库/对象/话术业务域 + 本地模型插件 + B 线双栏字幕外，全部对齐 LiveKit 官方栈（详见 `docs/DEV_TOOLS.md` + `AGENT.md` 决策记录）。查官方用 docs MCP `https://docs.livekit.io/mcp`（免费无 key）。**别重造官方轮子**（LLM 客户端/转写落库/崩溃补位等先查官方姿势）。**官方组件优先三档判据**：①新功能先查官方组件/官方模式（@agents-ui 走 shadcn registry 可增量拉；lk docs/Docs MCP 查事实）；②自定义 STT/LLM/TTS 插件、业务工作台 UI、bok.py 编排属官方授权自定义层非手搓；③实证分叉（尾部冻结重放/FlowController 规则先行/回声守卫/浏览器 setSinkId 设备层/B线 whoIs 双栏）动前必读本轮调研档案 .superpowers/sdd/2026-09-09-official-first/FINDINGS.md（本机档案，结论已浓缩于本条）。**web CallStudio connect() 陷阱**：勿在未连接的房间上 `await setMicrophoneEnabled(preConnectBuffer)`——发布等连接、连接等该行返回，死等到 livekit 内部 ~15s 超时（「首次接通 15.3s」根因，2026-09-06 浏览器探针实测）；麦克风采集走 `session.start({ tracks: { microphone: { publishOptions: { preConnectBuffer: true } } } })` 与 token/连房并行。
- **话术分步推进**：`apps/agent/agent_runtime/flow.py` 的 `FlowController` 是唯一推进引擎（`detect_whatsapp_signal`/`should_auto_advance`/`decide_advance`/judge）。agent 每轮 `on_user_turn_completed`：WhatsApp detect → rule_verdict+auto_advance → 模糊轮背景 LLM judge（fire-and-forget，`_judge_inflight` 防叠）；语言已整通固定，钩子不做任何语言处理。WhatsApp detect 带 `already_captured`：本通已 captured 过号码后，纯短应承/叫加不再判 offered（否则确认轮锁死、4B 原句复读，2026-09-06 call-e6e5f18e 实证）；新号码照捕。**话术 WhatsApp 步方向铁律=向客户索取客户自己的 WhatsApp/微信号码**——模板第 3 步 ref 必须与 detect 的 captured 逻辑（等客户报数字串）同向；写成「发我哋微信号俾客户加」客户就永远唔报号码、步锁死（2026-09-06 三语模板数据修复实证）。**联系渠道係对象变量 `{聯絡方式}/{联系方式}/{contact}`**（`object_vars` 解析：对象 `contact_channel` 显式指定优先，缺省按对象语言 zh→微信 / cantonese|en→WhatsApp；三语模板已用占位符，换渠道唔使改模板）。**开场白三段拆分+直念步（2026-09-12，三语模板 6 步）**：step1=纯身份确认（开场白直念只剩「你好，請問係{姓名}嗎？」）、step2=来电通知、step3=平台、step4=赔偿、step5=办理、step6=收尾——旧 5 步把整段通知塞 step1 一口气念完（客户听完就挂，call-aeda536c 全场一轮）。步字段 `say:1`=直念步：进入该步的当轮 agent 以 ref 首行 `_say_script` 直念作答（跳过 LLM、缓存线零 TTFT、与罐头同速同音色），`FlowController.said_steps` 账本 + 尾部【通知已念】防重复；通知（道歉+赔偿承诺）与赔偿三档（金额合规内容，4B 会把档位重排成「100-200→2-3倍」）两步已标 say=1。**say 锁铁律：轮开始时当前步是待念直念步 → 本轮先念、流程推进让位**（`_say_pending_before` + judge 同款门）——上一轮后台 judge 进入的直念步被本轮规则推进直接跳过去=客户从未听过的合规内容整段被吞（call-73f13391 实证修复）。**身份步（current==0）任何非拒绝回应都推进**（含「你係邊個」提问——通知步自报家门正正係答案）；**say 步（`say_step=True`）实质回应即推、纯提问原地答**（步分支+QA 罐头）。web 话术编辑器步表单有「直念」复选框（`say` 字段透传，旧数据无 flag 行为零变化）。**共享应答规则随总览进前缀（2026-09-17）**：`flow_overview()` 尾部追加 `_SHARED_RESPONSE_RULES`（标准书面中文，三语无条件进、字节静态 KV-cache 安全）——①【身份与来电质疑】防诈质疑简短安抚+重申身份/官方可回拨渠道、每通详答一次、**不得借回应质疑反复追问平台**（offscript 实测修复前平台问句 5 通重复 10+ 次）；②【赔偿数字纪律】档位/金额只准客户问赔偿或赔偿环节讲，其余轮一律不报（修复前「最低 300 蚊」在投诉/找主管轮乱入 3 次）。已知残留：转写碎裂轮（「詐騙→田静宁」级）规则触发条件不被激活——治本在 ASR 端；规则措辞含「官方渠道」时留意 4B 自编热线号码（实测编出 852-1234-5678），真号码应走 QA 词条而非 prompt。改动推进逻辑先读 `tests/test_flow_controller.py`；规则测试 `tests/test_prompt_objection_rules.py`。

- **话术渐进披露+会说话三层（2026-09-12 P0，call-8fa17d2b 实证）**：旧尾部把整段 ref（正稿+全部分支）以台词形态常驻注入，4B 复制引力压过「勿念原文」——「你说什么东西？」换来 11.9s 整段重念、两轮回复一字不差。现契约：`parse_step_ref` 拆 ref 为 正稿/分支（如果客户X→就Y）/注意 三件；正稿只进本步**首轮渲染**（`_last_render_step` 账本，此后已入对话史），分支按 verdict+客户原话**只命中单条**注入（`match_step_branch` 家族优先 bigram 兜底，命中不了不注入绝不退全量）；few-shot 回应范例（一句答所问+一句带回）进静态前缀整场缓存；【你上一句】锚只示开头 12 字（整句引用=递抄袭素材）。**出口复读防线** `_RepeatSelfGuardStream`（`BOK_REPEAT_GUARD=0` 关）：句级比对上一句回复（相似 ≥0.9 或净文子串=前缀截断复读）复读句剥掉不出声，REPEAT 轮（`ctx.repeat_requested`）放行。**DEFER 短应承**（`BOK_DEFER_ACK=0` 回退）：「我先查一下/稍等/有了再通知你」类社交拖延轮 → `_say_script` 直念三语短应承，不推进不 judge 不走 LLM。
- **推进防假跳（2026-09-12）**：say_step 宽松语义只对通知型直念步（goal 含「通知」）——赔偿直念步是承诺型问句步，UNCLEAR 语气点评不得借宽松语义跳步；`judge_confirm_advance_allowed`：问句步的 judge=confirm 需客户话有应承特征（≤4 字超短/CONFIRM 词/数字/平台词），「他这个就过来了。」类长 UNCLEAR 误判拦下。
- **话术图（`graph_json`，2026-09-18 Phase 2，画布画的=引擎跑的）**：模板可选携带 `graph_json`（`packages/core/bok_voice_core/flow_graph.py` 纯函数双轨：`validate_flow_graph` 严格→CP 保存 400 / `parse_flow_graph` 宽容→运行时坏数据=空图永不炸通话）。契约：意图=**确定性子串关键词**（双侧 casefold，无正则——运营面安全）+ 绑定边 `play_qa`（需 `qa_id`，保存不校验存在性）/`jump_step`（需 `step`，1-based）；`intents[].steps` 空=全程生效、`priority` 小者先（同轮多命中只执行一个）、`once=true` 每通至多一次（`graph_fired` 账本）。数据流：CP `POST/PUT /api/templates` 保存校验（迁移唯一落点 `deps.build_engine()` `_ensure_column`，Supabase 引导件须重跑）→ 建单快照 `call_sessions.template_id`（中途改图不动在途通话）→ agent 装配 `FlowController.from_template` 解析一次 → 每轮 `on_user_turn_completed` **graph 块插在 say 直念之后、QA 快路之前**（precedence 铁律 **REFUSE 收线>DEFER>say 直念>graph>QA 快路>自由 LLM**：`closing` 后图让位告别轮、say 直念合规内容不被图抢）。jump 副作用包=与规则推进逐字节同构三件套（`jump_to()` 钳制 `[0,len]`+closing 冻结+同位 no-op，`_invalidate_stale_preemptive("流程跳转 → 第 N 步")`，`context_state.set_flow_current()`），**按实际位移记账**（未位移不烧 `once`、不打 provider、日志落 `jump_noop`）；play 与 QA 快路共用同一条罐头出口（`_qa_canned_say`），条目/音频缺失→`FLOW_GRAPH play_miss` 放行 LLM 且不消耗 `once`（悬空 `qa_id` 的明确降级路径）。观测：agent.log `FLOW_GRAPH jump|play|play_miss|jump_noop binding=<id> …`；turns assistant 行 `provider=graph-jump`（LLM 轮）/`graph-play`（`gen` 复用 `qa_fastpath`——`gen` 列 VARCHAR(16) 不新增值）、`template_step`=跳后步号。kill-switch `BOK_FLOW_GRAPH`（默认 `"1"`，读法与全仓同款 `os.environ.get(...)=="1"`；空图零成本零变化）——kill-switch 经 `bok.py` `_agent_worker_env`/`_agent_prod_env` 进 worker env；**dev `serve`/`monitor` 本就 merge `os.environ`**（走 `_start_proc`），该白名单真正兜底的是 **prod launchd/schtasks 的封闭 env 面**——不列进表，prod 命令行 `BOK_FLOW_GRAPH=0` 到不了 worker、worker 按默认 `"1"` 跑，kill 腿「零 FLOW_GRAPH」结构性测不出（2026-09-18 实弹发现，与 `_interp_env` 同款教训）。**`_FORWARD_ENV` 立法（2026-09-19，spec §5 卫生项收口）**：worker 运营 env 全集单点表（`tools/bok.py`，agent 实读 87 键收编 69——此前 dev 靠 merge 全活/prod 封闭白名单全死，`BOK_CP_TOKEN` auth-on worker 上报同病在列）；**新增 env 开关的立法动作=在表加一行**，`tests/test_forward_env.py` 扫 agent_runtime 全部 `os.environ` 读取面，未登记（表/bok 既有注入/豁免）即 CI 红；豁免清单（测试腿专用/OS 变量）带理由活在测试文件里。消费模块清单：`control_plane.main`（保存校验）/`agent_runtime.flow`（装配+`jump_to`）/`agent_runtime.agent`（每轮命中与动作）/`apps/web`（画布意图节点+绑定边+编辑器 `updateTemplate({graph_json})`）。验收探针 `scripts/probe_flow_graph.py`（真栈：建带图模板→推触发/非触发语→断言日志+turns；**kill 腿 `--expect-off` 须先以 `BOK_FLOW_GRAPH=0` 重启 serve**，env 进程级定死、A/B 前后须确认零殭尸 worker；`play_miss` 只记信息位）。**追问链 `then_jump`（Phase 3.3，2026-09-18）**：`play_qa` 绑定可选 `then_jump`（1-based 步号，仅 `play_qa` 合法，`[1,999]` 闭区间；解析宽容=坏值丢字段不清退绑定，校验严格=越界/非 int（含 bool）/`jump_step` 带该键均 CP 保存 400；**不加 DB 列**，全在 `graph_json` 串里）→ 罐头**播完当场**（同轮、`raise StopResponse()` 之前）`FlowController.apply_then_jump()`：实际位移才打 `FLOW_GRAPH jump binding=<id> step=<N> via=then_jump`、**跳转承重件=位移（`jump_to` 私有状态 `_entered_by_jump`）**，spec marker（`_invalidate_stale_preemptive`）照调但随本轮 `turn_ctx` 副本蒸发（勿当抢跑失效保证读），同位/closing 冻结/越界钳到同位 → `FLOW_GRAPH jump_noop … via=then_jump` 零副作用；**play 分支不渲染当前步**（本轮 `StopResponse` 冇请求，渲染只会烧首渲染账本 `_last_render_step`/`_just_advanced` 令下一轮流程块退分支模式），渲染留给下一轮流程块首渲染（底稿+【跳转进入】真被请求消费；T2-R1 离线段实测：带渲染行时下一轮尾部 225 字且底稿/【跳转进入】全 False=退分支模式，删掉后 327 字两者皆 True）；`then_jump` **不另立 `graph_fired` 条目**（同一绑定的动作后缀，账本仍由 play 成功处烧），整闸继承 `BOK_FLOW_GRAPH`（无独立开关、零新 env）。观测补充：播放轮本体行在**跳之前**落库（步号=跳前步），跳后步号只能在**下一轮** turns 行看到 → 探针腿 `probe_flow_graph.py --then-jump`（轮次表「播+跳后」两轮、`--after-text` 默认避开规则七族词；硬判据=`play` 日志 且（同轮 `via=then_jump` 日志 **OR** 跳后轮 `template_step` 命中）；**无 QA 条目 → 跳过（退出码 1）；条目在场但音频未物化 → 腿照跑、`FLOW_GRAPH play_miss` 由 `play_logged` 硬 FAIL（先 `python tools/bok.py tts-pregen --qa`）**；kill 腿 `--then-jump --expect-off` 同 `BOK_FLOW_GRAPH=0` 姿势），离线面钉在 `tests/test_flow_graph_probe_then_jump.py`。**意图判据 `judge`（Phase 3.4，2026-09-19）**：意图可选 `judge:{"prompt":…}`（1-400 字，校验严格/解析宽容同 then_jump 不对称惯例；**关键词仍必填**——judge 只补关键词未中的模糊轮，确定性命中恒同步先行）→ 关键词未中时背景批量判定补位：`_maybe_schedule_intent_judge` 六闸（env+图在场+非收线+话语非空+单飞+pending 空）→ 资格预筛纯函数 `eligible_judge_intents`（enabled+判据在场+步 scope+存在可触发绑定）→ `_background_intent_judge` 复用 flow judge 管线形态（`FLOW_JUDGE_DELAY` 让路 + `FLOW_JUDGE_*` :1237 9B 专线，`timeout=20`——9B 判据集 prefill 慢，共用 5s 必超时=静默永久 miss）**单次调用批量评估全部候选**（多选一输出 id/NONE，`build_intent_judge_messages` 措辞钉「原样照抄 id 禁序号」，`parse_intent_judge_output` 剥围栏/引号/句尾中文标点后精确匹配）→ store 守卫（仍在本步+非暂停+非收线+env 仍开，迟到的命中不准注入）→ `_gjudge_pending` **下一轮图块求值时先取即清**（single-shot TTL，无论绑定是否真触发；消费位再过 `BOK_FLOW_GRAPH_JUDGE` 配对+pick 内全部守卫——判据唔豁免任何闸，`judge_pending_fired`/`judge_pending_expired` 打点归因）。调度钉死 `elif user_text:` 分支（图块真求值过才调度——平级 else 会令空转写轮漏进：白烧 9B+无话语支撑的 pending）。kill-switch `BOK_FLOW_GRAPH_JUDGE=0`（默认 "1"，无 judge 数据=零调用零变化，功能随数据 opt-in；进 `_FORWARD_ENV` dev/prod 双面）。画布意图编辑器「判据」文本域（空值省键=graph_json 字节级同旧）。观测：`FLOW_GRAPH judge_scheduled|judge_hit|judge_miss|judge_skipped|judge_pending_fired|judge_pending_expired`；探针腿 `probe_flow_graph.py --intent-judge`（fuzzy+consume 两轮+`--judge-soak-s` 等判定落账；judge 日志族**不在** `RE_FLOW_GRAPH`，kill 腿断言面单独并入 `killswitch_no_judge_logs`），离线面钉在 `tests/test_flow_graph_judge.py`/`test_intent_judge_runtime.py`/`test_intent_judge_wiring.py`/`test_flow_graph_probe_intent_judge.py`。**跳步话面 I3 二修（2026-09-19，prompt 域）**：跳步轮 4B 会被总览事实行（每步 ref 首行 60 字=逐字复制素材）拉回线性剧本——基线实弹 call-790fd558 跳第 4 步回复原样复排第 2 步通知稿+第 3 步问句、f13c3c06 先讲对本步尾句回头补问。三层修（全在 `flow.py`，零 env 零 DB）：①【跳转进入】标记加硬——`_jump_skipped` 记账（jump_to 前向跳=起点后目标前全部步号）点名「第2、3步已被跳过」+禁按总览顺序从头开始+「要提问只准问本步的问题」给替代出口；②**禁讲清单**（跳转首轮尾部最后一行）——被跳步原话照录+「本轮一个字不准出现、不准换个说法问同一件事」（抽象规则 3 跑 1 过压唔住逐字引力，具体反例才拉得住；正稿在前禁句在后=生成前最后看到的是禁令）；③总览尾部图模板专用「跳转係常态」规则行（`graph.intents` 非空才渲染，非图模板字节不变）。**验收定案**：修复后 6 发零复排（通知稿复播绝迹）；落地目标步内容 ~4/6（残留 1/3 讲成下一步措辞=4B 步定位方差，观察项）；**前置回补问题係合理行为不入硬判据**（目标步内容依赖被跳步答案时——如「按平台规则赔付」依赖平台——先讲对本步再回补=合理，prompt 侧推判据侧放）。探针腿 `probe_flow_graph.py --jump-speech`（单触发轮，词面判据：`--expect-words` 本步事实词全含 + `--forbid-words` 默认只钉「需要跟您确认」类无争议复播、真模板按需收紧），离线面钉在 `tests/test_flow_jump_speech.py`/`test_flow_graph_probe_jump_speech.py`。
- **分支动作（A-②，2026-09-20，画布可配的「这句话就照这样做」）**：步 ref 的分支行应答首部可选动作前缀 `【收线】/【挂断】/【转人工】/【跳第N步】/【留本步】`（`parse_branch_action` FL:107-160 → `(action,step,text)`，纯函数；标记可省=现状；`跳第0步` 标记消费后按无动作处理）。**执行在漏斗早段**（AG:4114-4240，插在 `_say_pending_before` 之后、`if verdict == REFUSE:` 之前——先于 verdict 车道，所以运营写的「打错电话→【收线】」不会被内置 REFUSE 抢走；`_branch_plan/_branch_hold` 每轮重置，规划异常当未命中）：`refuse`=镜像 REFUSE 车道（`enter_closing`+`_schedule_call_end()`+有台词时直念该台词后 `StopResponse`，gen=script/provider=branch-refuse）；`handoff`=镜像 graph `notify_human` 臂（`_report_notify_once` 打铃不抢话、**不 hold 不 raise**，provider=branch-notify）；`jump`=镜像 graph jump 三件套（位移才记账/`_flow_step_before=-1` 压 QA，provider=branch-jump，**不烧 `graph_fired`**）；`hold`/无标记=只置 `_branch_hold`。**`_branch_hold` 让规则推进段让位**（`elif … and not _branch_hold`）＝真·留本步；plan 闸门见 `branch_hit_plan`（AG:423-505，`action_enabled/canned_enabled/step_index/closing/paused/done/user_text/ref/wa_captured/verdict`）：**第 1 步只放 refuse+handoff**（身份步「非拒绝即推进」铁律）、closing 轮只放 refuse、WA 步未捕获全挡、`verdict∈{REFUSE,FAREWELL}` 只挡无动作分支、无动作/留本步的渲染文本空或残留 `{占位}` 一律不命中（防把流程钉死）。kill-switch `BOK_BRANCH_ACTION`（默认 `"1"`，入 `_FORWARD_ENV`；`0`=早段整块不评估、A-① 罐头腿按 `action_enabled=False` 自行求值＝原姿势）。观测：`BRANCH_ACTION refuse|handoff|jump|jump_noop|hold` + 原 `BRANCH_CANNED hit|miss`。**A-① 分支罐头腿随之前移**（AG:4465-4540 只消费 plan，不重复求值）。**画布侧（A-③）**：`apps/web/lib/flow-canvas.ts` `parseBranchAction/composeBranchResp/BRANCH_ACTIONS` 逐语义镜像（`resp` 原文含标记 → round-trip 无损）；答法抽屉每分支=条件+**动作下拉**（jump 时多一个步号输入）+应答文本域（只显示剥标记文本）+**录音状态点**+补录按钮；步节点分支 chip 带动作徽标。**分支罐头状态/补录端点**：`GET /api/tts/branch-canned-status?account_id=`（`_gate_page("templates")`；CP `pregen.branch_canned_status` TTL 缓存键含账号，spawn `scripts/pregen_tts.py --branch-status [--texts-file]` → `{resp原文: "ok"|"missing"|"ph"}`，`ph`=空变量渲染仍残留 `{占位}`=永远录不出）与 `POST /api/tts/branch-pregen`（admin/root，锁键 `__branches__`，审计 `branch.pregen`）；studio 主流程 tab 拉状态、两个 props（`branchCanned`/`onPregenBranch`）传给 FlowCanvas。
- **学习闭环驾驶舱（L-①，2026-09-20）+ D4/D7 止血**：`GET /api/stats/llm-gaps`（CP 新模块 `control_plane/gap_mining.py`，零 SQL 走仓储公开方法；`_gate_page("reports")`+`scoped_account`）返回 `coverage`（只数 A 线**回复轮**：gen∈{filler,interrupted} 剔除、gen∈{script,qa_fastpath} 判快路、**provider 只进细分不算快路**——`provider=graph-jump/branch-jump/branch-notify/stall-N` 的轮回复仍由 LLM 产出）+ `gaps`（「AI 轮走了 LLM」的前一条客户轮文本，归一化聚合后按 count 降序 + `min_calls` 门槛，带 `sample_answer`/`lang`；排除 <3 字/应承/数字主导/测试对象）；`POST /api/stats/llm-gaps/adopt` 复用 `POST /api/qa-entries` 的仓储/盖章/审计（detail.source=gap-adopt，同 question+lang 幂等）——**人工确认（answer 可编辑）才入库**；web 在 studio「场景学习」tab（`components/gap-mining.tsx`）。已知口径：adopt 恒落 `scope=global/step_index=-1`（`qa_gate` 的 `int(step_index or -1)` falsy 奇点令 `step_index=0` 永不命中，见 A_LINE_LOGIC §7 D14 备注）。**D4**（`providers/livekit_plugins.py` `_maybe_partial` 先清后发丢 PCM）：改「POST **成功才删已发前缀**」，失败保留整窗+12s 有界截断+`QWEN3_ASR_CHUNK_POST_ERR` 打点，`QWEN3_ASR_CHUNK_KEEP=0` 回退；sidecar `/api/chunk` 是**追加式**（重复提交=重复转写）故不主动重发。**D7**（`_close` 的 10s gather 窗口掐死 20s 意图 judge）：窗口自适应——无慢任务仍 10s、有在途 judge（`_spawn_report(..., slow_s=20.0)`）抬到 `max(10, 20+5)`，超时打 `SETTLE_WAIT_TIMEOUT`（任务名+等待），`BOK_SETTLE_WAIT_S` 覆盖。**D14 ✅已修（2026-09-20）**：旧扫描 `glob("*.py")` **非递归**，`agent_runtime/providers/**` 的读取面从未被扫——实测该子目录 61 个键（`MINIMAX_*`/`QWEN3_ASR_*`/`LLM_*`/`VOLC_*`/`BOK_REPEAT_GUARD` 等）不在表/注入面/豁免（=这些运营逃生门在 dev 靠 `os.environ` merge 活着、prod 封闭 env 下结构性不可设，同 `BOK_FLOW_GRAPH` 实弹教训）。修法：扫描改 `rglob`（显式跳过 `__pycache__`；读取面 16→26 文件、97→166 键），**逐键读读点分类**——60 个运营键进 `_FORWARD_ENV`（LLM 生成链 `LLM_*`/`BOK_LLM_*`/`BOK_REPEAT_GUARD`/`BOK_TAIL_SLIM`；MiniMax 凭据端点+自愈+语速音量 `MINIMAX_*` 全集；Volcano `VOLC_*`；插件侧 `QWEN3_ASR_*`/`QWEN3_TTS_*`），`FAKE_STT_TEXT` 进 `_EXEMPT`（`FakeLiveKitSTT` 仅 CP `asr.provider=fake` 时构造，生产永不用假 ASR，与 `USE_FAKE_MEDIA` 同族），未登记 61→0；prod 可达性由 `test_forward_env_keys_all_flow_to_dev_and_prod` 全表兜底。
- **学习回路另外两块（L-② 图词/分支提案 + L-③ 在库词条体检，2026-09-20）**：**L-②**（CP 纯函数模块 `control_plane/gap_proposals.py`）把同一批漏网轮再对**目标模板的 steps_json / graph_json** 提两条杠杆——**branch**（某步 ref 追加 `如果客户{cond}→{resp}` 行，resp 默认=当时 LLM 实答那句、**运营可先改再存**）与 **intent_keyword**（给「enabled + 有 enabled 绑定边」的意图加关键词；亲和=整词包含+2-gram 重叠，并列取 id 字典序）。**语法不发明**：`_BRANCH_COND_RE` 与 `flow.py:102` `_BRANCH_LINE_RE` 逐字节同款镜像（CP 不 import agent_runtime——云端镜像不含 apps/agent），实测 `pattern == original` True；`sanitize_branch_cond` 在 `sanitize_branch_text` 之上**剥首尾句末标点、保留内部逗号**（漏网原话是转写原文，否则生成「如果客户拼多多。→…」——真栈实弹发现）。**去重即「不可采纳」不从画面消失**：等价分支/已覆盖关键词照出 `available=false` + 人话 `blocked_label`，adopt 对这些键幂等 `created=false`。`GET /api/stats/template-proposals`（`_gate_page("reports")`；coverage/gaps 与 L-① 逐字节同源）→ `POST …/adopt`（**闸链与 `PUT /api/templates/{id}` 逐字对齐**：`_gate_page("templates")`+`deny_cross_account`+`deny_foreign_owner(edit=True)`；`append_template_revision` 快照排在全部校验**之后**=「拒绝对数据无副作用」；审计 `template.branch_adopt`/`template.intent_keyword_adopt` 仅记真实写入项）。**`published_json` 永不触碰**（发布两态：publish 是唯一写入口）——adopt 只改 live 草稿，驾驶舱成功提示明说「记得去话术页发布才会生效」，模板列表随即显示「有未发布改动」（真栈两侧 sha256 一致实证）。键=`branch|{tid}|{step}|{sha1(norm)[:10]}`/`intent|{tid}|{iid}|{sha1(norm)[:10]}`，服务端按 item 自带字段重算比对、不一致 400（改内容拿旧键/伪造键都拒）。**L-② 客户端读取口并掉 L-① 的 `api.llmGaps`**（超集；CP 端点保留，孤儿客户端函数按 W5 惯例删）。**L-③**（CP 纯函数模块 `control_plane/qa_drift.py`，零 SQL/零新列）反转体检**已在库的快答词条**，判据全来自 turns 账本零 LLM——`fired` 只在紧接其后的 AI 轮 `gen=="qa_fastpath"` 时记，**垫话/打断账本行不是「对这条的回复」，既不记 fired 也不消费待配对客户轮**（否则垫话落在问句与真快答之间会把真快答误记 miss——真栈实弹踩到）。四类义出：`never_asked`/`digits_bypass`→**retire**（建议删），`repeat_after_play`（播了快答客户又问一遍）/`never_fired`（occ≥3 且 fired=0）→**reanswer**（建议改），`occ>0 且 fired==occ`=健康不出提案。**两条保守闸（真栈 150 通实弹后补）**：①**建议答案必须与词条同语言**——`llm_answer` 是「当时 AI 自己答的那句」、可能来自别的语言通话（实弹：中文词条被建议粤语答案→罐头化会播出错语言录音）→ 跨语言一律不采用（留空+提示自写），`repeat_after_play` 照常出（复问是铁证）、`never_fired` 退化为补录音提示；②`NEVER_FIRED_MIN_OCCURRENCES=3`（occ=1 断言「没用上」=噪声）。**窗口排序键必须带 id 兜底**（`(created_at, id)` 降序）——`created_at` 缺失/并列时只按它稳定排序会按原序截断=把**最旧** N 通当「最近 N 通」（真栈实弹踩到）。每条提案自带 `headline`/`detail` 人话（通知形状）+ `counts`（铃铛数字）。`GET /api/stats/qa-drift`（`_gate_page("reports")`）→ `POST …/adopt`（**闸链与 `PATCH`/`DELETE /api/qa-entries/{id}` 逐字对齐**：`_gate_page("qa")`+跨账号/归属闸；审计沿用 `qa_entry.update`/`qa_entry.delete` 带 `detail.source=qa-drift`；幂等 no-op 不审计；整批 Pass1 全过才 Pass2 落库=绝不出半批写入）。**改答案顺带补录音**：罐头音频键=文本、改答案即令旧音频失效——调用方是 admin/root（本可直调 `POST /api/qa/pregen`，配额闸在那里）时**按批一次**触发物化让改动真生效；**非 admin 返回 `needs_pregen=true` 且不触发**（不因一次改答案绕过既有配额闸），前端提示找管理员补录。只体检 `enabled=True`；已知残余=`never_fired` 两成因（罐头缺料 vs 被更高优先级词条/同义簇代表抢了出场）判据分不清→两个可能都写进 detail、`hits` 只作旁证。web 同在 studio「场景学习」tab（`gap-mining.tsx` 现在三段）；**触发按钮与确认按钮必须不同文案**——真点击时两处都叫「确认删掉」令运营看不出点到没有（自动化也分不清），已改「删掉这条」/「改这条答案」。真栈验收（真库副本 + 隔离 CP:8010 + 静态站:3010 + 真浏览器点击）与 6 条真栈才暴露的缺陷清单见 `docs/A_LINE_LOGIC.md` §9（G1 条件标点 / G2 错语言建议 / G3 never_fired 噪声 / G4 窗口排序方向 / G5 按钮同名 / G6 垫话吃配对）。
- **真栈实弹验收（2026-09-20，A-①②③ / L-① / D4 / D7）**：`scripts/probe_branch_action.py` 是话术分支侧的**实弹入口**（六腿真通话：`canned`/`refuse`/`handoff`/`jump`/`hold`/`kill`；自带建模板+真 `/api/token` 建单+TTS 渲染客户语音推流+按字节偏移切 `agent.log`+turns 与 `call_sessions` 对账；`--selftest` 纯函数自检、`--expect-off` 为 kill 腿）——**离线单测不算验收**，动 A 线引擎/话术分支/学习端点必跑；本轮结论与逐腿证据见 `docs/A_LINE_LOGIC.md` §8。**本机多会话共享栈时换 worker 的最小侵入姿势**：`bok.py` 来源戳（`BOK_SERVE_ROOT` + `proc-<pid>.root`）保证「他树永不误杀」，故只需停**对方的 monitor 与 A 线 worker**，用 `bok._start_proc(bok._worker_specs(py)[0]…)` 起本树 worker（同 env/同 app-data 日志/同 :8081），跑完停本树 worker 再重启对方 monitor 即完整交还（CP/LiveKit/ASR/LLM 侧车全程不动，零模型重载）。**CP 侧新端点用「真库副本 + 独立端口」验**（`sqlite3 <真库> ".backup <副本>"`、`HOME`/`VAULT_ROOT` 指 /tmp 隔离审计），不碰别人在跑的 :8000。验收发现 **F1-F8** 见 `docs/A_LINE_LOGIC.md` §8.4/§8.5（**全部已修并经真栈复验**），最要紧三条的落地形态：①**补录音色错位**（人设 `account_id` 取 body 不取 query → 落空账号 → `pregen --persona` 找不到就**静默回落默认音色**，缓存键与运行时错位「看起来 ok 实际不播」）：现建人设缺账号兜底本账号、`pregen --persona` 找不到 → `PERSONA_MISSING` **exit 3 零落盘**、状态面加 `voice_source` 信息位（三态语义未动）；②**迟到 FINAL 掐断快路回复**：`late_final_is_new_speech`（AI 忙 + 极短尾巴 ≤`BOK_LATE_FINAL_MAX_TAIL_CHARS` → 丢，数字/字母 run 永远成轮）+ **收线/告别直念窗不打断**（告别说完优先），总闸 `BOK_LATE_FINAL_GUARD`；③**热词抄词误收线**（客户说「我还想想」被 ASR 抄成词表「打错电话」→ 真挂断）：破坏性动作双护栏——`refuse` 要求**条件字面命中 或 verdict=REFUSE**（纯家族/bigram 模糊命中不再能挂电话，多选条件任一项命中即可）+ **热词幻觉最终否决**（整轮/末子句剥词表词后为空或剩余过短 → `refuse_skipped reason=hotword_only`），两枚回退口 `BOK_BRANCH_REFUSE_CONFIRM` / `BOK_BRANCH_REFUSE_HOTWORD_GUARD`。**回归门槛**：改 F2/F4 相关必须跑 `scripts/e2e_barge_in.py`（真打断，基线 `interrupted=yes stop_ms≈2.4s resumed=yes`）——两条护栏都不得挡真插话；探针取 mark 前会等 `agent.log` 落盘稳定（窗口 race 曾把 warmup 轮身份步的合法推进算进触发窗，导致 hold 腿假 FAIL）。
- **ASR 吃字与回声四件套（2026-09-12，call-46b94ebd/快语速专项）**：①词表回声统一闸 `_echo_filter`（四闸口：停嘴/join-flush/interim/句级提交）=剥尾保头——真答案词恰在词表（答「拼多多」被吞根因）；无分隔符纯顺串保留贪心整丢、有分隔符短真答案（「拼多多，京东。」）豁免。②START pre-roll 喂会话：官方 silero 把 0.5s 前导+确认窗音频挂 START 事件 frames，新开会话并入 `_pending`（hold 续段不并防重复）——旧版快语速首 1-3 字结构性缺失、finish 救不回。③`_uncommitted` 长度感知取尾：finish 比 committed 长 >3 字且高相似=同头+新尾，diff 对齐截尾照发（旧版整条丢实测 19 次/387 字）；长度相当才当纯重解丢。④纯犹豫残片门（`QWEN3_ASR_HESITATION_GATE=0` 回退）：呃呃呃呃类 ≥2 语气字不成轮（成轮必发垫话+打断在途回复）；嗯/好/係单字应承豁免。验收探针 `scripts/probe_fast_speech.py`（1.4× 速首字存活+词表内真答案成轮，注意推尾静音）+ `scripts/probe_reply_quality.py`（真实轮次回放打本地 LLM 三断言）。通话质量诊断走 skill：`.agents/skills/call-diagnosis/`（症状速查+标记词汇表+代码地图+probe 用法）。
- **LLM system 顺序（KV-cache 关键，勿打乱；2026-09-05 二次实证修订）**：请求组装=`render_instruction_prefix()`（整场静态：语言规则/节奏/准则/话术总览/**对象档案**）+ 人设 base 作 system；**易变尾部（当前步/记忆/CONTEXT_RAG 时的知识联网）采用「跨轮纯追加」：每条 user 消息首次出现时拼上当时的尾部并由 `ContextState._applied_tails` 账本冻结，此后每轮原样重放（ContextAwareLLM）**。铁律=上一轮请求必须是下一轮的**严格前缀**（mlx_lm LRUPromptCache 只复用严格前缀；旧「尾部只拼最后一条 user、下轮即剥」会在中途分叉→cached 恒=system 锚点、历史每轮全量重 prefill——2026-09-05 INFO+指纹实证后改为账本冻结重放）。抢跑重建轮（n_new==0）若尾部在末条 user 拼上后已实质变化（ContextState `revision` 计数：set_flow_current/set_whatsapp_note 变更才 +1），只把**末条 user** 尾部重渲染成当前版并同步账本（否则推进后回复照旧步讲，2026-09-06 实证）；转写被修正（提交文本≠账本 orig）→ 末条 user 按**当前文本+当前尾部重锚定**（跳过会令该轮连尾部都丢、其后每轮 replay 全跳过）。**框架步骤标记（`[流程状态]` system 消息）只准留在框架侧做抢跑失效触发，`ContextAwareLLM.chat` 出口统一剥离**——它本轮在、下轮无，进请求流会令下一轮喺同一位分叉 → cached 钉死 system 锚点、每轮全量重 prefill、TTFT 随轮次 1.2s→8.5s（2026-09-06 指纹实证 call-b882cd69，探针 `scripts/probe_llm_cache.py` + `BOK_LLM_MSG_DEBUG=1` 复现与验收：修复后 cached 逐轮增长 966→1490→2067、TTFT 压平 ~1.5s）。开场白/心跳/收线一律 `session.say()` 脚本直念——`generate_reply(instructions)` 的临时 system 消息下轮即剥=前缀断裂，官方 filler 姿势用 say。**开场白=话术第 1 步 ref 首行直念**（`FlowController.opening_text()`：变量已替换、「\n如果客户…」分支指引不念出声；模板语言≠通话语言或变量缺失→退通用语；念完置 `opening_played` 并重渲染 `set_flow_current` → turn-1 尾部带【开场已念】提示防重复开场；三语模板各自第 1 步即各语言开场，EN 占位 {name}/{courier}/{tracking_tail} 已入 object_vars 别名）。**预热与开场白 TTS 并行起跑**（greeting_text 直传预热任务、不再等 playout；`session.say()` 返回=整段念完而非出声，打点 `greeting_playout_done`）。首轮预热用「真实开场白文本作 assistant 轮」的 turn-1 同构形状（`_build_prefix_prewarm_messages`），system 串联必须复刻 to_provider_format 的 `\n` join。历史截断是摊销式（超 2×max_turns 才截回 max_turns，`LLM_HISTORY_TURNS=8`）。会话首轮冷 prefill 由 `LLM_PREFIX_PREWARM=1` 真 system 预热吸收。`LLM_TTFT_MS` 日志带 `cached=N/M` 命中读数。
- **知识=沉淀非逐轮注入**：提示词只含对象信息+话术+session 记忆。知识/联网检索**默认全关**（`_context_rag_enabled()` 无参默认 False，`CONTEXT_RAG=1` 才开且只渲染进尾部 `rag_enabled` 段；`WEB_SEARCH` 默认 0）。沉淀知识经 `ContextState.set_object_brief()`（≤2 行×150 字，显式换行分界、多句整行截断不丢字段）在装配时渲染为静态【对象档案】入前缀。会中事实（平台/号码）每轮经 `flow.extract_call_facts` → `add_call_fact`（去重有界 ≤4）沉淀进尾部【通话中客户已讲】，配 `set_last_reply` 的【你上一句】重复锚——治「忘记早轮信息」与「原句复述」（2026-09-06 行为取证）。
- **VAD/endpointing 基线（2026-09-05 句号级提交）**：`min_silence=0.45` / endpointing `min_delay=0.25`/`max_delay=0.6` / `min_speech=0.15`；A 线 `turn_detection=stt`（STT 句末 `END_OF_SPEECH` 提交，说话中按句成轮，LLM/TTS 与说话重叠——≤1s 通路）。当年压端点致哑火=轮次在离线 ASR final 前提交；现提交结构性等待 STT FINAL 且句级路径 FINAL 即句文，三语 E2E 实证 0 丢转写。**回退须成对**：`TURN_DETECTION=` 置空回 EOT 档 + `QWEN3_ASR_SENTENCE_COMMIT=0`（B 线 2026-09-16 起同默认开、同配对 kill-switch，见 B 线同传条）；**kill-switch 档 endpointing min_delay 自动回 ≥0.35（`_endpointing_delays_from_env` 强制，无需手动）**——0.25 只对 stt 句级提交校准过，未校准 EOT 配 0.25 早提交截断粤语（p6 实证）。句级保护：≥6 字/无 ≥2 连续字母数字串/双窗稳定/1.5s 限流/小数点含窗口末尾不劈句；真实音频滑窗句间只出逗号 → VAD 停嘴（≥0.45s 微停顿）也是句边界源（`QWEN3_ASR_SENTENCE_PAUSE_TRIGGER` 默认 1）；**vad-pause 提交字数下限 10（`QWEN3_ASR_PAUSE_COMMIT_MIN_CHARS`，标点路径仍 6）**——微停顿只证明喘气，6-9 字碎片当整轮提交会被下一碎片掐掉在途回复（2026-09-07 碎片提交门，扣住并入下边界/停嘴兜底）。改这三处默认值要同步 agent 环境默认 + `repository.default_settings` + 设置页表单默认（web `EMPTY_FORM`；2026-09-14 起四张引擎卡收进设置页底部「开发者参数」折叠区，默认值不改）。
- **打断 = 官方误打断自愈组合**：`interruption.min_duration=0.6` + `resume_false_interruption=True` + `false_interruption_timeout=1.0`（真插话 0.6s 让位；1s 内无转写=噪声误打断，AI 自动从暂停处续讲）。旧 1.2s 高门槛已废（压住真插话）。`INTERRUPT_MIN_DURATION`/`RESUME_FALSE_INTERRUPTION`/`FALSE_INTERRUPTION_TIMEOUT` env 可回退。回声自听守卫：AI speaking 中收到与上一句（ContextState.last_reply）归一化相似度 ≥0.9（两边 ≥6 字）的用户轮 → 判 AEC 失效自听，日志 `QWEN3_ECHO_SELF_HEARD_DROP` + `StopResponse` 丢弃整轮（`QWEN3_ECHO_GUARD=0` 关）——治外放+输出设备失效时 AI 自闻自答；AI 讲完后的复述/引用不拦（条件钉死 speaking 中）。**livekit-agents 已升 1.8.0**（锁 <1.9）：1.7.1 的 playout pause 泄漏实测一次打断后本通所有回复哑火只剩心跳（call-e49bd34d，LLM 照常出文本、TTS 流开了不推音频、agent 卡 listening），官方 1.8.0 #7083/#7087/#6865 恰为此修——当年锁 <1.8 的 pre-release flake 理由已失效（GA 版 311 测试全过）。**心跳钩子（`_arm/_disarm/_fire/_on_agent_state/_on_user_state` 与 `session.on` 注册）必须先于 `session.start`/开场白**——开场白播完的 listening 转换发生在注册前会令首段沉默 arm 失效（2026-09-06 审查 P1）；开火护栏抽成纯函数 `_nudge_should_fire`（答案在途窗口 ≤2×delay 唔开火，test_heartbeat.py）。心跳/收线文本=脚本直念 `_nudge_line`（三骨架轮换）/_farewell_line，零 TTFT 即时补位、零前缀断裂（旧 generate_reply 版 4B 照抄例句两连发一字不差，2026-09-06 实证）。**测试对象心跳豁免（2026-09-09 #45）**：对象 `display_name` 命中 clean-testdata 同款前缀族（`^(E2E-|soak\d*-?|并发|LOAD-|边角-|多轮-|probe)`）→ 心跳整条关（`_effective_nudge_max`=0：arm/farewell/12s 自动收线全不挂），`BOK_E2E_NUDGE_IMMUNE=0` 逃生——E2E 的 greeting→用户语音间隙必跨 8s 心跳线，nudge 7-9s 音频会越过脚本 `agent_audio.clear()` 污染断言窗（call-6ba7c47c 实证）；豁免后 barge-in resume 断言首次测到**真实** reply2（5.3-6.1s；旧基线的 16.07s「恢复」实为 nudge 凑数假绿）。
- **VAD 微停顿短尾不补发**（打断自噬，2026-09-06）：pause-commit 已发句级 FINAL 后，<6 字短尾（「係。」）不再补发第二条 FINAL——否则新用户轮会 interrupt 掉生成中未出声的回复（「每问无答」根因之一）；≥6 字照发。**重解修正也唔补发**（2026-09-06 call-58601bba）：vad-pause 提交的半截转写（「这是我的牌」）与 /api/finish 整句修正（「这是我的快递」）唔係前缀关系时，`_uncommitted` 按归一化相似度 ≥0.55 判定为同一句话的更好重解 → 丢弃（QWEN3_ASR_REDECODE_DROP 日志）——补发迟到 FINAL 会在框架 `on_final_transcript` → `_interrupt_by_audio_activity()` 掐死生成中的回复（gen 完成但零音频零转写，只剩心跳）；归一化前缀对齐的真续句照发。
- **拆句组装 = 跨段 join-hold + WA 号码累积 + WA 步假确认护栏**（2026-09-06 拆句调查，call-c4f6e4f1/839ec9db 实证）：报号句中微停顿会把「我的WhatsApp係…六四三二…」切成两个 FINAL/两个用户轮——语境与数字分居两轮，捕获结构性失败、AI 对每段插话复读/捏造拼接（"03201"）。三层修复：①STT 层**跨段拼接 hold**（livekit_plugins `_join_worthy`/`_hold_flush`）：续接可能句（归一 ≥2 位数字/系词係|系|是|is 收尾/**热词词表某词的严格前缀**）喺 END_OF_SPEECH 唔发 EOS、唔 finish——sidecar session 续命等下一段并入同一会话，真正停嘴出一条完整 FINAL；超时 flush 照常出（该轮多等 ≤HOLD ms）。`QWEN3_ASR_JOIN_HOLD_MS`（默认 800，0=关）；词表前缀门 `QWEN3_ASR_JOIN_HOLD_VOCAB`（默认 1，0=关，2026-09-09 S3 拼多多专项：品牌词内部微停顿「京|東」「拼|多多」拆轮烂词，「京」⊂词表「京东」→ hold 并单会话整句解码；词表=模板 hotwords+行业词+对象 courier/contact_channel 同一份，运营加词即生效；probe_brand_words 基线 10/16→14/16，三语 E2E+barge-in+edge 同绿收编；剩 2 miss=小碎片被 ASR 源头解码烂（金≠京）非切分问题）。②agent 层**号码累积**（`BOK_WA_ACCUMULATE=0` 回退）：收号码步内数字主导句（`_wa_numberish`）累计 <8 位、或自报头碎片（`_WA_ANNOUNCE_HEAD_RE`「我的WhatsApp係」零数字）→ 暂存 + `StopResponse`（唔回复唔推进唔侦测；**必须在 except-pass try 之外 raise**），下一段拼埋一次过处理；5s 超时 flush 跑侦测，captured 则上报+`session.say` 复述确认（`_wa_number_line` 三语脚本直念），唔系则请客户继续——唔可以赖 8s 心跳（护栏会当答案在途压住）。③flow 侧：**WA 步 CONFIRM 假推进护栏**（抽成 flow.py `wa_confirm_advance_allowed`，agent.py rule CONFIRM 分支与背景 judge CONFIRM 分支两路共用：`_looks_like_whatsapp_step` 且未 captured → 唔 advance——碎片「我的WhatsApp是。」尾部裸係/是命中 `_CONFIRM_RE` 会假确认、4B judge 对模糊轮误判 advance 亦会假确认（c4f6e4f1 的 judge=confirm 正係 judge 路），都唔可以越过收号码步）；**捕获下限 4 位**（`_valid_digit_runs` min_len=4，1-3 位=ASR 碎片唔收；5 位测试短号「一二二三三」旧 6 位门槛曾全数走漏→唔爆闪，用户拍板「报出就收」）；`_digit_normalize` 收英文数字词（zero/oh/one…nine，en 号码词旧版结构性无法归一）；announce 正则容忍系词与数字间接缝标点（拼接文本「係。一七二…」）。
- **ASR 流式 partial + 抢跑**：sidecar mlx 后端 `QWEN3_ASR_STREAM=1`（默认）每 ~700ms（`QWEN3_ASR_PARTIAL_MS`，旧 400）滑窗出 partial、窗口 ≤12s、**FINAL 后停发**（削 GPU 租户）；**会话级 partial 抑制（2026-09-08 GPU 竞态专项）**：回复生成/播报中（thinking/speaking）agent 经 `Qwen3ASRLiveSTT.set_partial_ms` 把该通会话 partial 间隔抬到 `BOK_ASR_PARTIAL_SLOW_MS`（默认 3000，0=关），listening 恢复——同卡上持续 partial 全窗重解实测拖慢 LLM TTFT +24%、被 LLM 反拖 2-4.6×，这是「用户长讲时回复变慢」的主根因；打断係 VAD 判定不受影响。**抑制档下增量拼接收紧**（partial ≤1.2s 新鲜、尾段 ≤1s，否则整句兜底）——陈旧 partial + 无上下文尾段独立解码会拼出幻觉尾巴（实证「切把狗领…好啊好」）。agent `Qwen3ASRLiveSTT` 发 `INTERIM_TRANSCRIPT`（字幕）+ 稳定前缀（首 ≥6 字、增长 ≥4 字才发）→`PREFLIGHT_TRANSCRIPT`（语言不匹配抑制 `QWEN3_ASR_PREFLIGHT_LANG_GATE`，每通语言固定后基本闲置）；停嘴增量 finish（partial 拼接+数字串/接缝保护回退整句，WhatsApp 零降级）；en finish 走 `English` hint。**抢跑防抖（2026-09-10）：框架抢跑默认关（`PREEMPTIVE_GENERATION=0`，=1 回旧行为）**——实机 843 次失效/0 次命中（agent.log 全量）：插件 PREFLIGHT 只发稳定前缀（防幻觉尾巴）而提交转写是全句 FINAL，框架 `_transcripts_equivalent` 逐词相等恒假=命中结构性不可能；且 max_retries 烧穿后新 PREFLIGHT 先 cancel 旧快照再 bail，commit 时连比较都不跑；失效的投机请求被 mlx「无断连中止」解码到完才放锁，解码尾巴挤占真请求队列。替代=**PrefillSpeculator**（`prefill_speculator.py`，`BOK_PREFILL_SPEC=1` 默认开）：说话中按稳定前缀发 out-of-band max_tokens=1 预热请求（prompt=上次真实请求逐字节快照+assistant 历史原文+user(前缀+尾部)，严格前缀契约；MlxLlmLLM.on_request_messages 快照钩子；busy 门控/每轮限 2 次/间隔 ≥600ms，`BOK_PREFILL_SPEC_DEBUG=1` 看拒绝原因），真轮只 prefill 分叉尾巴；`PREEMPTIVE_TTS=0`。诊断探针 `BOK_PREEMPTIVE_DEBUG=1`（transcript/ctx 比较打点+speculate 闸门归因，须装在 job 进程=entrypoint 内）；复测 `scripts/probe_preemptive.py`（长句喂 PREFLIGHT 链，E2E 短句结构性发不出 PREFLIGHT）。**推进轮不错步**：步骤推进/REFUSE 时向 turn_ctx 落步骤标记（抢跑关闭后为无害残留，回退框架抢跑时恢复失效触发作用）。ASR 模型维持 **8bit**：4bit（快 ~24%）A/B 实证数字路径同音字滑失（九→狗/號→后，同渲染音频 8bit 逐字全对）触碰「数字零降级」铁律被否——`model_path` 按目录存在性解析 .lmstudio/app-data 的断层已修。
- **ASR 热词/context 软偏置**：Qwen3-ASR 官方 customizable context（system message 词汇表「Vocabulary: …」，与 language 强制可叠加，mlx 层 `generate(system_prompt=)`）——治领域词误听。**实证（2026-09-07）**：e2e_edge_cases E2「我個單號係三七七八九零」前导误听（main/PR#31 三轮 ctx=False，「單號」聽成「打啊」）开启后 ctx=True 修复、7/7 全绿、真实通话轮零词表污染；context prefill 开销实测可忽略（真实长语音 A/B 无差），但**极低内容音频直打 sidecar 会把词表当转写抄出来**（纯静音探针稳定复现）——生产路径靠 VAD 门控+语音内容天然规避，留意短应承场景回归。来源三层：话术模板 `templates.hotwords` 字段（2026-09-08 二期落地：运营按套话术维护、web 话术表单可编辑，逗号/顿号/分号/换行分隔，超限截断时优先保留）+ 行业静态词（`agent.py` `_ASR_HOTWORDS` 三语各一套，话术域高频词）+ 对象文字字段（`courier`/`contact_channel` 专名类）；**数字串唔进**（biasing 幻听数字风险，下游 known-number 过滤兜底）；总长护栏 **≤200 字符**（2026-09-17 由 120 放宽：防诈/异议域词入表后粤语档 113 字、prefill 成本实测 +2~8% 无感；截断策略同步修正=行业段整段保底、模板词瓜分剩余——旧「模板词先入列超限 break」会令行业表尾静默截掉运营词，字符串位次契约不变）。**防诈/异议域词（2026-09-17 offscript 轮入表）**：詐騙/呃人/證明/機器人/投訴/主管/人工/轉接/退款/倉庫/熱線/官網 三语各一套——`probe_hotword_ab.py` 三档 A/B 实测过线词真实受益（機器人/邊度/熱線/證明类）；**但嬲/賠償/單號/倉类在词表内外、多档渲染下都错**（**2026-09-21 云话音重取推翻其适用范围：三档 10/10 无差、相似度 .929/.929/.940——「过线词受益」只在本地话音的碎裂面上成立，边际收益随刺激源而变；`extended` 档 prefill 成本 +14.2%，见计划档 §37.4**）。**模型档位结论（2026-09-17 bf16 A/B 判空）**：`mlx-community/Qwen3-ASR-1.7B-bf16` vs 8bit 同音频双端点逐字对比 5 句 3 全同 2 差一字=**零精度增益**，且 bf16 慢 ~30%（243→325ms）、+1.7GB——**8bit 维持**；碎裂句探针读数有 **±2-3 句 TTS 渲染方差**（同模型跨次合成 4/10↔6/10 实证），探针结论必须跨渲染采样/背靠背同音频对照先可信。剩余硬混淆的下一杠杆=云端同家族大杯（Qwen3-ASR-Flash，待 key 试点）。装配时 `asr_hotword_context(asr_pin_lang, object_card, extra_hotwords=template.hotwords)` 一次性组装，`Qwen3ASRSTT(hotword_context=)` 随 `/api/start` 下发、sidecar session 级透传每次解码。两级 kill-switch：`BOK_ASR_HOTWORDS=0`（agent 唔下发）/ `QWEN3_ASR_CONTEXT=0`（sidecar 唔透传）。改词表：行业层动 `_ASR_HOTWORDS`，套话术层动话术表单热词输入框。
- **罐头音三件套（TTS 缓存/垫话/QA 快路，2026-09-09 #38/#40/#43/#46）**：本地 TTS 音频缓存 `tts_cache.py`（`CachedTTS` 只包 `synthesize()`=脚本直念线；key=text+voice+model **精确匹配**——运行时人设/音色不同即 miss 优雅降级走 LLM，pregen 用 lang_personas 音色映射与运行时同源；`BOK_TTS_CACHE=0` 关）。**垫话** `fillers.py`：慢生成轮回复首音频 `BOK_FILLER_DELAY_MS`（默认 500ms，用户实选定档）未到 → **`BackgroundAudioPlayer` out-of-band 音轨**播预合成应承语——**绝不能走 `session.say()`**（1.8 speech 队列严格串行，垫话必排回复后，实机实证）。**2026-09-10 资产化+播放排序改版**：垫话=随源码分发 wav 资产（`assets/fillers/`+manifest.json；`scripts/gen_filler_assets.py` 固定音色 zh=Chinese_wenrounvxing/粤=Cantonese_crisp_news_anchor_vv2/en=socialmedia_female_2_v1，speed 1.2(zh/粤)/1.0(en)+pitch 0 预生成，实测时长 1.0-1.5s 窗校验，超窗打回）——与人设音色/tts_cache 解耦；**2026-09-16 池深补缺+cat 入源（60 条=每语言 20=s17+l3）**：gen 脚本行格式改 `(text, cat)` 五类标签（empathy/ack/check/minimal/default，对齐 `classify_filler_category`）随 dict→manifest 全权产出——此前 cat 是生成后手补 manifest、再生成即洗标签的陷阱堵死；文件名 tier 前缀 `s/l` 防插行错位；补缺前 zh minimal/default=0、粤 minimal=0（default 仅 1 条=call-c76832ac 同句连播主角）、en empathy/ack=0——分类器选不出即回退 check 主导整池=语境错位；en 定档**真实语速 ~0.28s/词、上限 7 词**（10 词首轮全爆窗；`I understand let me check it now.` 三轮 2.6-4s 抽奖后 1.65s 过=同句方差大，超窗重跑 lottery 是正解，勿硬砍语义）；长句 tier 沿革：短句 1.0-1.5s 只盖 ~1.5s 慢生成窗，长句 1.7-2.3s（实测均值 zh 2.22/粤 2.15/en 2.01s，独立时长窗校验超窗打回）把链发覆盖上限拓到 2.3s；en 音色实证任何停顿（逗号/句号/静音标记）都令相邻词拖腔 +1.8s（逗号版 3.4-4.2s 三轮复现）→ en 长句定版**零停顿连读**；**播放排序=垫话播完→`BOK_FILLER_GAP_MS`(默认 300ms)→回复**：不再掐垫话，回复首帧由 `_RelaySynthesizeStream` 向 `hold_if_playing()` 询问「垫话剩余+gap」帧缓冲到点再放（内芯 iterator 惰性天然背压）；**语言铁律**=垫话语言=装配时钉死的 `greet_lang`（en 通话曾因运行时 lang 漂移落 zh 池放出普通话垫话——用户实测怪声根因），池缺失明文日志跳过绝不跨语言；`BOK_FILLER_LINES` 文本覆盖口已删（改话术=改 gen 脚本重新生成）；**垫话双层出声（2026-09-10 task-14b 复活）**=先查 tts-cache 人设物化版（`tts-pregen --fillers` 按人设音色物化，命中=与通话完全同人声），miss 播源码资产兜底+后台补物化（日志 `BOK_FILLER voice_fallback`=缺料提醒）；每通限次 `BOK_FILLER_MAX=3`（代码默认 6=2026-09-13 乙节放宽，文档待对齐）；**2026-09-17 call-11132bdd 垫话听感三修**：①**让话家族清污**——`filter_deflect_entries` 在 manifest 加载与罐头索引双入口剔除「繼續講/聽住/聽緊/go ahead」类条目（风暴/starve 让话语义混进 ack/minimal 桶，客户报号码/应承轮被答「继续讲我听着」=语境错位主根因；资产文件保留，选池剔除，日志 `BOK_FILLER pool hygiene`）；②**连轮冷却**——`FillerDirector` arm 序号计数，相邻轮只垫一轮歇一轮（8 轮垫 6 轮=轰炸感），`_turn_seq=0` 直调（旧测试/嵌入方）不适用，链发豁免；③**fallback×垫话互斥**——`MlxLlmLLM.set_fallback_gate(lambda: _filler.fired_this_round())`：垫话已盖耳的轮抑制 2s 超时的流内道歉句（「垫话+道歉+晚到真答案」三连叠音根治），靠 drain/晚到补答交付，drain 无产出落 watchdog（垫话开播已顺延），日志 `LLM_FALLBACK suppressed (filler covered)`；**自动链发第二垫话**（2026-09-10）：首条播完、回复首音频仍未到 → gap 呼吸后自动补一发（`PlayHandle.wait_for_playout()` 播完即醒精确补位，不靠估时长；`BOK_FILLER_CHAIN` 默认 1，0 关；每轮链发封顶 1 次，与主动 arm 消耗同一计数——MAX 2→3 即为「单轮至多 1+1 仍给后续轮留 1 发」，默认 2 时一次链发即耗尽全通）；A 线 `min_consecutive_speech_delay=0.3`（speech 队列相邻两条播报最小间隔 0.3s，开场白→回复/心跳衔接不再背靠背机械感；垫话走 out-of-band 音轨不经此队列零影响，B 线不加）；out-of-band 不入 LLM 上下文（KV 前缀/回声锚零污染）；`BOK_FILLER=0` 关。**Q→A 检索快路** `qa_gate.py`：用户轮命中 `qa_entries` 条目（0.6×HybridLexical 余弦+0.4×子串，阈值 0.90 **只认字面措辞**——同义改写要按说法补词条）+ 四道闸全过（digits/refuse/wa/closing/advanced/verdict 全旁路）+ 应答音频已物化（`tts-pregen --qa`）→ 跳过 LLM 直播缓存 ~50ms；词库增长靠 `reports/qa-pairs` 真实转写挖掘（`tts-mine --apply`，source=mined）；**同义聚类（2026-09-16 `tts-mine --cluster`，治「0.90 只认字面措辞」）**：本地 LLM（:1235，`/v1/models` 自动取 4B 真实路径，端点钉本机回环 `BOK_LLM_PORT`）把挖掘候选对着现有词条判 variant/new/junk——variant 以**候选原话**为 question、**继承目标词条 answer_text** 入库（真实措辞即匹配面；同文同音色 → TTS 缓存键同条目零重合成），默认 dry、配 `--apply` 落地；new 留给 `--sync` 质量闸、junk 报原因；**问法长度门（AUTO_MIN/MAX_Q_LEN）在 plan_cluster 处决 4B 碎片误判**（实弹:「啊」「多多」被判 variant，字数门全拦；纯函数 plan_cluster/_parse_llm_decisions 离线可测 tests/test_qa_cluster.py）；**匹配优先级（2026-09-18 Phase 3.1）**：`qa_entries.priority`（小者先，[0,1000]，默认 10=与 graph `DEFAULT_PRIORITY` 同约定）——胜者键=**阈值过关者中** `(priority, -score, 插入序)`（阈值先行：不过关永不出线），全默认时与旧纯分数档逐字节同（含平分吃 created_at），`BOK_QA_PRIORITY=0` 一键回旧档；CP 落库前钳制、web 表单可编辑（≠10 时列表/画布出 P 徽标），验收探针 `probe_qa_hit --priority-duel`（离线三档对照）；**多答案轮换（2026-09-18 Phase 3.2）**：同义簇（`qa_entries.cluster_head_id`）在 `QaIndex` 装配折组——变体命中由 **head 代表出场**（只换出场代表，胜者键/分数不变；head 不在索引的孤儿/自指变体退独立条目=旧行为），簇内按**本通最少播放**轮换取成员播（账本 `FlowController.qa_played` 真播出才记，与 `graph_fired` 同纪律，容量 64 截断；graph `by_id` 播放不入此账本——运营钉死 id 的播放不参与轮换计数），无 PCM 成员沿轮换序下移（`agent.py::_qa_rotation_plan`，全组无 PCM 照旧 `no_audio` 落 LLM），**graph `by_id`（运营钉死条目 id）不轮换**；`BOK_QA_ROTATION=0` 一键回旧档（变体自己赢、播自己的答案），验收探针 `probe_qa_hit --rotation-duel`（离线三断言：折组 / 账本轮换位移 / kill 裸胜）；`BOK_QA_FASTPATH=0` 关。**尾部锚拟声剥离** `_StripTailAnchorStream`（`ContextAwareLLM.chat` 出口包一层，跨 chunk 状态机）：4B 会把尾部【你上一句】「…」模板块照抄进回复输出（call-974d8da3 念出声实证，C4 情绪标签同族）——出口单点剥离，TTS/历史/turns/重复锚四下游全干净，渲染不动（锚的重复控制功能保留）；命中打 `TAIL_ANCHOR_MIMIC_SUPPRESSED`。**人设保存点自动物化（2026-09-10 W3，用户 mandate）**：CP `POST/PUT /api/personas` 音色相关变化（reference_audio/tts_provider/language 任一；内存 repo 返回活引用，existing 必须冻结快照再比）→ detached 子进程跑 `scripts/pregen_tts.py --greetings --fillers --qa --persona <id>`（幂等重跑零云调用；单飞防叠发=MiniMax 60 RPM；`BOK_PERSONA_AUTO_PREGEN=0` 关；失败/打包缺脚本绝不阻人设保存）；响应 `tts_pregen.status`=提醒面（queued/no_voice/script_missing/already_running/unchanged/disabled/failed），运行时逐轮提醒=agent.log `BOK_FILLER voice_fallback`。**罐头集钉住不逐出**：pregen 物化的 greetings/fillers/qa 落盘 `pin=True`（meta.pinned），`_evict` 只淘汰未钉条目——逐对象开场白这类无界动态条目（tee 默认不钉）灌满 LRU 也不会把罐头挤出、音色一致性静默破功；运行时 tee 落盘保持不钉。
- **B 线同传句级出稿 + manual 自驱管线 + 术语表（2026-09-16 P0/P2，对标调研后落地，E2E 8/8 实证）**：①**句级出稿**——B 线 `QWEN3_ASR_SENTENCE_COMMIT` 与 A 线同档默认开（bok.py `_interp_env` setdefault，显式 0 仍是应急逃生口），`interpret._turn_handling_opts` 单源复用 A 线 `_turn_detection_mode_from_env`/`_endpointing_delays_from_env` 校准句级出稿（endpointing 0.25/0.6；框架层 P2 起升级为 manual 自驱管线见⑨）——说话中按句成轮翻译，粒度从「停嘴整段」提前到句级（同传粒度对标业界全员「说话中出稿」）。②**打断恒关（`BOK_INTERP_INTERRUPT` 已随 P2 manual 架构废止，env 不再是恢复口）**——同传语义下源说话人续讲≠抢话：A 线打断语义会整轮取消生成中/播报中的译文（首轮 E2E call-b79e1f1b 实证 fwd 译文被吞 3/8）；而 P0 的「只关打断」档同样错——框架对「播报中到达的新用户轮」只有打断或**整轮丢弃**两条路（agent_activity `skipping reply to user input, current speech generation cannot be interrupted`，连原文落库都没有，probe_interp_backlog 首跑 8 句只落 5 句实证），两条都唔係译员行为，P2 起由 manual 自驱管线整体取代（见⑨）。③**术语表全链**——web 同传页 glossary 输入（「源=译」/纯词条，逗号/分号/换行分隔）→ `POST /api/calls`（1000 字截断）→ `call_sessions.glossary`（deps 幂等补列 + `.p0_supabase_schema.sql` 已重跑）→ fwd/rev dispatch metadata → `parse_glossary` 双路注入：ASR `asr_hotword_context(include_industry=False)`（B 线不吃 A 线快递行业词，数字主导项仍丢）+ MT `_mt_prompt` 术语槽（会话级常量进前缀、KV 友好，空串逐字节同旧模板）；回退 LLM 路径走 instructions glossary 行。④**MT 出口引号剥离（`_StripMTQuoteStream`）**——Hy-MT2 系统性给译文裹反引号（镜像模板 ` 包裹，:1236 直打 100% 复现）+偶发弯引号，首尾独立剥（流出口单点，TTS/字幕/历史全干净；一字符 hold 延迟≈一个 chunk）。⑤**MT 滚动上下文 A/B 档（`BOK_INTERP_MT_CONTEXT`，默认 0=关）**——非零带最近 N 对「源→译」进 prompt 上文参考块（LLMA 式，`_rolling_pairs` 现场重抽零内部状态）；:1236 直打 A/B 指代/术语中性偏好，probe 实测延迟代价≈0（avg 2463 vs 基线 2562ms，prompt cache 吸收参考段位移），默认仍 0 待更多通话质量取证。⑥**延迟探针 `scripts/probe_interpret_latency.py`**——逐句感知 lag（译文语音起点−源句语音结束，AL 工程化简版），分两段推流+段间 0.7s 真静音，主判据 avg/逐句 ≤`BOK_PROBE_LAG_BUDGET_MS`（默认 3500；基线实测 avg ~2.5s PASS），ASR 回读仅信息位；改 MT/分段/打断后必跑。⑦**`_interp_env` 显式透传 `BOK_INTERP_MT_CONTEXT`/`BOK_INTERP_REV_AUDIO`/`BOK_INTERP_BACKLOG`/`BOK_INTERP_MAX_BACKLOG_S`**——`_agent_worker_env` 是白名单 env 不带 os.environ，不透传则 B 线逃生门在 dev/prod 栈都是死的（探针 A/B 实证后补；`BOK_INTERP_INTERRUPT` 已随 P2 manual 架构废止）。⑧**同传纪要（2026-09-16 P1）**——`summarize.Summarizer` 按 `call.kind=interpret` 切 `_SYSTEM_INTERP`（双语对照/我方对方框架；B 线 turns 天然「原文：/译文：」分行=对照输入现成）；**en 译文播放期变速不可行已记录**——译文放音 2026-09-12 起全走 AudioContext（crbug 40647375），WebAudio 无 preservesPitch 变速=花栗鼠音，合成侧变速又被语速统一铁律钉死，两路都不动。⑨**manual 自驱同传管线（2026-09-16 P2，probe_interp_backlog 实弹 orig=5/8 逼出）**——`turn_detection=manual`：框架不再自动回复，worker 监听 `user_input_transcribed(is_final=True)` 收句级 FINAL → 单消费 FIFO 队列 → `_mt_once` 直调翻译 LLM（conn_options 必显式给 `APIConnectOptions(max_retry=1)`，直调传 None 会 AttributeError max_retry of None；单句 15s wait_for 防句堆积）→ `session.say()` 排队串行播，MT 下一句与播报上一句流水线重叠，重试是延迟放大器积压归背压管；落库面不变（say 的 assistant item 照发 conversation_item_added→译文行，原文行改由 final 监听即时落，`_on_item` user 分支只兜收线残余防重行；译文 latency 从 llm_ttft 换成直调计时）。kill-switch 配对同判：`TURN_DETECTION=stt` 才走 manual，vad/""→不带 turn_detection key 回 EOT 自动回复旧档+句级 FINAL 熄火，manual 与句级 FINAL 同进同退无错配组合。manual 下 STT final 事件照发与模式无关（audio_recognition.on_final_transcript 先 emit 再做模式分支，E2E 实证）。官方姿势核实：`RoomOutputOptions.sync_transcription` 1.8 默认即开（NOT_GIVEN≠False 且有音频输出就挂 TranscriptSynchronizer，无需显式设）；`use_tts_aligned_transcript` 只对带时间戳的 TTS（Cartesia/ElevenLabs 系）生效，MiniMax 插件不适用；MultiSpeakerAdapter 是多方多语房间布局，B 线 1:1 双 worker 不适用。⑩**译文播放背压（`_PlaybackBacklog`，v1 PlaybackScheduler 的 maxBacklogMs 门移植）**——say 队列估时（zh 字/5s、en 词/2.6s、非空地板 0.8s；say 句 chat_item 即刻在场估时准）超门槛（`BOK_INTERP_MAX_BACKLOG_S` 默认 6s）从最旧 force 中断未开播句（interrupt 必须 force=True——会话打断关时非 force 会 RuntimeError），队头（播报中）与最新一条永不弃，depth<3 结构性不触发；`BOK_INTERP_BACKLOG=0` 总闸关。实弹探针 `scripts/probe_interp_backlog.py`（长句**无逗号**先行+短句 7-9 字 0.85s gap 连发——逗号会被 vad-pause 劈轮、<6 字句被句级保护吞、0.6s gap 会被下一句开头粘连，全是实测过的刺激物坑；主判据 drop≥1+原文零丢；低门槛 1.0s 实弹 `drop=1` 落地、队头与最新保住）。⑪**语气词标记 + 听感开关（2026-09-16）**——译文 TTS 真人化：`_apply_voice_tags` 句首引导词→MiniMax 2.8 括号标记（(laughs)/(coughs)/(sighs) 等 19 个，官方文档核实**仅 speech-2.8-hd/2.8-turbo 支持**）——Hy-MT2 会把语气照词翻译（Hahaha/Coughs，念出来假人念稿），say 前确定性替换令合成层出真声；教 MT 加标记两条路都死（前缀位被无视/后缀位被当内容整句翻出来，:1236 直打实证），所以不走 prompt。标记进 say() 文本，字幕与译文行由前端 `stripVoiceTags`/`_strip_voice_tags` 剥掉，只活合成层。B 线默认 TTS 随之 2.6-turbo→**2.8-turbo**（标记需 2.8 系；代价 ~0.2s 感知 lag 2532→2721ms 三把实测，预算 3500 内；`MINIMAX_MODEL=speech-2.6-turbo` 回退档标记自动熄火、`BOK_INTERP_VOICE_TAGS=0` 只关标记——双门控 `_voice_tags_supported`+env，`_interp_env` 透传含 `MINIMAX_MODEL`）。一体台「听对方原声」做成开关（默认开=像直接通话；关=纯字幕同传，localStorage 持久化），字幕区默认切译员视角（只看「对方→我」流），新增大字幕浮窗（scope/三档字号/拖动/全屏/追帧 6 行）与逐句翻译延迟面板（原文行→译文字幕到达差+近 8 句均值；rev 纯字幕口径干净，fwd 被 sync_transcription 锚到播报不进均值）。⑫**子句级提交（2026-09-16，砍「说话时间+生成时间」的主刀）**——`QWEN3_ASR_CLAUSE_COMMIT`（`_interp_env` setdefault 1，仅 B 线；A 线 worker 不带此 env 零变化）：`_sentence_boundary` 次级标点（，、；,;）也作提交边界，门槛 `_clause_commit_min_chars`（默认 8>标点路径 6，防机关枪碎片），数字 run/稳定性/限速门全套照走，唔够格照例并入下一边界。实弹：30 字 3 子句长句 → 3 条原文+3 条译文（日志 `source=partial-punct` 逗号路径命中——说话中滑窗见逗号即提交，不用等停嘴），译文与说话流水线重叠，对方听到的译文落后约一个子句而非一整句；标准探针无回归（2478ms）。`QWEN3_ASR_CLAUSE_COMMIT=0` / `QWEN3_ASR_CLAUSE_COMMIT_MIN_CHARS` 均 `_interp_env` 透传逃生。⑬**全双工实测 + 方向级服务覆盖钩子（2026-09-16）**——`scripts/probe_interp_duplex.py` 两向**同时**说话（gather 重叠推流）：fwd 音频 lag [2568,2740]/[2627,2646] avg 2636-2654ms ≈ 单路基线 2581（争用增量 ~70ms，噪声级）；rev 原文行→译文行 created_at 差 avg 302-325ms（亚秒落字幕）；双向 orig/tran 全落零丢句，PASS×2——**单机 2 路全双工零争用劣化，GPU 排队是伪问题**（真要分 GPU：`QWEN3_ASR_BASE_URL_REV`/`MT_LLM_BASE_URL_REV` 等 `*_REV` env 经 `_apply_interp_direction_env` 把 rev 指第二套端点，fwd 恒默认；云端 ASR 适配器可插同钩子）。测量教训写进探针注释：rev 译文行在推流中途（子句提交）就落库，"推完"锚会产出假负差；轮询计数迟到一次性计数产出假 60s+——原文行→译文行 created_at 配对差才是纯生成耗时。全双工形态前提=虚拟声卡/耳机物理隔离 + 关自动半双工（声源仲裁已归入该开关，console 控制卡）。⑭**长度触发边说边译（2026-09-17，第三提交事件源）**——`QWEN3_ASR_CLAUSE_LEN_COMMIT`（`_interp_env` setdefault 1，仅 B 线；A 线 env 缺席零变化）：标点档靠逗号、停顿档靠 VAD 0.45s，连续语流（无标点无停顿，典型=英文/退化语音）两者都哑火等 EOS——`_sentence_boundary` 标点扫描落空后 `_length_commit_cut`（纯函数）按「未提交前缀攒够字数（CJK 主导 10 / 英·数字主导 2×=20，`QWEN3_ASR_CLAUSE_LEN_CHARS` 可调地板 8）且**跨窗稳定**（prev_full 同坐标逐字相同，SimulStreaming AlignAtt「稳定前缀才出」的工程化）」就地切句，切点不劈 ASCII 字母/数字 run（单号/英文词完整；整段含数字 run 唔再拦是 B 线唯一分歧——无 WhatsApp 捕获语义，号码子句照翻）；与标点档共用 1.5s 限速阀，标点边界够格时标点优先、长度档只补位（健康语音上惰性）。**二轮收紧（同日，治「每句话讲完先翻」真人体感）**：①门槛 12→10——真人语音子句普遍 5-12 字，12 字在停顿到来前常攒不够（长度档空转）；收紧后 zh 语料 10 字长度档抢在 ASR 自打逗号之前交稿（`partial-len chars=10 '我想请你们帮我查一下'`）。②`VAD_MIN_SILENCE_DURATION` B 线 setdefault 0.45→**0.35**——0.45 是 A 线校准（防碎片提交打断在途回复），B 线 manual 管线假切句零链路损伤，而真人子句换气普遍 0.3-0.45s，0.45 门槛下嗰啲微停顿完全不产生提交；透传含该 env。**体感物理下限**：译文首声最快也係「源子句结束（或 10 字稳定点）+VAD 确认 0.35s+部分解码 ~0.3s+MT ~0.3s+TTS 首包 ~0.3s ≈ 1.2-1.5s」——真人语音说话中提交几乎全要过稳定性门（滑窗改写中的字唔可以未定型就交），呢个係译员级延迟唔係「说完才翻」；A 线真实通话日志佐证真人语音提交源几乎全 vad-pause（TTS 合成音韵律才喂得饱标点档）。`_interp_env` 透传含两枚 env；提交日志 `source=partial-len` 可辨。实弹（`scripts/probe_interp_continuous.py`，butt-join 无停顿连续推流，主判据=译文首声早于源话音讲完+原文轮 ≥2）：**Qwen3-ASR 韵律打标点非常勤**（zh TTS 语料 10-11 字自打逗号，标点档已边说边译、长度档惰性零回归）；**en 语料现场触发** `partial-len chars=25 'I want to ask you to help'`（词边界切、说话中交稿）——长度档真实战场=英文标点稀疏+真实退化语音（快语速含混/电话编解码，ASR 不打标点时唯一事件源）；标准延迟探针 2503ms 持平、pytest 1162 全过。调研档案 `.superpowers/sdd/2026-09-17-interp-while-speaking/FINDINGS.md`（Hibiki fr→en only 否决/Soniox·Gladia 云端内联翻译记为 `*_REV` 钩子候选/SimulStreaming 思想采纳）。探针噪声档 `BOK_PROBE_NOISE_DB`（20dB 起，12dB 全链无输出）。⑮**会话级音色（2026-09-17，按语言选声入口）**——同传页建单我方/对方语言各选一把 MiniMax 音色（`minimaxVoiceOptionsFor` 按语言过滤，空=跟随设置）；`CreateCallRequest.voices_json`（512 字硬截）→ `call_sessions.voices_json`（deps 幂等补列 + `.p0_supabase_schema.sql` 重跑 + Supabase migration `b0_interp_voices`）→ fwd/rev dispatch metadata `"voices"` 同份透传 → `interpret._parse_session_voices` 归一（坏 JSON/空值/未知键丢弃不炸）→ `_build_tts_provider` 音色优先级=**会话级 > 设置三键 > 硬编码默认**，会话级误配本地 Qwen3 音色（预设 9 个/克隆前缀）同闸过滤回落（防 2054）；设置页 `speaker_zh/speaker_en` 同步 text→MiniMax 下拉（与粤语键同构，存量手填值 FieldInput 显示「自定义：」原样保存不丢）；`speaker` 单音色语义不变（B 线按设计不读——单音色跨语言串口音）。rev 默认纯字幕，我方音色仅 `BOK_INTERP_REV_AUDIO=1` 出声时听得见（同传页 hint 已注明）。测试 `tests/test_interpret_tts_provider.py`（解析容错/优先级/会话级过滤）+ `tests/test_token_dispatch.py`（落列/metadata 透传/512 截断）。
- **主管静默旁听 + 话术快照优先级（2026-09-14 路线 A）**：`POST /api/supervisor/{id}/listen` 签发 `purpose=listen` token——`can_publish/can_publish_data=False`、`can_subscribe=True`、**不挂 RoomConfiguration**（主管若先到不得替房间建房/派 agent）、**不翻通话状态**（听一通 paused 不得把它悄悄恢复 active）；被听方无提示（产品拍板），start/stop（含时长）双审计。web 端 `ListenPanel`（入口 `/supervisor?listen=<id>`）用官方 `LiveKitRoom`+`RoomAudioRenderer`+`useTranscriptions`，只订阅绝不发布麦克风。**话术来源优先级：`call_sessions.template_id`（建单快照，外呼战役/话务员自选话术）> `object_profiles.template_id`**——agent 装配读通话快照优先；`POST /api/calls` 收 `template_id` 显式指定；`campaigns.template_id` 由 `campaign._start_call` 写入通话快照（此前只存不读=外呼选的话术被静默忽略）；`DELETE /api/campaigns/{id}`：running 拒删（409），删除连名单项并审计 `campaign.delete`。
- **战役调度+工作台仪表盘（2026-09-17 campaign-scheduling-dashboard）**：外呼时段窗双层（全局 `settings.campaign.call_windows` × 任务 `call_windows`，各≤3 组取交集、本地时域；跨零点窗解析层不支持，`parse_call_windows` 非法项静默丢弃）；任务级 `max_concurrency`（0=不限，一轮至多补一通温和爬升）；未接通自动重拨（`redispatch`，attempts 收割回 pending 时 +1；回队判定单点 `redispatch_harvest_updates`——dial-result 直写与循环收割两路共用，dial-result 曾直写终态令重拨在生产主路静默 no-op（终审 C-1）；**PUT 三态：显式 `{}`=清除 / 缺键=保留旧值 / 合法策略=覆盖**——web 开关关闭必须传 `{}`，「关=不传键」曾致开关关不掉）；`PUT /api/campaigns/{id}` running 锁定（409）。工作台数据源 `/api/stats/dashboard`：answered 只认 `status==ENDED` 且 disposition 不在 {no_answer,rejected,failed}（reaper 振铃超时 FAILED+abandoned 不算接通、仍进 answer_rate 分母=ENDED+FAILED 全体）；today/answered_today=created_at ≥ 本地午夜对应 UTC 边界（`_local_midnight_utc_boundary` 单点公式，测试同源现算）；dial-result answered→ACTIVE 的 `started_at` coalesce（已有起点不重置，防重复上报推后 duration 起点）。
- **DB 迁移**在 `apps/control-plane/control_plane/deps.py` `build_engine()` 幂等段（补列 + 数据迁移），启动时自动跑；不要在别处手写迁移。
- **turns 分析账本生产者（P0 §6.1，2026-09-10 接线）**：agent 每轮上报 line/speaker/gen/template_step/started_ms/ended_ms/perceived_ms——A 线 `line=a`、user=customer、assistant=agent_ai；B 线 `line=b`、speaker=me/other（旧版缺省误标 line=a 已修）；`gen` 标生成源：llm/script（开场白/心跳/收线/WA 复述直念）/qa_fastpath（item_added 统一上报，旧 ④ 直报双写已收编）；`perceived_ms` 三段时序实测：tts_metrics 整段合成完才 emit、llm 流关闭又在 TTS 之后 → item_added 时 pending 已就位，启动即取勿等待；垫话 PR 曾把 `_RelaySynthesizeStream._metrics_monitor_task` pass 掉致 tts 段整体哑火（勿覆写）。上报任务走 `_spawn_report` 登记挂断 flush（裸 create_task 会被 job teardown 杀掉丢轮）。
- **分发型拓扑（P0 起）**：业务库必须保持 SQL 方言可移植（`tests/test_db_portability.py` 门禁，
  新列/新表禁用 sqlite 专有语法）；turns 新轮上报带分析账本字段（speaker/gen/template_step/
  perceived_ms，缺省值兜底旧调用）；节点身份=NodeStore 注册签发 node_token（只存 sha256），
  node-agent 心跳失联 ≥3 次置 REFUSE_JOBS；web CP 地址运行时注入（`window.__BOK_CONFIG__.cpUrl`
  优先于构建期 env，`apps/web/lib/api.ts` `apiBase()` 单点），禁止再把 CP 地址烤成构建期常量。
- **健康面三表单点 + 真探针（2026-09-17 体检收编）**：`CORE_PORTS`/`WORKER_PORTS`/`PROD_HTTP_CHECKS`
  （tools/bok.py 单点常量，status/doctor/prod status 三张表共用，加服务只改这里，`tests/test_health_surface.py`
  钉住）；worker 探针读 livekit-agents 内建 `GET :port/worker` 真端点（TCP UP 对「进程在、没 register/错码
  假活」不可见；1.8.0 payload 无 active_jobs 字段，不得打印）——prod status 对 interp-fwd/rev(8082/8083)
  失联报 DEGRADED（`_prod_units` 常驻单元，旧版只探 8081）；LLM 功能探针 `_probe_llm`（max_tokens=1
  prefill 测往返，预算 `BOK_DOCTOR_LLM_PROBE_TIMEOUT_S` 默认 10s，informational 不进打包门禁——空闲
  >2h 权重页入 ~40s 会一次假警，重跑区分）；b-line :8790 无明文 /health（WS worker 非 upgrade 恒 426=
  本体作答判活；旧版健康栈恒 `b-line DOWN → prod DEGRADED` 假警报）；模型在盘判定 `_model_present`
  双布局（app-data + mac lmstudio，与 cmd_download ensure 同款——9B settle lmstudio-only 曾恒报 MISSING）。
  prod 稳定性分工：launchd KeepAlive（`prod install`）管「死了拉起来」，doctor/健康面管「活着但残废」。**dev monitor 强杀误判（2026-09-17 call-54cab865 通话 63s 被杀实证）**：`cmd_monitor` 旧语义一轮 `healthy()`（1s TCP）全探测失败就 kill+respawn 全部 worker——GPU 满载整机卡顿一瞬即误判，在途通话 job 陪葬（「parent process shutdown」）；现改连续失败计数（per-worker streak，无通话 ≥2 轮/≥10s）+ `_cp_active_calls()` 在途通话闸（有 active 通话门槛抬到 ≥12 轮/≥60s，活 worker 卡顿攒不满、真死 worker 一分钟照补拉），日志带 `down xN (active_calls=M)`。
- **全量 debug 收编（2026-09-17 systematic-debugging 四阶段，`tests/test_debug_sweep.py` 12 条钉住）**：
  ①账号管理面（POST/GET/PATCH `/api/users`）在加固模式（`_hardened_auth()`=auth-on 或 BOK_CP_TOKEN 单设）下
  拒绝机器通道/裸 token 403——此前任意持 CP token 的 agent worker 进程可铸 root/重置 root 密码；真 auth-off
  （双关）零变化。②`/api/webhook/livekit` 补进 optional_bearer_auth 豁免表（与 identity_gate `_EXEMPT_PATHS`
  对齐——CP-token-only 模式 LiveKit 签名 webhook 曾被 401、agent 崩溃重派静默死）。③`scoped_account` 对空账号
  user fail-closed 403（仓库层把 "" 过滤值当 match-all 的通配符漂移，root 传空 account_id 建号才造得出）。④
  `supervisor_listen_stop` 补 `deny_cross_account`（同族端点唯一漏网，此前可注入他账号审计行）。⑤`tts/preview`
  补审计（真实烧云配额却无痕）。⑥classic MiniMax 流补 bidi 同款守卫：重连动作序列抽 `_minimax_classic_reconnect`
  （单测缝），重连失败 try/finally 清 `_reconnecting` 闸——此前闸悬挂=发送点 `await wait()` 永挂整轮静默；
  finally cancel `stall_task` + `closed_ws.set()`（此前 `closed_ws` 从未 set 是死代码、barge-in 提前结束流后
  看门狗僵尸重开 WS 重发文本白烧云配额）。⑦结算 task 入模块级 `_SETTLE_TASKS` 强引用池（事件循环弱引用+GC
  中途回收=post_session_report/settle 静默丢失，同 `_duration_fuse`/孤儿 invalidate 实证类）；WA 上报改
  `_spawn_report` 入池 + `except asyncio.CancelledError` 回滚 `_wa_reported` key（teardown 掐杀不走
  `except Exception` → 捕获永不补报）。⑧`ControlPlaneClient.report_dial_result` 重复定义删除（后者静默遮蔽
  前者，改一份即死代码）。⑨ASR sidecar `_sessions` 懒清扫（TTL 180s/总量 512，**插入后**清扫不变量）——
  client 取消/崩溃致 finish 永不到达时 PCM bytearray 跨通话驻留泄漏。**缓项（有档未修）**：14 处未持引用
  fire-and-forget task（A-F4 其余：context 更新/背景 judge/interpret 落库等，内部自捕异常、残余风险仅
  teardown GC）、`/api/token` 无记录房间可铸 publish token+拉 agent dispatch、diag 路由（asr/tts health、
  web_logs）无角色闸、webhook 无 secret 时 fail-open、B 线 interp 409 session-report 竞态（A 线先写则纪要丢）。

## Testing Guidelines

- Python: pytest `tests/test_*.py` (`test_*` functions); Node: `node --test` under `services/realtime-translation/test`.
- 改完 Python 跑 `python -m compileall -q apps packages services tools scripts`；web 改动跑 `cd apps/web && npx tsc --noEmit && npm run build`。
- **术语门禁**：`tests/test_cantonese_terminology.py` 是全仓测试的一部分（新增 `yue` 字面量即失败）。
- E2E needs the running stack and local models. **Never fake-green**: A-line E2E must use the real `/api/token` (`E2E_SELF_TOKEN=1` is debug-only).
- **并发/边界回归资产（2026-09-07 QA）**：`scripts/e2e_barge_in.py`（打断：AI 播报中插话→断言停声+恢复不哑火）、`scripts/e2e_edge_cases.py`（静音/超短音频不毒化链路）、`scripts/load_cp_concurrency.py`（CP 独立 :8001+临时 DB 并发压测，含 turns 并发 30/30 回归——turn_id 竞态丢数据防复发）`scripts/e2e_interpret.py`（B 线同传双向 E2E：fwd/rev 双向译文音轨 ASR 回读断言目标语 + turns 原文/译文分行落库 + 启停×3 + 长流，2026-09-08 首跑 5/5）、`scripts/load_audio_concurrency.py`（N 路真实通话并发，`LOAD_ROADS/TURNS` 可调；单机上限参照 prompt-cache 4-6 路、TTS sidecar 全局锁串行）。改动 turns/audit/并发相关代码后必跑前三个。垫话/体感改动跑 `scripts/probe_filler_timing.py`（主判据=首声 <2.5s 预算；filler_fired 默认仅信息位，`FILLER_REQUIRE=1` 才强断言）。延迟/竞争态（LLM 超时/拆句累积/打断风暴/打断账本）改动跑 `scripts/probe_latency_soak.py`（主判据=正常轮哑 ≥2 FAIL + 逐指标 p50/p95 与预算超标计数；对抗轮拆轮/风暴/兜底哨兵为信息位，报告判读）。**话术外问题集锦 `scripts/probe_offscript_soak.py`（2026-09-17）**：5 套主题（防诈质疑/转人工情绪/追问细节/推搪拖延/普通话混合）×10 轮真实语音、对话实录逐轮对照、哑轮/兜底/watchdog 哨兵+质量旗（空答/整句复读）+JSON 落盘 `reports/offscript-soak/`——改 prompt 规则/兜底闸/watchdog/异议处理相关代码后必跑（判读口径：轮 1 首声含开场白尾是测量伪值要剔除；粤 ASR 碎裂轮的实录比对表）。**热词 A/B `scripts/probe_hotword_ab.py`（2026-09-17）**：ASR sidecar 直打三档（none/current/extended）×10 碎裂句，量词表扩容收益与 prefill 成本——改 `_ASR_HOTWORDS` 后建议跑。
- **stack A/B 须防殭尸 worker（2026-09-09 实证）**：`bok.py down` 杀不掉失联的旧 agent worker（orphan sweep 只扫部分），殭尸持续跑**旧代码** serving 新请求——曾致基线/分支 A/B 结论完全互换的污染。A/B 前后 `ps aux | grep agent_runtime` 必须为 0 再 serve；`git stash pop` 会被运行中 web server 重新生成的 `apps/web/next-env.d.ts` 挡道，pop 前先 `git checkout --` 之。
- **E2E 测试句形铁律（2026-09-09）**：话音句**不带逗号/大换气**（渲染内停顿 >0.45s 被 vad-pause 劈轮，尾段前缀续句 interrupt 掉生成中回复 → `sentences=0 canceled=1` 零回复音频 → 断言窗跑满 90s 假故障）；**<10 字单口气句结构性免疫**（低于 `QWEN3_ASR_PAUSE_COMMIT_MIN_CHARS=10` 门槛只可能 EOS 单轮提交）——cantonese 腿与 barge-in SECOND_TEXT 用 9 字句；隔离测 max_gap<0.45s 不可靠（in-room 渲染方差），以 in-room 不劈轮为准。
- **auth-on 开发栈标准姿势（2026-09-15 定案）**：`BOK_AUTH_REQUIRED=1 BOK_JWT_SECRET="$(cat ~/.bok_dev_jwt_secret)" BOK_CP_TOKEN="$(cat ~/.bok_dev_cp_token)" BOK_ROOT_USERNAME=root BOK_ROOT_PASSWORD="Root-Bok2026!" python tools/bok.py serve`——**BOK_CP_TOKEN 与 BOK_AUTH_REQUIRED 同设**（agent worker 机器通道靠它过闸：缺了的话 auth-on 下 turns/QA/设置上报全 401、`doctor --packaged` 的 token 探针同源失败）；doctor 侧同 env 跑（token 探针带 Bearer）。两把密钥文件 ~/.bok_dev_jwt_secret、~/.bok_dev_cp_token 均 0600 本机生成勿提交。两把密钥必须异值（`jwt_secret()` 不再回落 CP token，同值/缺失/<32 字节 startup 拒启）。
- **CP 可选鉴权**：`BOK_CP_TOKEN` 设置后除 `/health` 外全部端点要求 `Authorization: Bearer <token>`（agent/web 需同步携带）；未设=全放行（本机单用户形态）。暴露到局域网/云前必须设置。
- **三层 RBAC（B1/B2，2026-09-15）**：`root / admin / user`（spec §7 三层修订）。`BOK_AUTH_REQUIRED=1` 开启用户 JWT 门禁（`control_plane/auth.py` identity_gate；豁免 /health、/api/auth/login、/api/nodes/heartbeat、/api/nodes/register、/api/webhook/livekit（/docs 已移出豁免、auth-on 关闭）；开认证必须配 `BOK_JWT_SECRET`，fail-closed）；root 种子走 `BOK_ROOT_USERNAME/PASSWORD`。**密钥分离铁律（2026-09-16 深测修复）**：`BOK_CP_TOKEN` 与 `BOK_JWT_SECRET` 必须异值、JWT secret ≥32 字节——机器通道凭据不得兼作签名密钥（CP startup fail-closed 校验；`jwt_secret()` 已删 CP token 回落）；agent worker 的 `ControlPlaneClient` 自动携带 `BOK_CP_TOKEN`（env 注入即可，无需手工拼头）。节点加固模式注册必须带指纹；心跳对已绑定指纹节点强制带指纹。收窄语义=**有身份就按身份收紧**：列表经 `scoped_account`（user/admin 强制本账号）、by-ID 经 `deny_cross_account`（越权 404 不泄露存在性）、管理面（settings/knowledge/personas/fillers/reports/audit/insights/supervisor 操作/nodes=root 专属）经 `require_role`（/api/insights 已收管理面——reports 页面键不再覆盖它，web 报表页对无管理面用户做了洞察卡降级）；**机器通道**（`BOK_CP_TOKEN`，`state.machine`）恒直通——agent worker 调 CP（turns/whatsapp/QA/垫话/设置）不受角色闸误杀，分布式部署时 agent env 必须带同值。角色规则：root 全域；admin 管理面+本账号；user=工作台/名册/对象只读/话术与 QA 可编辑/同传/外呼。`BOK_CP_TOKEN` 与 `BOK_AUTH_REQUIRED` 同设时旧中间件让位给 identity_gate。admin「整 org」视角待 org→account 映射落地（interim=本账号）。
- **话务员级资源（B3，2026-09-14）**：`conversation_templates`/`qa_entries` 带 `owner_user_id`（''=账号共享 / user_id=话务员个人，deps 幂等补列）；user 建的 CP 强制盖章本人（body 无效）、admin/root 建默认共享可显式指派。可见性：user=自己的+共享（`owner_scope_filter` 强制本人，仓库层 `owner_scope` 三态 None=不滤 / ''=仅共享 / uid=共享+本人）；编辑权：user 只改自己的，**共享条目改动=admin/root（403）**、别人的 404（`deny_foreign_owner`，与跨账号口径一致）；所有权转移（owner_user_id 字段）只归 admin/root。`call_sessions.created_by`（建单身份盖章；战役建单=''）供运行时收窄：agent 装配 `list_qa_entries(account_id=本通账号, owner_scope=created_by)`——**勿再硬编码 acc-001**（B3 已修的错库 bug），垫话罐头保持账号级无 owner。模板 PUT 的 `account_id` 对非 root 冻结（曾可经 body 改写归属账号，已堵）。auth-off/机器通道原样不过滤（agent 显式传 owner_scope）。
- **主管配置权限+web 登录分面（B4，2026-09-14）**：`users.permissions_json`（''=默认集 7 键 / 精确集合 / '[]'=全关）+ `permissions.py` 8 键目录（calls/roster/campaigns/objects/interpret/templates/qa 默认开，**reports 默认关可授予**；与 web `session-context.tsx` PAGE_KEYS 逐字对齐勿重排）；主管专属面（settings/knowledge/personas/audit/supervisor/users/nodes）**永不进目录**恒走 `require_role`。**JWT 只装身份、权限逐请求查库**（`_gate_page`：user 缺键 403，root/admin/机器通道/无身份直通）——主管改权限对在线 token 即时生效。user 管理端点收 `permissions` 字段（仅 role=user 目标+键 ⊆ grantable，违者 400）。web：`/login`（localStorage `bok_token`，api.ts `request()`/裸 fetch 自动附 Bearer、401→清 token 跳登录；**无 token=匿名本地模式全页面+acc-001，单机零变化**）；SessionProvider（`me()` 含 permissions）→导航过滤+`gateForPath` 路由守卫；`/users` 员工管理页（建号/启停/重置密码/按人勾权限）、`/qa` 快答库页（B3 owner 分档；2026-09-17 画布 Phase 1 加列表/画布双视图——`components/qa-canvas-view.tsx`+`lib/qa-canvas.ts`，拖线挂簇=PATCH `cluster_head_id`、挂步=PATCH `scope+step_index`，罐头状态面 `GET /api/qa/canned-status`+试听 `GET /api/qa/{id}/canned-audio`+补料 `POST /api/qa/pregen`）、话术页归属徽标。**`purpose=listen` 与 `role=supervisor` 的 token 同为 admin/root 专属**（B4 审计补的绕过洞：不带 role 字段自签 listen 曾可绕 B2 闸）。qa-entries `/hit` 端点不接权限闸（agent 机器通道）。**管理面下发制（2026-09-20，本句取代「永不进目录恒走 require_role」旧口径）**：`permissions.py` 新增管理键目录 6 键（settings/knowledge/personas/audit/users/supervisor；**nodes/licenses 仍 root 专属、不进任何目录**）；admin 行 `permissions_json` 语义=''/缺失=存量全量（升级零变化）/显式 JSON=root 裁定集（新建 admin 建号盖默认章=页面默认集+管理键全关）；43 处管理面 `require_role("admin","root")` 整批换装 `auto_gate_management`（路径前缀表→`_gate_management`：root/机器通道/auth-off 直通、admin 逐键查库 403 带键名、user 403；映射=TTS 声纹/预览+短信+SIP 站点+setup→settings、calls 删单→supervisor、qa/pregen+fillers→qa 键、objects 写门→objects 键、insights→reports 键）；**包含规则**=admin 授不出自己没有的页面键（`_ensure_grant_within_creator`，越界 400）；users 三缺陷收口（空账号 admin 列表 403 fail-closed、admin 视角列表恒滤 root 行、admin 行仅 root 可管理而自身 display_name 保留自助、改权限（含自提权）403）；`GET /api/settings?internal=1` 明文回源收 root+机器通道（admin 403、掩码面照常）——被盗 admin 凭据拉不走云凭据。web 同步：`session-context.tsx` MANAGEMENT_KEYS/`hasManagement`/`permLabel`、`hasPage` admin 改按有效集、导航三形态（root=纯平台面、admin=运营页按有效集+管理页按下发键、/nodes 仅 root）、users 页 root 下发面板（管理键勾选区+新建 admin 表单管理段）+「平台方·角色固有」徽标。钉：`tests/test_permissions.py` 下发制五测试组（存量全量回归/新建默认章/下发收回即时/users 键闸+包含规则/三缺陷/密钥面）。
- **CI/CD 三通道（P0，2026-09-15）**：①云镜像 `Dockerfile`（两段式：node 导出管理台静态站 → python:3.12-slim 装 CP 四包，**不含 apps/agent**；`VAULT_ROOT=/app/data/vault` 与审计 HOME 目录要在镜像里预建属主，否则 appuser 下 startup 即 PermissionError）+ `cloud-image.yml`（build→容器冒烟 /health+管理台→main/tag 推 GHCR）；②`postgres-smoke.yml` + `scripts/smoke_postgres.py`（真 PG：build_engine 幂等二跑+诱饵 schema 升级演练+10 表 CRUD 回程——静态 `test_db_portability` 只编译不连库，方言差异只有这里能逮）；③`gitleaks.yml`（公开仓全历史密钥门禁，官方镜像 docker run，`fetch-depth: 0`）。`scripts/dump_postgres_ddl.py` 从代码生成 Supabase 引导件 `scripts/.p0_supabase_schema.sql`（**改表后重跑再应用**，产物带回环校验：干净库应用+build_engine 双跑零变更；pg_dump ≥16.10 的 `\restrict` psql 元命令必须剥掉否则非 psql 客户端拒执行）。Supabase 真库已引导（22 表，MCP migration `p0_control_plane_schema`）；**Supabase 新表默认开 RLS**——CP 以 postgres 角色连接绕过，P4 多租户硬化正面处理。**CP→Supabase 必须走 Supavisor session pooler（IPv4 pooler 域名:5432）**：直连 db.*.supabase.com 是 IPv6-only，多数容器出网无 IPv6 直接连不上；transaction 模式(6543)与 SQLAlchemy 预编译语句有坑。`deps._ensure_column` 存在性探测已改 SQLAlchemy inspector（方言无关+schema 收窄——旧 information_schema 查询在共享库会被别 schema 同名表骗成「列已存在」静默丢列）。多实例冷启会撞幂等迁移 ALTER——P4 多实例前要「迁移与启动分离」。
- **P1 节点鉴权（license key+机器指纹，2026-09-15）**：加固模式（`BOK_AUTH_REQUIRED=1` 或 `BOK_CP_TOKEN` 任一，`node_license_required()` 单点判定）下 `POST /api/nodes/register` **必须携带有效 license_key**（root 经 `POST /api/nodes/licenses` 签发：明文 `bokn_*` 只在响应出现一次、库存 sha256；配额=未吊销节点数；吊销=名下节点 token 即刻失效）+ 机器指纹（node-agent `collect_fingerprint()`：macOS 序列号/Linux machine-id/Win MachineGuid 的 sha256）；本地双关全空=开放注册零变化。register 进中间件豁免表（**端点内自鉴权**：license key 本身是鉴权因子，与 heartbeat 的 node_token 同构——中间件预拦会把只有 license 的节点挡死）。同 (license,fingerprint) 重注册**幂等复用 node_id 换 token**（含克隆自动吊销后的**复活路径**——`find_node_by_fingerprint` 不排除 revoked 行，机器重装不累积死行）；心跳指纹不符=fingerprint_mismatch 自动吊销+审计 `node.denied.*`。node-agent license 流：`--license-key`+状态文件（`~/.bok/node-state.json`，0600）缓存 token、探测 401 才幂等重注册，`--node-token` 旧流程并存。**机器码鉴权=威慑不是 DRM**：指纹客户端自报可被抹，防线在服务端（签发/配额/吊销/克隆检出），二进制内零静态密钥。握手链路 smoke `scripts/node_handshake_smoke.py`（加固流 9 步含克隆/复活断言）+ CI `node-handshake.yml`。
- **静态站 GET 豁免（2026-09-15）**：两道门禁（identity_gate 与 BOK_CP_TOKEN 中间件）对**非 `/api` 的 GET/HEAD 一律放行**——管理台 SPA/登录页本身不得被 401（云端 auth-on 下曾整站拦死、B4 登录流程不可达，compose 排练实测）；特权数据全在 `/api/*` 后面，豁免不泄露数据。
- Merge gate: pytest、`services/realtime-translation` `npm test`、web `npx tsc --noEmit && npm run build`、CI `Launcher smoke` 全绿（`Desktop shell (Rust)` 必检上下文已随 Tauri 退役移除——**GitHub branch protection 记得同步删 required context，否则删 job 后 PR 永久阻塞**）。发版（tag）走 release.yml 的 node-pkg 冒烟（解包+布局+版本注入校验）；`doctor --packaged` 语义改为对节点安装树（`BOK_ROOT` 指安装根）。

## Commit & Pull Request Guidelines

- Conventional commits with a scope (`fix(desktop):`, `ci(release):`, …); body states root cause and verification evidence.
- One logical change per commit.
- Update `docs/RUNTIME_TOPOLOGY.md` / `docs/REPO_MAP.md` whenever ports, paths, or data flow change.
- **Do not tag or release until full local acceptance passes** (project policy; releases are CI-gated and user-verified).

## Multi-Session Git Discipline（多会话并行纪律）

本仓库为多 Agent 会话并行开发：多个会话共享同一 Git 存储。任何会话触碰仓库一律走本节隔离流程（2026-09-18 起强制；违反红线视为错误）。

**会话启动：**
1. 每次会话生成唯一 `SESSION_ID` 作为隔离标识（如 `$(date +%Y%m%d-%H%M%S)-$(openssl rand -hex 2)`）；绝不直接操作 main/master 主分支。
2. 基于 `origin/main` 最新创建专属临时分支与独立 worktree（先 `git fetch origin main`）：
   ```bash
   git fetch origin main
   git worktree add -b session-${SESSION_ID} ./work-session-${SESSION_ID} origin/main
   ```
   worktree/分支已存在则直接复用，不重复创建。所有代码修改、编辑、`git add` 全部在 worktree 目录内完成，禁止在仓库根目录直接修改文件。
3. **输出要求**：每次涉及 git 操作，输出前先声明三件事——①当前会话ID；②当前使用的 worktree 路径；③目标分支名称。所有 git 命令必须完整可直接运行（变量用 `${SESSION_ID}`）。

**提交幂等（防重复提交）：**
- commit message 必带标记 `[${SESSION_ID}]`；
- 提交前先 `git log --grep="${SESSION_ID}"`——已存在本会话 commit 则跳过本次提交，不重复提交；
- 提交前 `git diff --cached` 判断是否存在真实变更；没有变更严禁 `git commit`（禁空 commit、禁不带会话ID的匿名自动 commit）。

**推送：**
- 只 push 当前会话专属分支 `origin/session-${SESSION_ID}`；禁止直接 push main/master；
- 禁止对主分支使用 `git push --force`；如需强制仅允许 `--force-with-lease` 且只作用于自己的临时会话分支；不允许多会话并行抢写同一远端共享分支。

**任务收尾：**
- 输出 MR/PR 创建指令，提示合并到主分支；保留远端分支等待合并；
- 清理 worktree：`git worktree remove ./work-session-${SESSION_ID}`（仅构建产物拒删时，允许对自己 worktree 加 `--force`）。

**红线（违反即错误）：**
- 禁止多个会话共用同一个工作目录进行 `git add` / `git commit`；
- 禁止直接在 main/master 分支上修改提交；
- 禁止跳过幂等检查直接提交；
- 禁止删除、修改其他会话的 worktree 和分支；
- 禁止 `git reset --hard` / `git clean`——只允许操作本会话自己的 worktree。

不需要修改代码的任务，明确说明「无需执行 git 流程」。本流程不允许省略步骤，不允许「简化处理」「为了方便跳过校验」。

## Context 管理纪律（长会话，2026-09-21 起强制）

**触发阈值：会话上下文估算超过 512K tokens（或收到 harness 压缩提示）时，不得直接压缩——先落盘、再压缩。**

顺序（不可颠倒、不可简化）：

1. **整理已完成计划**：把本轮已完成的计划项（结论、证据、落盘产物路径、commit 短哈希、测试读数、被推翻的旧结论及改判理由）合并进对应计划档 `docs/superpowers/plans/*.md`。
2. **消灭双轨结论**：同一议题只准留一份现行结论；旧结论被数据推翻时**替换**而不是并列保留（保留改判记录本身，不保留双轨口径）。
3. **写清未完成项可续性**：把「下一批要做什么」写成计划档里的待办段（含文件路径、验收判据、已知坑），确保压缩后仅凭计划档即可接着干。
4. **落盘确认后再压缩**：计划档已写入（涉及代码则已提交）后，才执行 `/compact`。

**理由**：压缩是有损的——只存在于对话里的结论（探针读数、根因定位、改判理由、真栈证据）一旦被摘要吞掉就永久丢失，后续会话会拿旧结论重跑或按错前提实施。**计划档是唯一无损的跨压缩载体**，所以压缩前必须先把「已完成」沉淀进去。

**红线**：禁止未落盘就压缩；禁止把只存在于对话里的口径写进代码注释或 commit message 后自称「已落盘」（代码注释是给改代码的人看的，不是计划档）。

## Operational Constraints

- Never reintroduce Ollama, Docker, or CosyVoice runtime paths (removed by design).
- **Ubuntu 节点形态（2026-09-20 接线补齐）**：节点 = `node_agent`（systemd 常驻）→ `cmd_up` = 服务面 + **通话面**（`_start_call_plane` 单点：媒体 :7880 + worker :8081-8083 + monitor；旧版通话面只在 dev `serve` 内联 → 节点装完打不了电话）；`app_data_dir` Linux 走 `$XDG_DATA_HOME`（旧版非 nt 恒落 `~/Library/Application Support`）、`platform_key()` 仅 Darwin=mac（旧版 Linux 会下 mlx 模型）、`bundled_llama()` 认 Linux 档（`runtime/llama/llama-server`，`build_runtime.sh` 增 ubuntu-x64 打包档）；`_livekit_config_path()` 以 `BOK_LIVEKIT_BIND`/`BOK_LIVEKIT_WEBHOOK_URL`/`LIVEKIT_API_KEY+SECRET` 行级补丁生成 `app-data/run/livekit.yaml`（无 env 逐字节沿用仓内文件；节点形态回调必须指**云 CP**）；systemd 单元=`tools/systemd_units.py`（**纯文本模块零 subprocess**，装载归安装脚本/操作者；`Restart=on-failure` 复刻更新码 75 拉回/熔断码 0 保持停止，勿改 always）；模型档位=`BOK_LLM_TIER=4b` 取表内 `llm_4b`（**空=未配置→告警回退默认档**，运维填权重后生效；非 mac 表 mt/settle 同为可缺档位，缺=运行时回退主模型）+ `download --only` 子集下载；装机七步=`scripts/install-node.sh`（体检/venv+CUDA/**license 提前注册**/选型下载/常驻安装/拉起+**六点硬自检**/摘要——打印面中性词、不打印凭据）；`is_linux()` 判定勿写成 `not is_mac() and os.name!="nt"`（会吃 test_prod_windows 的 is_mac 打桩）。
- **agent worker 端口必须显式分**：A 线 main 与 B 线 interp（fwd/rev 两 worker）的 `WorkerOptions` 都要传 `port`（显式 8081 / 8082·8083 分配）——都不传时默认同绑 :8081，后绑者 `Errno 48` 即崩，web 侧表现为「Agent did not join」（2026-09-06 实证）。
- Bundled app resources are read-only: SQLite, vault, `tts-data`, logs, and metrics always live in app-data.
- **Tauri 桌面壳已退役（2026-09-17）**：分发=节点安装脚本（`install-node.ps1 -Fetch` / `bootstrap-node.sh`，从云 CP 鉴权下载，**客户链路零 GitHub**）+ schtasks/launchd 常驻 + 纯浏览器 UI（node_agent 内嵌 :3000 托管，`--ui-dir` 给定即自动起）。dev 栈 runtime/ 已迁到仓库根（原 `desktop/runtime`，`runtime_root()` 兼容祖先查找）。**交付链去 GitHub 化**：发版=`build_node_pkg.sh`+`build_runtime_pkg.sh` → GitHub Release（私有仓）→ `deploy/cloud/publish_node_pkg.sh` 推云 CP 工件卷 → 节点 `GET /api/nodes/downloads/{pkg|runtime|bootstrap}/…`（license/node_token 端点内自证）。**远程更新=commands 通道**：`POST /api/nodes/{id}/commands`（白名单 update/restart/shutdown，无在途通话才发、force 显式越过）→ 心跳响应 `commands` 领走 → update=CP 拉包+sha256 校验+覆盖+pip 重装+exit(75) 交守护拉起新版，完成判定=心跳 version 收敛自动关单；仓库私有化后**云主机必须 `docker login ghcr.io`**（镜像随仓转私有）。
- 本地 LLM 是 **mlx_lm server**（非 vLLM，Mac 上 vLLM 跑不了），prefill ~0.6k token/s 架构硬墙；runtime 的 mlx_lm 已打 **模板边界归一补丁**（`scripts/mlx_lm_template_leak_fix.py`，bok.py 起 server 前自动重打，BOK_TEMPLATE_LEAK_FIX=0 回退）——模板 endfor 后注释泄漏换行曾使跨轮缓存命中坍缩回 system 锚点，修复后每轮只 prefill「上轮回复+新问题」；A 线 :1235（`--prompt-cache-size 128 --prompt-cache-bytes 按内存分档(6/12GB，见下) --prefill-step-size 512`，2026-09-08 双轮反向 A/B 实证暖 TTFT -15%，dev `--log-level INFO` 看 Prompt processing progress），B 线翻译走 :1236 **Hy-MT2 专用 MT server**（`MODELS["mt"]`，缺模型自动跳过 B 线回退 :1235；`StatelessMTLLM` 官方模板逐句无状态）。**后台重活专线 :1237（2026-09-17，`MODELS["settle"]` huihui 9B 可选）**：settle 纪要/知识蒸馏（CP Summarizer 优先吃 `BOK_SETTLE_*` env）与 flow judge（agent worker 经 `_apply_judge_env` 注入 `FLOW_JUDGE_*`）都指向这颗 9B——延迟不敏感岗位吃大模型质量、与活通话的 :1235 分进程隔离；**注入前必查 `Path(_settle).exists`**（否则模型缺盘时 env 照发=打死端口，judge 全灭，test_settle_line.py 钉死），缺盘自动回退 :1235。争用实测（`/tmp/bench_settle_9b.py` 口径）：9B 出 512-token 纪要时 4B 暖轮 +60ms/冷轮 +360ms、单篇 4-9s——settle 挂断触发即 GPU 空档，直接吃无需空闲闸。**prompt cache 按内存分档**（`_default_prompt_cache_bytes`）：≥32GB 机型 12GB、16GB 维持 6GB，`BOK_LLM_PROMPT_CACHE_BYTES` 覆盖（HW_MEMSIZE 在 os.sysconf 不认，用 SC_PHYS_PAGES×PAGE_SIZE）；**ensure-models 认 lmstudio 布局**（与 `model_path`「哪边存在用哪边」同语义，否则 lmstudio 已有模型被 serve 静默重下 5.5GB）。**永不给请求加 seed/draft**（会关 continuous batching）。评估模型用 OpenAI 兼容端点，request model 必须填真实模型路径。
- **MiniMax TTS 运行规约**：**语速统一（2026-09-12 探针定案）**——`minimax_speed_for`（zh/cantonese 1.2、en 1.0）是唯一速度源，四层（bidi/classic 运行时 `_ws_voice_setting`、脚本直念 `_say_script`、pregen 物化、垫话资产 gen_filler_assets）全部同源解析，`tests/test_speed_unification.py` 钉死；bidi 的 task_start `voice_setting.speed` 服务端实证生效（`scripts/probe_minimax_speed_ab.py`：classic/bidi 1.0→1.2 加速比 1.33/1.16），听到的「回复慢半档」若再现先查缓存旧 1.0 条目/音色不同而非 speed 通道。凭据存设置 DB `tts.api_key`（勿提交仓库，`/tmp/mmkey` 是本地测试临时档）；**venv 无系统 CA——`SSL_CERT_FILE`（certifi）已由 bok.py 固化进全部 worker env**，手起进程要自带；**bidi 持久连接默认**（`t2a_v2_bidi`，2026-09-07 翻默认：服务端攒句防碎裂——classic 客户端按句+overlap 半句喂是「一句话连不起来、前后语气不一」根因，2026-09-06 用户听感实证+官方文档定位；`MINIMAX_WS_MODE=classic` 一键回退，classic 池/看门狗代码保留）；**emotion 默认不下发**（模型按文本自动匹配，官方文档建议+LiveKit 官方 minimax 插件 None 默认同款；旧 mood→emotion 映射是轮间语气跳变放大器，`MINIMAX_EMOTION=map` 回旧行为、枚举值直透）；`MINIMAX_CONTINUOUS_SOUND=1` 实验档（仅 2.8：模型侧不切分连续推理，韵律更自然 vs 延迟，默认关）；bidi 轮级打点 `MINIMAX_BIDI_PERF sentences=N canceled=0/1 first_audio_ms=Y`（sentences 应≈回复句数，远超=服务端按长度强切）。classic WS keep-warm 池（`MINIMAX_WS_POOL=1`，握手 216-648ms 全离关键路径）仅 classic 模式生效；A 线默认音色兜底 `Cantonese_crisp_news_anchor_vv2`（三语通用，空音色防 beep）；A 线 2.8-hd / B 线 turbo 按 `MINIMAX_MODEL` 分进程注入。链内 `TTS_FIRST_AUDIO_MS` 含「等 LLM 首句文本」时间，别当纯合成延迟读。首包前导静音自动修剪（≤200ms 上限防误剪气声起句，剪口 15ms fade-in 防咔哒，日志 `MINIMAX_TTS_LEAD_SILENCE_TRIM ms=N`；零静音零改动）——可闻出声更早。**卡死自愈看门狗**：首段文本发出后 `MINIMAX_FIRST_AUDIO_TIMEOUT_S`（默认 4s，旧 6/旧起点 task_started 均废——慢 LLM 轮会误触发+空重连）无首包 → 断开重连+重发已发文本（日志 `MINIMAX_TTS_STALL`）；重连窗口有发送闸（`_reconnecting` Event，发送点先 `is_set()` 再 `wait()`——Event.wait() 对未 set 事件挂起，不能无条件 await）——MiniMax 云端偶发 >8s 无首包是「每问无答」的现行真凶（2026-09-06 实证）。**bidi 三件自愈（2026-09-10）**：①**连接健康**——recv 死亡即后台重预热（`MINIMAX_BIDI_AUTO_REWARM` 默认 1 总闸，`invalidate(*, reprewarm=True)`；死亡连接不再等真实轮才发现，冷启动离群源根治）+ ping 连失 ≥`MINIMAX_BIDI_PING_MAX_MISS`（默认 2）打 `MINIMAX_TTS_BIDI_DEAD` 强断重预热（单次 miss 不断、pong 清零；强断**不可内联 await invalidate**——`_stop_ping()` 自 cancel 当前 ping task 会掀 CancelledError 吞掉 close 收尾，须甩独立 task）；孤儿 invalidate task 必须存引用+aclose 先 cancel（裸 create_task 事件循环只持弱引用可被 GC 中途回收，teardown 后才执行 reprewarm 会拉新连接+60s ping=关机泄漏，变异数测试实证）。②**首包看门狗**——首条 task_continue 起 `MINIMAX_BIDI_FIRST_AUDIO_TIMEOUT_S`（默认 6s 攒句放宽、0 关；classic 同款 4s 档不适用）无首包 → 弃连重连+已发文本**单条合并**重发（截 10k 官 2204 上限）+新纪元认领+计时复位，一流只自愈一次防重连风暴（重连窗口发送闸盖全部发送点；此前 bidi 只能干等 30s recv 超时=整轮静默）。③**预热韧性**——prewarm 失败 1s 后重试一次（`MINIMAX_BIDI_PREWARM_RETRY` 默认 1，`_connect_and_start` 成功尾清零）；可选合成级预热 `MINIMAX_BIDI_SYNTH_WARMUP`（默认 0——T1 探针 `scripts/probe_minimax_bidi_cold.py` 实证处女连接服务端预热仅 ~22ms 不值得默认开，2201 重建全程 ~0.8s 由①吸收；开启时连接建好后锁内一次真实合成焐热、音频全丢、自身失败不 invalidate）。音色有效清单以 `apps/web/lib/minimax-voices.ts` 为唯一数据源（新 id 须先 `/api/tts/preview` 验 200；EN 旧 `male/female_english_speaker` 已实测 2054 移除）。**拟声/emotion 探针（2026-09-16，`scripts/probe_minimax_emotion_tags.py` 实弹定案）**：①2.8-hd 拟声标记 `(breath)/(coughs)/(sighs)/(laughs)` 全部转声效不照念（ASR 回读逐字验证；sighs 渲染为可闻「哎」叹声；时长 +0.25~0.68s）——`agent.py` `_MINIMAX_VOCAL_TAGS` 白名单既有六词放行，say 步/脚本线 ref 文本可直接写（纪律：**首句前不放**——首声预算不吃 0.4-0.7s；改文本后重跑 `tts-pregen`，标记是 cache key 一部分）；`{happy}{/happy}` 内联伪语法证伪（照念「Happy」），LLM 输出永不带情绪标签。②emotion 是 voice_setting 请求级参数（9 枚举；音高中位 203→239/192Hz、sad 时长 +29% 实测生效）但 **bidi `task_continue` 中途带 voice_setting 换挡被服务端静默无视**（时长比 0.978/音高比 1.013）——换情绪必须重连 ~0.8s。③**架构决议=罐头带情绪、实时线不带**：say 步模板 `emotion` 字段（`FlowStep.emotion`，parse 归一小写）只由 pregen 物化时经 `MINIMAX_EMOTION` 烧进音频（批量线串行 env 注入无竞态），`tts_cache` key 折入 emotion 维度（sparse 非空才进 key，存量条目零失效，`test_key_emotion_dimension_sparse` 钉死）；实时生成线恒不下发 emotion（轮间语气稳定铁律，旧 mood 映射教训不重蹈）。运行时已接线（2026-09-16）：`_say_script(…, emotion=)` 只用 emotion 查缓存——say 步经 `flow_ctrl.pending_say_emotion()` 穿线（与 `pending_say_text` 同门槛），命中播情绪版条目、**miss 退化为无情绪合成**（运行时不做逐次切换）。模板已落地 v1：三语 step2（致歉通知）=sad、step4（赔偿问句）=calm、step6/7 留空自动匹配；`(breath)` 只进 zh/粤 step2 首句号后气口位（en 音色 breath 实测 +1.8s 太贵，en 只上情绪）；pregen 物化 pin=True（sad=3/calm=3）。**观察项**：say 行含 `(breath)` 会进字幕/对话史/回声锚（声效不被 ASR 吐词，误触发风险≈0），留意 4B 是否在自由回复模仿 `(breath)`——白名单放行下最坏是多发一次气声效非照念，出现即把 prompt 渲染与直念文本拆双版本。**云端声音克隆（路线 B，2026-09-18）**：CP `POST/GET/DELETE /api/tts/minimax-voices` 三端点——官方两步（`/v1/files/upload` purpose=voice_clone 拿 file_id → `/v1/voice_clone` 落 voice_id），voice_id 服务端生成 `bokclone<hex8>`（官方命名 [8,256] 字母开头；避开 `agent-`/`acceptance-` 前缀防 `_cloud_voice` 过滤闸误伤）；**拍板「先克隆不激活」**：克隆请求不带 text/model（不触发合成=0 费用），MiniMax 规则 7 天内未用于合成会删、首用合成收 ¥9.9/音色——试听（走 `/api/tts/preview`）或首次会话使用即激活；账号需实名认证（未认证 2038 → 403）。清单存 `tts.minimax_clones_json`（settings blob 免迁移；**键必须声明进 ProviderSettings**，否则 PUT model_dump 蒸发），域名照 preview 同款 `MINIMAX_REGION/MINIMAX_BASE_URL` 逻辑。克隆音色与系统音色同 `voice_setting.voice_id` 通道（t2a_v2/bidi 双支持）→ A/B 线运行时零改动；web 设置页 TTS 卡「克隆我的声音」面板 + 分语言三键与同传页会话级选择器合并显示（「克隆 · 标签」前缀，全语言槽可选——克隆音色无语言绑定，language_boost 按请求生效，普/粤/英跨语言质量待实弹试听）。测试 `tests/test_minimax_clone.py`（假 httpx：RBAC/缺 key/成功流/2038/base 覆盖/PUT roundtrip/删除）。
- 延迟调试读 agent.log 打点：`QWEN3_ASR_HINT`/`ASR_MS`/`LLM_TTFT_MS`（含 cached=N/M）/`LLM_FIRST_SENT_MS`/`TTS_FIRST_AUDIO_MS`/`PERCEIVED_MS`（北极星：用户讲完→AI出声=eou+llm ttft+tts ttfb，三段到齐即打，是上界口径——llm 段取抢跑请求 TTFT 含提交前提前量）/`LLM_REQ_MS`（LLM 请求级 header 时刻，配 llm.log 拆排队）；`scripts/measure_latency.py`/`scripts/measure_prompt.py` 可复测；历史预算表在 `.superpowers/sdd/2026-09-04-mt-minimax-latency/`（p4-p7 为 ≤1s 攻坚实测档案）；现行预算与超时政策表=**`docs/LATENCY_BUDGETS.md`（2026-09-17 定档单一事实源，任何默认值变更须同步该表）**。
- **延迟/超时治理与竞争态自愈四件（2026-09-17）**：①**LLM 首 token 截止+兜底直念+弃流重生+响应看门狗**——客服口径两轮拍板（「等 8s 再重试，这通电话已经废了」→「2.5 还是太松」）：框架恒传默认 conn_options（timeout=10s×retry=3=最坏 ~46s 静默），现 `MlxLlmLLM.chat` 对框架默认档覆写 conn_options（`LLM_REQUEST_TIMEOUT_S`=8s 传输层字节流后盾 ×`LLM_REQUEST_RETRIES`=**0 插件重试归零**），真闸在兜底壳 `_LlmFallbackStream`：首块 ChatChunk 计时 **`LLM_FIRST_TOKEN_TIMEOUT_S` 默认 2.0s**（0=关），超时→三语短应承直念→**原流后台续读（drain）晚到补真答案**（`LLM_LATE_ANSWER_DEADLINE_S` 默认 8s、0=kill-switch 回「立即 aclose+regen」旧行为；截止仍无产出/内芯异常才 aclose+factory 二发最后手段——弃流重生会令二发排在 mlx「无断连中止」僵尸解码后面拖高后续轮 TTFT，offscript 二轮实测 [watchdog] 后紧跟 TTFT 3872ms；部分已生成文本优先 salvage 交付）。**首 token 计时必须用 `asyncio.wait({task}, timeout)` 唔准 `wait_for(inner.__anext__())`——超时 cancel 会杀 livekit `tee_peer`，原流后续 chunk 结构性拿不到（`InvalidStateError` 实证）**（`BOK_LLM_REGEN` 默认开只管 factory 路，`LLM_LATE_ANSWER` 哨兵带 `source=drain|regen`，成功经 `_late_answer_say` 走 speech 队列 `provider=late-answer`，任务挂 plugin 长寿命强引用集防 GC；首字已出的流中失败唔重生防重念；`_llm_fallback_line` 装配经 `_wire_llm_fallback(_raw_llm, greet_lang)` 注入——**必须打 `_raw_llm`，打 ContextAwareLLM 包装层会 getattr 静默跳过令 2.0s 闸整场休眠**（offscript 首轮 50 轮 GATE 哨兵 0 次实证，装配线测试 `tests/test_llm_fallback_wiring.py` 钉死），`BOK_LLM_FALLBACK=0` 整闸关回旧行为；未注入的 B 线 worker 零跨线影响；自带 conn_options 的调用方如 prefix_prewarm 不受覆写）。TTFT 必须喺 ChatChunk 层计——SSE keepalive 会喂 httpx 字节令传输层 read 超时计唔准 TTFT。最坏可闻 ≈2.0s+TTS≈2.6s，0.5-2.3s 有垫话盖。兜底壳排空监视分支不双发 metrics。**响应看门狗**（`BOK_RESPONSE_WATCHDOG_S` 默认 4s，0=关）：`on_user_turn_completed` 钩子顶端武装，N 秒零 assistant 音频（生成从未启动/TTS 死火/钩子静默失败/垫话 6 次配额耗尽——run-5 轮9 33s 死寂实证）→ force-interrupt 清僵死队列+三语兜底直念+账本 `provider=watchdog-ack`；有意静默分支（回声/热词纯回声/暂停/风暴/双暂存）与 QA 快路显式拆弹，真回复首音频经 CachedTTS `_fire_first_audio` 监听器拆弹（**缓存命中 `_CachedChunkedStream` 2026-09-17 起推首帧同样 fire——此前 cache-hit 直念（watchdog ack 跨轮重复等）对 watchdog 隐身、照开火掐掉在途真回复：offscript 实测被掐轮账本只剩 watchdog-ack 行；唯一生产构造点=synthesize hit 分支，垫话/QA 快路/物化读取都唔经它**；非 CachedTTS 通路不武装）；**垫话开播顺延**（`BOK_RESPONSE_WATCHDOG_FILLER_EXT_S` 默认 2.0s、0=关）——垫话走 out-of-band 音轨不触发首声回调，播了垫话仍算「零音频」会「垫话+兜底+慢答」三连道歉，现每轮一次性顺延「剩余+2s」（`_watchdog_extend` 纯函数、`FillerDirector.set_on_fired` 接线）；`[watchdog]` 哨兵。②**数字句跨步累积**——WA 号码累积泛化到任意步（`BOK_DIGIT_ACCUMULATE=0` 回退）：非 WA 步单号/电话主导句（语境词 `_NUM_CONTEXT_RE` 或数字 ≥4 位，且残句 ≤6 字；金额句 3 位/疑问句 `_wa_questionish` 天然豁免）拆段暂存攒齐（`[digit-accum] stash`），flush ≥4 位落 call fact「单号 …」+直念确认、<4 位直念请重报——治「join-hold 只认数字尾/系词尾，半截单号句 ≥10 字照成轮 → AI 答半句、后半截再打断一次」。③**连环打断风暴退避**（`BOK_INTERRUPT_STORM_BACKOFF=0` 关；`BOK_INTERRUPT_STORM_WINDOW_S`=20/`_THRESHOLD`=3/`_QUIET_S`=8）——`speech_created` watcher 按 handle.interrupted 计数，滚动窗达阈值直念让路语一次 + `on_user_turn_completed` 静听（轮照落库 `provider=storm-listen`，starve 计数让位），客户停嘴安静 QUIET_S 恢复完整生成。④**打断轮落账**（`BOK_INTERRUPT_LEDGER=0` 关）——被打断的回复不出 conversation_item_added=账本零记录，现 ContextAwareLLM 出口 tee 部分文本（`set_partial_capture`→`_PartialCaptureStream`，正常走完自清/异常保留），watcher 补记 `gen=interrupted` 行；`_on_conversation_item` 与 watcher 双向清零防双记。配套超时补缺：real 档 dial 补 `BOK_MAX_CALL_DURATION_S`（默认 900s，0=关）本地保险丝（dial 块显式键优先）；`PERCEIVED_BUDGET_EXCEEDED` 超预算哨兵（`BOK_PERCEIVED_BUDGET_MS` 默认 3000，0=关）；bidi 首包看门狗一流自愈 1→`MINIMAX_BIDI_STALL_MAX_HEALS`（默认 2，超限落回 recv fail-fast 防重连风暴）。**多轮多样话术延迟测试台 `scripts/probe_latency_soak.py`**（复用 e2e_real_customer 骨架：逐轮墙钟首声+PERCEIVED_MS 三段+拆轮/风暴/兜底/哑轮哨兵+p50/p95+预算超标计数+JSON 落盘 `reports/latency-soak/`；每场景正常轮 7×三语同图换措辞 + 对抗轮 4：拆句劈半推流/1.4× 快语速/连环打断 3 连打/报号拆两截；`--no-adversarial` 基线档）——改延迟/竞争态相关代码后必跑。
- **感知延迟结构（2026-09-09 S5 实测修订）**：暖轮 TTFT 主导=未缓存后缀（新 user 话+新尾部+上轮回复 ≈240 tok）的 prefill，实测 **0.4-0.6k tok/s**（0.6k 墙只对大跳变轮成立，勿再引「暖轮 5-6k」误读——mlx_lm streaming 的 prefill 懒执行发生在 headers 之后，SSE keepalive 行可精读进度，`scripts/mlx_lm_template_leak_fix.py` 的 `[bok-timing]` 计时补丁即为此设，`BOK_TIMING_PATCH=0` 关）；mlx_lm 0.31.3 已最新、server 无断连中止（被作废抢跑请求解码到完才放锁=排队尾巴）；空闲 ~2h 后首请求付权重页面换入 ~40s（会话 prewarm 已吸收）；探针对 :1235 发请求 model 字段必须用本地路径（repo id 触发 HF hub 解析）。
- **EOS 停嘴时钟锚点铁律（2026-09-09）**：STT 发 END_OF_SPEECH 必须带 `speech_end_time`（停嘴时刻）——框架 min_delay 锚定该字段（audio_recognition.py:1332/1679），锚缺失坍缩为 now → min_delay 全额叠在解码延迟后（白付 ~0.25s/句）。vad-pause 路径锚 VAD 静音起点、partial-punct 锚 chunk POST 发出时刻、join-flush 锚=flush 时刻-hold 窗。新加提交路径照此办。
- **尾部瘦身（BOK_TAIL_SLIM=1 默认，0 回退）**：revision 与上一条已冻结尾部相同（流程/事实/WhatsApp 无实质变化）时尾部只发「【第N/M步·继续】+WhatsApp+你上一句引文」紧凑标签——全量指引在上轮冻结尾部原样可见，逐字重发是暖轮 TTFT 大头；【你上一句】固定指令文本已上移稳定前缀【重复控制】（前缀缓存命中零成本，尾部只留引文）；add_call_fact 必须 +revision（新事实漏出全量尾=事实失明）。未缓存后缀从 249-275 字降到 72-211 字。
- **REPEAT verdict（2026-09-09）**：「听唔清/再说一次/你说什么」等短句（≤12 字，简繁混拼全收）判 repeat → 指引照讲复述（数字/单号逐位放慢），不算复读；不推进/不走背景 judge/QA 快路白名单自动排除。复读类提示措辞必须留复述门（尾部账本冻结重放整通，「严禁复读」一刀切会 fight 客户要求的重复）。
- **E2E 测试话音已真化（2026-09-09）**：三套 E2E（trilingual/barge-in/edge_cases）全弃 fixtures 改 TTS sidecar 现场合成（`tts_pcm`，语言参数 zh/cantonese/en），句句不同真人客户口吻——fixtures（135-138s 长音频）与 E2E 解耦，勿再让测试整条推长音频（0.55× pacing 一条 137s，曾致 E4 假 FAIL）；品牌词/碎片化探针停顿须 ≥0.5s 跨 VAD min_silence 门，0.3s 测的是接缝解码不是拆轮。
- **E2E/压测脚本 auth-on 适配（2026-09-17）**：`e2e_interpret`/`e2e_barge_in`/`e2e_edge_cases` 全部 CP 调用带 `_CP_HEADERS`（Bearer `BOK_CP_TOKEN`，未设 env 零变化）——auth-on 标准姿势下老脚本建单/取 token 曾 401；`load_cp_concurrency` 自建 :8001 CP **必须裸跑**（勿传 auth env——自建 CP 会继承并把自己 401，scenario C KeyError 'id' 实证）。
- **B3 风暴静听 × E4 回归（2026-09-17 实弹定位，同日修复）**：`BOK_INTERRUPT_STORM_BACKOFF` 旧语义把 e2e_edge_cases E3 的设计化 8 句连发判成风暴后**无限续期**（每个用户轮 +QUIET_S、starve 清零=合法连讲整段独白零反馈零恢复），E4 正主回复被 starve-ack 压死（两连挂）。**重排（`_storm_on_turn` 纯函数，tests/test_interrupt_storm.py）**：①静听轮数封顶 `BOK_INTERRUPT_STORM_MAX_ROUNDS`（默认 5，0=回退旧无限续期语义）到顶强制 resume 恢复完整生成；②第 3 轮起奇数轮给 starve-ack 短承接（A3 交接，独白不哑，provider=starve-ack）；③过期/cap resume 时清 ts+starve——re-engage 需新一轮 3 次打断（防旧计数复燃振荡），恢复首轮唔会被 starve 吃掉。验收：单测 12 过 + **e2e_edge_cases 8/8 全绿（默认档开）**。注意 agent worker env 在 serve 时定死，storm 相关 A/B 必须重启 serve。
- **KV-cache/缓存诊断（2026-09-06 起）**：`BOK_LLM_MSG_DEBUG=1` 打逐请求逐消息 sha1 指纹（定位前缀分叉消息）；`scripts/llm_cache_report.py <worker.log>` 出 cached 分布汇总；档案在 `.superpowers/sdd/2026-09-05-llm-cache/FINDINGS.md`（三根因+锚点封顶开放项=mlx_lm 上游 LRU 段键可查性异常，影响每轮历史增量 0.1-0.5s）。
- **回声/AEC**：浏览器采集默认已开 `echoCancellation/noiseSuppression/autoGainControl`（livekit-client 默认预设，勿关）；扬声器外放+失效输出设备会造成 AI 自闻自打断（sink 失败已自动清档回默认输出，日志 `Failed to set sink id` 出现即说明存档设备已失效）。实测/演示**建议戴耳机**；agent 侧 ai_coustics 降噪为可选后手（需授权），未接。
