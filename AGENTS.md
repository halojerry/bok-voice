# Repository Guidelines

Contributor guide for **Bok Voice**, a local-first voice customer-service assistant (A-line) and real-time interpretation workbench (B-line). Read [RUNTIME_TOPOLOGY.md](docs/RUNTIME_TOPOLOGY.md) before changing runtime behavior, and [AGENT.md](AGENT.md) for LiveKit decisions. S2S（端到端语音模型）评估结论与试点路线见 [docs/S2S_ROADMAP.md](docs/S2S_ROADMAP.md)——粤语输出当前无本地 S2S 可用，勿轻言换架构。

## Project Structure & Module Organization

- `apps/agent` — LiveKit agent runtime (VAD → ASR → LLM → TTS orchestration and providers).
- `apps/control-plane` — FastAPI API (:8000): objects, personas, knowledge, templates, calls, audit, tokens.
- `apps/web` — Next.js static export (Tauri-hosted UI).
- `packages/` — shared Python: core models, SQLite repository, knowledge, observability.
- `services/` — local sidecars: Qwen3-ASR (:8787), Qwen3-TTS (:8788), realtime-translation worker (:8790), LiveKit server config.
- `desktop/` — Tauri shell and bundled runtime assembly.
- `tools/bok.py` — single orchestrator: `serve`, `status`, `down`, `doctor`, `download`.
- `tests/`, `services/realtime-translation/test`, `scripts/` — Python/Node suites and CI helpers.

## Build, Test, and Development Commands

```bash
./scripts/bootstrap.sh                  # create .venv312 + install Python deps
./scripts/test.sh                       # full pytest suite
cd services/realtime-translation && npm ci && npm test   # B-line Node tests
cd desktop/src-tauri && cargo test      # Rust shell tests
python tools/bok.py serve               # start the full local stack
python tools/bok.py status | down | doctor --packaged
python tools/bok.py prod install        # generate launchd plist units (KeepAlive auto-restart)
python tools/bok.py prod status         # official health surface (server GET / + worker :8081/worker)
E2E_ONLY=cantonese .venv312/bin/python scripts/e2e_trilingual_livekit.py  # A-line E2E
cd apps/web && npm run build            # static web export
cd desktop && npx tauri build --bundles app   # macOS bundle
```

## Coding Style & Naming Conventions

- Python: PEP 8, 4-space indent, `from __future__ import annotations`, type hints on signatures; run `python -m compileall -q` after edits.
- TypeScript/React: match surrounding components; no formatter is enforced in CI.
- Naming: `snake_case` Python modules/functions, kebab-case services; model repo dirs use `owner--name` as declared in `tools/bok.py` `MODELS`.
- Bash scripts: `set -euo pipefail`.

## Language / Terminology Rules（跨层约束）

- **粤语规范值 = 小写 `cantonese`，全时空唯一拼写**，语言三态只有 `zh / cantonese / en`。全栈（DB、web、TS 类型、prompt 分支、`LanguageState`、voice-map 键、`speaker_cantonese`）一律 `cantonese`；旧拼写已存量清零（CP 启动迁移 `deps.py build_engine()` 是唯一兼容点），运行时代码**不留任何别名分支**。
- **防复发门禁**：`tests/test_cantonese_terminology.py` 扫描全部跟踪源文件，旧拼写只允许出现在白名单单点，新增即测试失败——字段单轨化，杜绝双轨技术债。
- **外部系统真字面量不改（门禁白名单内）**：Wikipedia 域名 `zh-yue.wikipedia.org`、Volcano dialect 枚举、MiniMax 音色 ID `Cantonese_*`。音色 id 值是不透明标识符，不属语言字段。
- **每通对话语言固定（A 线，取代逐轮语言跟随）**：粤语通话全程粤语、中文全程中文、英文全程英文，中途不切换。会话装配时一次钉死三方：ASR hint 恒钉通话语言（`_call_language` 人设→对象→zh；`PinnedLanguageState`+`pin_language=True` 三语全钉，zh 也下发 `Chinese`；设置 `asr.language_mode=fixed`+显式 `language` 仍优先，**mode=auto 在 A 线=钉到本通语言，不是滞回跟随**——LanguageState 滞回/sticky 机制已在 A 线退役，B 线不变）、LLM【用户语言】规则装配时 `set_user_language` 一次字节静态整通（逐轮钩子不碰语言）、TTS 音色+MiniMax `language_boost`（zh→Chinese、cantonese→Chinese,Yue、en→English，env 按通话注入）按通话语言固定。sidecar 传 `language=cantonese` 等规范名（mlx 大小写不敏感回填模型 config）消除 auto 误判（啱唔啱→难唔难）。**Prompt 语言纯度**：进 prompt 的共享指引文本（【应答准则】/当前步守卫/【新一步】/后备块/收尾/尾部节头）一律**标准书面中文**——这些无条件进每通通话，写成粤语书面语会把 4B 模型的普通话回复带偏成夹粤语（2026-09-06 实证后整批改写，`test_zh_prompt_purity_no_cantonese_marks` 钉死）；方言风格只准出现在按通话语言条件渲染的规则里（`_cantonese_rule`/港式词表，仅 cantonese 通话渲染）；`_zh_rule` 明确禁方言夹杂。

## Architecture Boundaries & Runtime Rules

- **官方组件优先（契约+组件，2026-09-09 拍板）**：除知识库/对象/话术业务域 + 本地模型插件 + B 线双栏字幕 + Tauri 设备层外，全部对齐 LiveKit 官方栈（详见 `docs/DEV_TOOLS.md` + `AGENT.md` 决策记录）。查官方用 docs MCP `https://docs.livekit.io/mcp`（免费无 key）。**别重造官方轮子**（LLM 客户端/转写落库/崩溃补位等先查官方姿势）。**官方组件优先三档判据**：①新功能先查官方组件/官方模式（@agents-ui 走 shadcn registry 可增量拉；lk docs/Docs MCP 查事实）；②自定义 STT/LLM/TTS 插件、业务工作台 UI、bok.py 编排属官方授权自定义层非手搓；③实证分叉（尾部冻结重放/FlowController 规则先行/回声守卫/Tauri 设备层/B线 whoIs 双栏）动前必读本轮调研档案 .superpowers/sdd/2026-09-09-official-first/FINDINGS.md（本机档案，结论已浓缩于本条）。**web CallStudio connect() 陷阱**：勿在未连接的房间上 `await setMicrophoneEnabled(preConnectBuffer)`——发布等连接、连接等该行返回，死等到 livekit 内部 ~15s 超时（「首次接通 15.3s」根因，2026-09-06 浏览器探针实测）；麦克风采集走 `session.start({ tracks: { microphone: { publishOptions: { preConnectBuffer: true } } } })` 与 token/连房并行。
- **话术分步推进**：`apps/agent/agent_runtime/flow.py` 的 `FlowController` 是唯一推进引擎（`detect_whatsapp_signal`/`should_auto_advance`/`decide_advance`/judge）。agent 每轮 `on_user_turn_completed`：WhatsApp detect → rule_verdict+auto_advance → 模糊轮背景 LLM judge（fire-and-forget，`_judge_inflight` 防叠）；语言已整通固定，钩子不做任何语言处理。WhatsApp detect 带 `already_captured`：本通已 captured 过号码后，纯短应承/叫加不再判 offered（否则确认轮锁死、4B 原句复读，2026-09-06 call-e6e5f18e 实证）；新号码照捕。**话术 WhatsApp 步方向铁律=向客户索取客户自己的 WhatsApp/微信号码**——模板第 3 步 ref 必须与 detect 的 captured 逻辑（等客户报数字串）同向；写成「发我哋微信号俾客户加」客户就永远唔报号码、步锁死（2026-09-06 三语模板数据修复实证）。**联系渠道係对象变量 `{聯絡方式}/{联系方式}/{contact}`**（`object_vars` 解析：对象 `contact_channel` 显式指定优先，缺省按对象语言 zh→微信 / cantonese|en→WhatsApp；三语模板统一 5 步已用占位符，换渠道唔使改模板）。改动推进逻辑先读 `tests/test_flow_controller.py`。
- **LLM system 顺序（KV-cache 关键，勿打乱；2026-09-05 二次实证修订）**：请求组装=`render_instruction_prefix()`（整场静态：语言规则/节奏/准则/话术总览/**对象档案**）+ 人设 base 作 system；**易变尾部（当前步/记忆/CONTEXT_RAG 时的知识联网）采用「跨轮纯追加」：每条 user 消息首次出现时拼上当时的尾部并由 `ContextState._applied_tails` 账本冻结，此后每轮原样重放（ContextAwareLLM）**。铁律=上一轮请求必须是下一轮的**严格前缀**（mlx_lm LRUPromptCache 只复用严格前缀；旧「尾部只拼最后一条 user、下轮即剥」会在中途分叉→cached 恒=system 锚点、历史每轮全量重 prefill——2026-09-05 INFO+指纹实证后改为账本冻结重放）。抢跑重建轮（n_new==0）若尾部在末条 user 拼上后已实质变化（ContextState `revision` 计数：set_flow_current/set_whatsapp_note 变更才 +1），只把**末条 user** 尾部重渲染成当前版并同步账本（否则推进后回复照旧步讲，2026-09-06 实证）；转写被修正（提交文本≠账本 orig）→ 末条 user 按**当前文本+当前尾部重锚定**（跳过会令该轮连尾部都丢、其后每轮 replay 全跳过）。**框架步骤标记（`[流程状态]` system 消息）只准留在框架侧做抢跑失效触发，`ContextAwareLLM.chat` 出口统一剥离**——它本轮在、下轮无，进请求流会令下一轮喺同一位分叉 → cached 钉死 system 锚点、每轮全量重 prefill、TTFT 随轮次 1.2s→8.5s（2026-09-06 指纹实证 call-b882cd69，探针 `scripts/probe_llm_cache.py` + `BOK_LLM_MSG_DEBUG=1` 复现与验收：修复后 cached 逐轮增长 966→1490→2067、TTFT 压平 ~1.5s）。开场白/心跳/收线一律 `session.say()` 脚本直念——`generate_reply(instructions)` 的临时 system 消息下轮即剥=前缀断裂，官方 filler 姿势用 say。**开场白=话术第 1 步 ref 首行直念**（`FlowController.opening_text()`：变量已替换、「\n如果客户…」分支指引不念出声；模板语言≠通话语言或变量缺失→退通用语；念完置 `opening_played` 并重渲染 `set_flow_current` → turn-1 尾部带【开场已念】提示防重复开场；三语模板各自第 1 步即各语言开场，EN 占位 {name}/{courier}/{tracking_tail} 已入 object_vars 别名）。**预热与开场白 TTS 并行起跑**（greeting_text 直传预热任务、不再等 playout；`session.say()` 返回=整段念完而非出声，打点 `greeting_playout_done`）。首轮预热用「真实开场白文本作 assistant 轮」的 turn-1 同构形状（`_build_prefix_prewarm_messages`），system 串联必须复刻 to_provider_format 的 `\n` join。历史截断是摊销式（超 2×max_turns 才截回 max_turns，`LLM_HISTORY_TURNS=8`）。会话首轮冷 prefill 由 `LLM_PREFIX_PREWARM=1` 真 system 预热吸收。`LLM_TTFT_MS` 日志带 `cached=N/M` 命中读数。
- **知识=沉淀非逐轮注入**：提示词只含对象信息+话术+session 记忆。知识/联网检索**默认全关**（`_context_rag_enabled()` 无参默认 False，`CONTEXT_RAG=1` 才开且只渲染进尾部 `rag_enabled` 段；`WEB_SEARCH` 默认 0）。沉淀知识经 `ContextState.set_object_brief()`（≤2 行×150 字，显式换行分界、多句整行截断不丢字段）在装配时渲染为静态【对象档案】入前缀。会中事实（平台/号码）每轮经 `flow.extract_call_facts` → `add_call_fact`（去重有界 ≤4）沉淀进尾部【通话中客户已讲】，配 `set_last_reply` 的【你上一句】重复锚——治「忘记早轮信息」与「原句复述」（2026-09-06 行为取证）。
- **VAD/endpointing 基线（2026-09-05 句号级提交）**：`min_silence=0.45` / endpointing `min_delay=0.25`/`max_delay=0.6` / `min_speech=0.15`；A 线 `turn_detection=stt`（STT 句末 `END_OF_SPEECH` 提交，说话中按句成轮，LLM/TTS 与说话重叠——≤1s 通路）。当年压端点致哑火=轮次在离线 ASR final 前提交；现提交结构性等待 STT FINAL 且句级路径 FINAL 即句文，三语 E2E 实证 0 丢转写。**回退须成对**：`TURN_DETECTION=` 置空回 EOT 档 + `QWEN3_ASR_SENTENCE_COMMIT=0`（B 线 interp env 已强制 0，否则句级 FINAL 叠进停嘴 FINAL 重复转写）；**kill-switch 档 endpointing min_delay 自动回 ≥0.35（`_endpointing_delays_from_env` 强制，无需手动）**——0.25 只对 stt 句级提交校准过，未校准 EOT 配 0.25 早提交截断粤语（p6 实证）。句级保护：≥6 字/无 ≥2 连续字母数字串/双窗稳定/1.5s 限流/小数点含窗口末尾不劈句；真实音频滑窗句间只出逗号 → VAD 停嘴（≥0.45s 微停顿）也是句边界源（`QWEN3_ASR_SENTENCE_PAUSE_TRIGGER` 默认 1）；**vad-pause 提交字数下限 10（`QWEN3_ASR_PAUSE_COMMIT_MIN_CHARS`，标点路径仍 6）**——微停顿只证明喘气，6-9 字碎片当整轮提交会被下一碎片掐掉在途回复（2026-09-07 碎片提交门，扣住并入下边界/停嘴兜底）。改这三处默认值要同步 agent 环境默认 + `repository.default_settings` + web `EMPTY_FORM`。
- **打断 = 官方误打断自愈组合**：`interruption.min_duration=0.6` + `resume_false_interruption=True` + `false_interruption_timeout=1.0`（真插话 0.6s 让位；1s 内无转写=噪声误打断，AI 自动从暂停处续讲）。旧 1.2s 高门槛已废（压住真插话）。`INTERRUPT_MIN_DURATION`/`RESUME_FALSE_INTERRUPTION`/`FALSE_INTERRUPTION_TIMEOUT` env 可回退。回声自听守卫：AI speaking 中收到与上一句（ContextState.last_reply）归一化相似度 ≥0.9（两边 ≥6 字）的用户轮 → 判 AEC 失效自听，日志 `QWEN3_ECHO_SELF_HEARD_DROP` + `StopResponse` 丢弃整轮（`QWEN3_ECHO_GUARD=0` 关）——治外放+输出设备失效时 AI 自闻自答；AI 讲完后的复述/引用不拦（条件钉死 speaking 中）。**livekit-agents 已升 1.8.0**（锁 <1.9）：1.7.1 的 playout pause 泄漏实测一次打断后本通所有回复哑火只剩心跳（call-e49bd34d，LLM 照常出文本、TTS 流开了不推音频、agent 卡 listening），官方 1.8.0 #7083/#7087/#6865 恰为此修——当年锁 <1.8 的 pre-release flake 理由已失效（GA 版 311 测试全过）。**心跳钩子（`_arm/_disarm/_fire/_on_agent_state/_on_user_state` 与 `session.on` 注册）必须先于 `session.start`/开场白**——开场白播完的 listening 转换发生在注册前会令首段沉默 arm 失效（2026-09-06 审查 P1）；开火护栏抽成纯函数 `_nudge_should_fire`（答案在途窗口 ≤2×delay 唔开火，test_heartbeat.py）。心跳/收线文本=脚本直念 `_nudge_line`（三骨架轮换）/_farewell_line，零 TTFT 即时补位、零前缀断裂（旧 generate_reply 版 4B 照抄例句两连发一字不差，2026-09-06 实证）。**测试对象心跳豁免（2026-09-09 #45）**：对象 `display_name` 命中 clean-testdata 同款前缀族（`^(E2E-|soak\d*-?|并发|LOAD-|边角-|多轮-|probe)`）→ 心跳整条关（`_effective_nudge_max`=0：arm/farewell/12s 自动收线全不挂），`BOK_E2E_NUDGE_IMMUNE=0` 逃生——E2E 的 greeting→用户语音间隙必跨 8s 心跳线，nudge 7-9s 音频会越过脚本 `agent_audio.clear()` 污染断言窗（call-6ba7c47c 实证）；豁免后 barge-in resume 断言首次测到**真实** reply2（5.3-6.1s；旧基线的 16.07s「恢复」实为 nudge 凑数假绿）。
- **VAD 微停顿短尾不补发**（打断自噬，2026-09-06）：pause-commit 已发句级 FINAL 后，<6 字短尾（「係。」）不再补发第二条 FINAL——否则新用户轮会 interrupt 掉生成中未出声的回复（「每问无答」根因之一）；≥6 字照发。**重解修正也唔补发**（2026-09-06 call-58601bba）：vad-pause 提交的半截转写（「这是我的牌」）与 /api/finish 整句修正（「这是我的快递」）唔係前缀关系时，`_uncommitted` 按归一化相似度 ≥0.55 判定为同一句话的更好重解 → 丢弃（QWEN3_ASR_REDECODE_DROP 日志）——补发迟到 FINAL 会在框架 `on_final_transcript` → `_interrupt_by_audio_activity()` 掐死生成中的回复（gen 完成但零音频零转写，只剩心跳）；归一化前缀对齐的真续句照发。
- **拆句组装 = 跨段 join-hold + WA 号码累积 + WA 步假确认护栏**（2026-09-06 拆句调查，call-c4f6e4f1/839ec9db 实证）：报号句中微停顿会把「我的WhatsApp係…六四三二…」切成两个 FINAL/两个用户轮——语境与数字分居两轮，捕获结构性失败、AI 对每段插话复读/捏造拼接（"03201"）。三层修复：①STT 层**跨段拼接 hold**（livekit_plugins `_join_worthy`/`_hold_flush`）：续接可能句（归一 ≥2 位数字/系词係|系|是|is 收尾/**热词词表某词的严格前缀**）喺 END_OF_SPEECH 唔发 EOS、唔 finish——sidecar session 续命等下一段并入同一会话，真正停嘴出一条完整 FINAL；超时 flush 照常出（该轮多等 ≤HOLD ms）。`QWEN3_ASR_JOIN_HOLD_MS`（默认 800，0=关）；词表前缀门 `QWEN3_ASR_JOIN_HOLD_VOCAB`（默认 1，0=关，2026-09-09 S3 拼多多专项：品牌词内部微停顿「京|東」「拼|多多」拆轮烂词，「京」⊂词表「京东」→ hold 并单会话整句解码；词表=模板 hotwords+行业词+对象 courier/contact_channel 同一份，运营加词即生效；probe_brand_words 基线 10/16→14/16，三语 E2E+barge-in+edge 同绿收编；剩 2 miss=小碎片被 ASR 源头解码烂（金≠京）非切分问题）。②agent 层**号码累积**（`BOK_WA_ACCUMULATE=0` 回退）：收号码步内数字主导句（`_wa_numberish`）累计 <8 位、或自报头碎片（`_WA_ANNOUNCE_HEAD_RE`「我的WhatsApp係」零数字）→ 暂存 + `StopResponse`（唔回复唔推进唔侦测；**必须在 except-pass try 之外 raise**），下一段拼埋一次过处理；5s 超时 flush 跑侦测，captured 则上报+`session.say` 复述确认（`_wa_number_line` 三语脚本直念），唔系则请客户继续——唔可以赖 8s 心跳（护栏会当答案在途压住）。③flow 侧：**WA 步 CONFIRM 假推进护栏**（抽成 flow.py `wa_confirm_advance_allowed`，agent.py rule CONFIRM 分支与背景 judge CONFIRM 分支两路共用：`_looks_like_whatsapp_step` 且未 captured → 唔 advance——碎片「我的WhatsApp是。」尾部裸係/是命中 `_CONFIRM_RE` 会假确认、4B judge 对模糊轮误判 advance 亦会假确认（c4f6e4f1 的 judge=confirm 正係 judge 路），都唔可以越过收号码步）；**捕获下限 4 位**（`_valid_digit_runs` min_len=4，1-3 位=ASR 碎片唔收；5 位测试短号「一二二三三」旧 6 位门槛曾全数走漏→唔爆闪，用户拍板「报出就收」）；`_digit_normalize` 收英文数字词（zero/oh/one…nine，en 号码词旧版结构性无法归一）；announce 正则容忍系词与数字间接缝标点（拼接文本「係。一七二…」）。
- **ASR 流式 partial + 抢跑**：sidecar mlx 后端 `QWEN3_ASR_STREAM=1`（默认）每 ~700ms（`QWEN3_ASR_PARTIAL_MS`，旧 400）滑窗出 partial、窗口 ≤12s、**FINAL 后停发**（削 GPU 租户）；**会话级 partial 抑制（2026-09-08 GPU 竞态专项）**：回复生成/播报中（thinking/speaking）agent 经 `Qwen3ASRLiveSTT.set_partial_ms` 把该通会话 partial 间隔抬到 `BOK_ASR_PARTIAL_SLOW_MS`（默认 3000，0=关），listening 恢复——同卡上持续 partial 全窗重解实测拖慢 LLM TTFT +24%、被 LLM 反拖 2-4.6×，这是「用户长讲时回复变慢」的主根因；打断係 VAD 判定不受影响。**抑制档下增量拼接收紧**（partial ≤1.2s 新鲜、尾段 ≤1s，否则整句兜底）——陈旧 partial + 无上下文尾段独立解码会拼出幻觉尾巴（实证「切把狗领…好啊好」）。agent `Qwen3ASRLiveSTT` 发 `INTERIM_TRANSCRIPT`（字幕）+ 稳定前缀（首 ≥6 字、增长 ≥4 字才发）→`PREFLIGHT_TRANSCRIPT`（语言不匹配抑制 `QWEN3_ASR_PREFLIGHT_LANG_GATE`，每通语言固定后基本闲置）；停嘴增量 finish（partial 拼接+数字串/接缝保护回退整句，WhatsApp 零降级）；en finish 走 `English` hint。**抢跑防抖（2026-09-10）：框架抢跑默认关（`PREEMPTIVE_GENERATION=0`，=1 回旧行为）**——实机 843 次失效/0 次命中（agent.log 全量）：插件 PREFLIGHT 只发稳定前缀（防幻觉尾巴）而提交转写是全句 FINAL，框架 `_transcripts_equivalent` 逐词相等恒假=命中结构性不可能；且 max_retries 烧穿后新 PREFLIGHT 先 cancel 旧快照再 bail，commit 时连比较都不跑；失效的投机请求被 mlx「无断连中止」解码到完才放锁，解码尾巴挤占真请求队列。替代=**PrefillSpeculator**（`prefill_speculator.py`，`BOK_PREFILL_SPEC=1` 默认开）：说话中按稳定前缀发 out-of-band max_tokens=1 预热请求（prompt=上次真实请求逐字节快照+assistant 历史原文+user(前缀+尾部)，严格前缀契约；MlxLlmLLM.on_request_messages 快照钩子；busy 门控/每轮限 2 次/间隔 ≥600ms，`BOK_PREFILL_SPEC_DEBUG=1` 看拒绝原因），真轮只 prefill 分叉尾巴；`PREEMPTIVE_TTS=0`。诊断探针 `BOK_PREEMPTIVE_DEBUG=1`（transcript/ctx 比较打点+speculate 闸门归因，须装在 job 进程=entrypoint 内）；复测 `scripts/probe_preemptive.py`（长句喂 PREFLIGHT 链，E2E 短句结构性发不出 PREFLIGHT）。**推进轮不错步**：步骤推进/REFUSE 时向 turn_ctx 落步骤标记（抢跑关闭后为无害残留，回退框架抢跑时恢复失效触发作用）。ASR 模型维持 **8bit**：4bit（快 ~24%）A/B 实证数字路径同音字滑失（九→狗/號→后，同渲染音频 8bit 逐字全对）触碰「数字零降级」铁律被否——`model_path` 按目录存在性解析 .lmstudio/app-data 的断层已修。
- **ASR 热词/context 软偏置**：Qwen3-ASR 官方 customizable context（system message 词汇表「Vocabulary: …」，与 language 强制可叠加，mlx 层 `generate(system_prompt=)`）——治领域词误听。**实证（2026-09-07）**：e2e_edge_cases E2「我個單號係三七七八九零」前导误听（main/PR#31 三轮 ctx=False，「單號」聽成「打啊」）开启后 ctx=True 修复、7/7 全绿、真实通话轮零词表污染；context prefill 开销实测可忽略（真实长语音 A/B 无差），但**极低内容音频直打 sidecar 会把词表当转写抄出来**（纯静音探针稳定复现）——生产路径靠 VAD 门控+语音内容天然规避，留意短应承场景回归。来源三层：话术模板 `templates.hotwords` 字段（2026-09-08 二期落地：运营按套话术维护、web 话术表单可编辑，逗号/顿号/分号/换行分隔，超限截断时优先保留）+ 行业静态词（`agent.py` `_ASR_HOTWORDS` 三语各一套，话术域高频词）+ 对象文字字段（`courier`/`contact_channel` 专名类）；**数字串唔进**（biasing 幻听数字风险，下游 known-number 过滤兜底）；总长护栏 ≤120 字符。装配时 `asr_hotword_context(asr_pin_lang, object_card, extra_hotwords=template.hotwords)` 一次性组装，`Qwen3ASRSTT(hotword_context=)` 随 `/api/start` 下发、sidecar session 级透传每次解码。两级 kill-switch：`BOK_ASR_HOTWORDS=0`（agent 唔下发）/ `QWEN3_ASR_CONTEXT=0`（sidecar 唔透传）。改词表：行业层动 `_ASR_HOTWORDS`，套话术层动话术表单热词输入框。
- **罐头音三件套（TTS 缓存/垫话/QA 快路，2026-09-09 #38/#40/#43/#46）**：本地 TTS 音频缓存 `tts_cache.py`（`CachedTTS` 只包 `synthesize()`=脚本直念线；key=text+voice+model **精确匹配**——运行时人设/音色不同即 miss 优雅降级走 LLM，pregen 用 lang_personas 音色映射与运行时同源；`BOK_TTS_CACHE=0` 关）。**垫话** `fillers.py`：慢生成轮回复首音频 `BOK_FILLER_DELAY_MS`（默认 500ms，用户实选定档）未到 → **`BackgroundAudioPlayer` out-of-band 音轨**播预合成应承语——**绝不能走 `session.say()`**（1.8 speech 队列严格串行，垫话必排回复后，实机实证）。**2026-09-10 资产化+播放排序改版**：垫话=随源码分发 wav 资产（`assets/fillers/`+manifest.json；`scripts/gen_filler_assets.py` 固定音色 zh=Chinese_wenrounvxing/粤=Cantonese_crisp_news_anchor_vv2/en=socialmedia_female_2_v1，speed 1.2(zh/粤)/1.0(en)+pitch 0 预生成，实测时长 1.0-1.5s 窗校验，超窗打回）——与人设音色/tts_cache 解耦；**万能话术**原则（只保留注意力应承+等待邀请，零动作动词——随机触发语境永不穿帮）；**池 39 条=每语言 10 短+3 长**（2026-09-10 长句 tier：短句 1.0-1.5s 只盖 ~1.5s 慢生成窗，长句 1.7-2.3s（实测均值 zh 2.22/粤 2.15/en 2.01s，独立时长窗校验超窗打回）把链发覆盖上限拓到 2.3s；en 音色实证任何停顿（逗号/句号/静音标记）都令相邻词拖腔 +1.8s（逗号版 3.4-4.2s 三轮复现）→ en 长句定版**零停顿连读**、靠 8-9 词 ~0.2s/词落窗）；**播放排序=垫话播完→`BOK_FILLER_GAP_MS`(默认 300ms)→回复**：不再掐垫话，回复首帧由 `_RelaySynthesizeStream` 向 `hold_if_playing()` 询问「垫话剩余+gap」帧缓冲到点再放（内芯 iterator 惰性天然背压）；**语言铁律**=垫话语言=装配时钉死的 `greet_lang`（en 通话曾因运行时 lang 漂移落 zh 池放出普通话垫话——用户实测怪声根因），池缺失明文日志跳过绝不跨语言；`BOK_FILLER_LINES` 文本覆盖口已删（改话术=改 gen 脚本重新生成）；`tts-pregen --fillers` 停用；每通限次 `BOK_FILLER_MAX=3`；**自动链发第二垫话**（2026-09-10）：首条播完、回复首音频仍未到 → gap 呼吸后自动补一发（`PlayHandle.wait_for_playout()` 播完即醒精确补位，不靠估时长；`BOK_FILLER_CHAIN` 默认 1，0 关；每轮链发封顶 1 次，与主动 arm 消耗同一计数——MAX 2→3 即为「单轮至多 1+1 仍给后续轮留 1 发」，默认 2 时一次链发即耗尽全通）；A 线 `min_consecutive_speech_delay=0.3`（speech 队列相邻两条播报最小间隔 0.3s，开场白→回复/心跳衔接不再背靠背机械感；垫话走 out-of-band 音轨不经此队列零影响，B 线不加）；out-of-band 不入 LLM 上下文（KV 前缀/回声锚零污染）；`BOK_FILLER=0` 关。**Q→A 检索快路** `qa_gate.py`：用户轮命中 `qa_entries` 条目（0.6×HybridLexical 余弦+0.4×子串，阈值 0.90 **只认字面措辞**——同义改写要按说法补词条）+ 四道闸全过（digits/refuse/wa/closing/advanced/verdict 全旁路）+ 应答音频已物化（`tts-pregen --qa`）→ 跳过 LLM 直播缓存 ~50ms；词库增长靠 `reports/qa-pairs` 真实转写挖掘（`tts-mine --apply`，source=mined）；`BOK_QA_FASTPATH=0` 关。**尾部锚拟声剥离** `_StripTailAnchorStream`（`ContextAwareLLM.chat` 出口包一层，跨 chunk 状态机）：4B 会把尾部【你上一句】「…」模板块照抄进回复输出（call-974d8da3 念出声实证，C4 情绪标签同族）——出口单点剥离，TTS/历史/turns/重复锚四下游全干净，渲染不动（锚的重复控制功能保留）；命中打 `TAIL_ANCHOR_MIMIC_SUPPRESSED`。
- **DB 迁移**在 `apps/control-plane/control_plane/deps.py` `build_engine()` 幂等段（补列 + 数据迁移），启动时自动跑；不要在别处手写迁移。
- **turns 分析账本生产者（P0 §6.1，2026-09-10 接线）**：agent 每轮上报 line/speaker/gen/template_step/started_ms/ended_ms/perceived_ms——A 线 `line=a`、user=customer、assistant=agent_ai；B 线 `line=b`、speaker=me/other（旧版缺省误标 line=a 已修）；`gen` 标生成源：llm/script（开场白/心跳/收线/WA 复述直念）/qa_fastpath（item_added 统一上报，旧 ④ 直报双写已收编）；`perceived_ms` 三段时序实测：tts_metrics 整段合成完才 emit、llm 流关闭又在 TTS 之后 → item_added 时 pending 已就位，启动即取勿等待；垫话 PR 曾把 `_RelaySynthesizeStream._metrics_monitor_task` pass 掉致 tts 段整体哑火（勿覆写）。上报任务走 `_spawn_report` 登记挂断 flush（裸 create_task 会被 job teardown 杀掉丢轮）。
- **分发型拓扑（P0 起）**：业务库必须保持 SQL 方言可移植（`tests/test_db_portability.py` 门禁，
  新列/新表禁用 sqlite 专有语法）；turns 新轮上报带分析账本字段（speaker/gen/template_step/
  perceived_ms，缺省值兜底旧调用）；节点身份=NodeStore 注册签发 node_token（只存 sha256），
  node-agent 心跳失联 ≥3 次置 REFUSE_JOBS；web CP 地址运行时注入（`window.__BOK_CONFIG__.cpUrl`
  优先于构建期 env，`apps/web/lib/api.ts` `apiBase()` 单点），禁止再把 CP 地址烤成构建期常量。

## Testing Guidelines

- Python: pytest `tests/test_*.py` (`test_*` functions); Node: `node --test` under `services/realtime-translation/test`.
- 改完 Python 跑 `python -m compileall -q apps packages services tools scripts`；web 改动跑 `cd apps/web && npx tsc --noEmit && npm run build`。
- **术语门禁**：`tests/test_cantonese_terminology.py` 是全仓测试的一部分（新增 `yue` 字面量即失败）。
- E2E needs the running stack and local models. **Never fake-green**: A-line E2E must use the real `/api/token` (`E2E_SELF_TOKEN=1` is debug-only).
- **并发/边界回归资产（2026-09-07 QA）**：`scripts/e2e_barge_in.py`（打断：AI 播报中插话→断言停声+恢复不哑火）、`scripts/e2e_edge_cases.py`（静音/超短音频不毒化链路）、`scripts/load_cp_concurrency.py`（CP 独立 :8001+临时 DB 并发压测，含 turns 并发 30/30 回归——turn_id 竞态丢数据防复发）`scripts/e2e_interpret.py`（B 线同传双向 E2E：fwd/rev 双向译文音轨 ASR 回读断言目标语 + turns 原文/译文分行落库 + 启停×3 + 长流，2026-09-08 首跑 5/5）、`scripts/load_audio_concurrency.py`（N 路真实通话并发，`LOAD_ROADS/TURNS` 可调；单机上限参照 prompt-cache 4-6 路、TTS sidecar 全局锁串行）。改动 turns/audit/并发相关代码后必跑前三个。垫话/体感改动跑 `scripts/probe_filler_timing.py`（主判据=首声 <2.5s 预算；filler_fired 默认仅信息位，`FILLER_REQUIRE=1` 才强断言）。
- **stack A/B 须防殭尸 worker（2026-09-09 实证）**：`bok.py down` 杀不掉失联的旧 agent worker（orphan sweep 只扫部分），殭尸持续跑**旧代码** serving 新请求——曾致基线/分支 A/B 结论完全互换的污染。A/B 前后 `ps aux | grep agent_runtime` 必须为 0 再 serve；`git stash pop` 会被运行中 web server 重新生成的 `apps/web/next-env.d.ts` 挡道，pop 前先 `git checkout --` 之。
- **E2E 测试句形铁律（2026-09-09）**：话音句**不带逗号/大换气**（渲染内停顿 >0.45s 被 vad-pause 劈轮，尾段前缀续句 interrupt 掉生成中回复 → `sentences=0 canceled=1` 零回复音频 → 断言窗跑满 90s 假故障）；**<10 字单口气句结构性免疫**（低于 `QWEN3_ASR_PAUSE_COMMIT_MIN_CHARS=10` 门槛只可能 EOS 单轮提交）——cantonese 腿与 barge-in SECOND_TEXT 用 9 字句；隔离测 max_gap<0.45s 不可靠（in-room 渲染方差），以 in-room 不劈轮为准。
- **CP 可选鉴权**：`BOK_CP_TOKEN` 设置后除 `/health` 外全部端点要求 `Authorization: Bearer <token>`（agent/web 需同步携带）；未设=全放行（本机单用户形态）。暴露到局域网/云前必须设置。
- Merge gate: pytest, `npm test`, `cargo test`, and `scripts/verify_bundle.sh` (`--staging`, `--app`, `--doctor`, one mode per run) all green; `doctor --packaged` must report `token endpoint: ok (real JWT)`.

## Commit & Pull Request Guidelines

- Conventional commits with a scope (`fix(desktop):`, `ci(release):`, …); body states root cause and verification evidence.
- One logical change per commit.
- Update `docs/RUNTIME_TOPOLOGY.md` / `docs/REPO_MAP.md` whenever ports, paths, or data flow change.
- **Do not tag or release until full local acceptance passes** (project policy; releases are CI-gated and user-verified).

## Operational Constraints

- Never reintroduce Ollama, Docker, or CosyVoice runtime paths (removed by design).
- **agent worker 端口必须显式分**：A 线 main 与 B 线 interp（fwd/rev 两 worker）的 `WorkerOptions` 都要传 `port`（显式 8081 / 8082·8083 分配）——都不传时默认同绑 :8081，后绑者 `Errno 48` 即崩，web 侧表现为「Agent did not join」（2026-09-06 实证）。
- Bundled app resources are read-only: SQLite, vault, `tts-data`, logs, and metrics always live in app-data.
- **dev 栈 vs 桌面包版本断层**：`python tools/bok.py serve` 的开发栈用仓库 `runtime/`（symlink→`desktop/runtime`，gitignored）+ PYTHONPATH 指向仓库 `apps/`，改代码 `bok.py down && serve` 即生效；但 `/Applications/BokVoice.app`（桌面包）跑的是打包进 `_up_/` 的旧代码。测试改动先确认跑的是哪个。
- 本地 LLM 是 **mlx_lm server**（非 vLLM，Mac 上 vLLM 跑不了），prefill ~0.6k token/s 架构硬墙；runtime 的 mlx_lm 已打 **模板边界归一补丁**（`scripts/mlx_lm_template_leak_fix.py`，bok.py 起 server 前自动重打，BOK_TEMPLATE_LEAK_FIX=0 回退）——模板 endfor 后注释泄漏换行曾使跨轮缓存命中坍缩回 system 锚点，修复后每轮只 prefill「上轮回复+新问题」；A 线 :1235（`--prompt-cache-size 128 --prompt-cache-bytes 6GB --prefill-step-size 512`，2026-09-08 双轮反向 A/B 实证暖 TTFT -15%，dev `--log-level INFO` 看 Prompt processing progress），B 线翻译走 :1236 **Hy-MT2 专用 MT server**（`MODELS["mt"]`，缺模型自动跳过 B 线回退 :1235；`StatelessMTLLM` 官方模板逐句无状态）。**永不给请求加 seed/draft**（会关 continuous batching）。评估模型用 OpenAI 兼容端点，request model 必须填真实模型路径。
- **MiniMax TTS 运行规约**：凭据存设置 DB `tts.api_key`（勿提交仓库，`/tmp/mmkey` 是本地测试临时档）；**venv 无系统 CA——`SSL_CERT_FILE`（certifi）已由 bok.py 固化进全部 worker env**，手起进程要自带；**bidi 持久连接默认**（`t2a_v2_bidi`，2026-09-07 翻默认：服务端攒句防碎裂——classic 客户端按句+overlap 半句喂是「一句话连不起来、前后语气不一」根因，2026-09-06 用户听感实证+官方文档定位；`MINIMAX_WS_MODE=classic` 一键回退，classic 池/看门狗代码保留）；**emotion 默认不下发**（模型按文本自动匹配，官方文档建议+LiveKit 官方 minimax 插件 None 默认同款；旧 mood→emotion 映射是轮间语气跳变放大器，`MINIMAX_EMOTION=map` 回旧行为、枚举值直透）；`MINIMAX_CONTINUOUS_SOUND=1` 实验档（仅 2.8：模型侧不切分连续推理，韵律更自然 vs 延迟，默认关）；bidi 轮级打点 `MINIMAX_BIDI_PERF sentences=N canceled=0/1 first_audio_ms=Y`（sentences 应≈回复句数，远超=服务端按长度强切）。classic WS keep-warm 池（`MINIMAX_WS_POOL=1`，握手 216-648ms 全离关键路径）仅 classic 模式生效；A 线默认音色兜底 `Cantonese_crisp_news_anchor_vv2`（三语通用，空音色防 beep）；A 线 2.8-hd / B 线 turbo 按 `MINIMAX_MODEL` 分进程注入。链内 `TTS_FIRST_AUDIO_MS` 含「等 LLM 首句文本」时间，别当纯合成延迟读。首包前导静音自动修剪（≤200ms 上限防误剪气声起句，剪口 15ms fade-in 防咔哒，日志 `MINIMAX_TTS_LEAD_SILENCE_TRIM ms=N`；零静音零改动）——可闻出声更早。**卡死自愈看门狗**：首段文本发出后 `MINIMAX_FIRST_AUDIO_TIMEOUT_S`（默认 4s，旧 6/旧起点 task_started 均废——慢 LLM 轮会误触发+空重连）无首包 → 断开重连+重发已发文本（日志 `MINIMAX_TTS_STALL`）；重连窗口有发送闸（`_reconnecting` Event，发送点先 `is_set()` 再 `wait()`——Event.wait() 对未 set 事件挂起，不能无条件 await）——MiniMax 云端偶发 >8s 无首包是「每问无答」的现行真凶（2026-09-06 实证）。**bidi 三件自愈（2026-09-10）**：①**连接健康**——recv 死亡即后台重预热（`MINIMAX_BIDI_AUTO_REWARM` 默认 1 总闸，`invalidate(*, reprewarm=True)`；死亡连接不再等真实轮才发现，冷启动离群源根治）+ ping 连失 ≥`MINIMAX_BIDI_PING_MAX_MISS`（默认 2）打 `MINIMAX_TTS_BIDI_DEAD` 强断重预热（单次 miss 不断、pong 清零；强断**不可内联 await invalidate**——`_stop_ping()` 自 cancel 当前 ping task 会掀 CancelledError 吞掉 close 收尾，须甩独立 task）；孤儿 invalidate task 必须存引用+aclose 先 cancel（裸 create_task 事件循环只持弱引用可被 GC 中途回收，teardown 后才执行 reprewarm 会拉新连接+60s ping=关机泄漏，变异数测试实证）。②**首包看门狗**——首条 task_continue 起 `MINIMAX_BIDI_FIRST_AUDIO_TIMEOUT_S`（默认 6s 攒句放宽、0 关；classic 同款 4s 档不适用）无首包 → 弃连重连+已发文本**单条合并**重发（截 10k 官 2204 上限）+新纪元认领+计时复位，一流只自愈一次防重连风暴（重连窗口发送闸盖全部发送点；此前 bidi 只能干等 30s recv 超时=整轮静默）。③**预热韧性**——prewarm 失败 1s 后重试一次（`MINIMAX_BIDI_PREWARM_RETRY` 默认 1，`_connect_and_start` 成功尾清零）；可选合成级预热 `MINIMAX_BIDI_SYNTH_WARMUP`（默认 0——T1 探针 `scripts/probe_minimax_bidi_cold.py` 实证处女连接服务端预热仅 ~22ms 不值得默认开，2201 重建全程 ~0.8s 由①吸收；开启时连接建好后锁内一次真实合成焐热、音频全丢、自身失败不 invalidate）。音色有效清单以 `apps/web/lib/minimax-voices.ts` 为唯一数据源（新 id 须先 `/api/tts/preview` 验 200；EN 旧 `male/female_english_speaker` 已实测 2054 移除）。
- 延迟调试读 agent.log 打点：`QWEN3_ASR_HINT`/`ASR_MS`/`LLM_TTFT_MS`（含 cached=N/M）/`LLM_FIRST_SENT_MS`/`TTS_FIRST_AUDIO_MS`/`PERCEIVED_MS`（北极星：用户讲完→AI出声=eou+llm ttft+tts ttfb，三段到齐即打，是上界口径——llm 段取抢跑请求 TTFT 含提交前提前量）/`LLM_REQ_MS`（LLM 请求级 header 时刻，配 llm.log 拆排队）；`scripts/measure_latency.py`/`scripts/measure_prompt.py` 可复测；历史预算表在 `.superpowers/sdd/2026-09-04-mt-minimax-latency/`（p4-p7 为 ≤1s 攻坚实测档案）。
- **感知延迟结构（2026-09-09 S5 实测修订）**：暖轮 TTFT 主导=未缓存后缀（新 user 话+新尾部+上轮回复 ≈240 tok）的 prefill，实测 **0.4-0.6k tok/s**（0.6k 墙只对大跳变轮成立，勿再引「暖轮 5-6k」误读——mlx_lm streaming 的 prefill 懒执行发生在 headers 之后，SSE keepalive 行可精读进度，`scripts/mlx_lm_template_leak_fix.py` 的 `[bok-timing]` 计时补丁即为此设，`BOK_TIMING_PATCH=0` 关）；mlx_lm 0.31.3 已最新、server 无断连中止（被作废抢跑请求解码到完才放锁=排队尾巴）；空闲 ~2h 后首请求付权重页面换入 ~40s（会话 prewarm 已吸收）；探针对 :1235 发请求 model 字段必须用本地路径（repo id 触发 HF hub 解析）。
- **EOS 停嘴时钟锚点铁律（2026-09-09）**：STT 发 END_OF_SPEECH 必须带 `speech_end_time`（停嘴时刻）——框架 min_delay 锚定该字段（audio_recognition.py:1332/1679），锚缺失坍缩为 now → min_delay 全额叠在解码延迟后（白付 ~0.25s/句）。vad-pause 路径锚 VAD 静音起点、partial-punct 锚 chunk POST 发出时刻、join-flush 锚=flush 时刻-hold 窗。新加提交路径照此办。
- **尾部瘦身（BOK_TAIL_SLIM=1 默认，0 回退）**：revision 与上一条已冻结尾部相同（流程/事实/WhatsApp 无实质变化）时尾部只发「【第N/M步·继续】+WhatsApp+你上一句引文」紧凑标签——全量指引在上轮冻结尾部原样可见，逐字重发是暖轮 TTFT 大头；【你上一句】固定指令文本已上移稳定前缀【重复控制】（前缀缓存命中零成本，尾部只留引文）；add_call_fact 必须 +revision（新事实漏出全量尾=事实失明）。未缓存后缀从 249-275 字降到 72-211 字。
- **REPEAT verdict（2026-09-09）**：「听唔清/再说一次/你说什么」等短句（≤12 字，简繁混拼全收）判 repeat → 指引照讲复述（数字/单号逐位放慢），不算复读；不推进/不走背景 judge/QA 快路白名单自动排除。复读类提示措辞必须留复述门（尾部账本冻结重放整通，「严禁复读」一刀切会 fight 客户要求的重复）。
- **E2E 测试话音已真化（2026-09-09）**：三套 E2E（trilingual/barge-in/edge_cases）全弃 fixtures 改 TTS sidecar 现场合成（`tts_pcm`，语言参数 zh/cantonese/en），句句不同真人客户口吻——fixtures（135-138s 长音频）与 E2E 解耦，勿再让测试整条推长音频（0.55× pacing 一条 137s，曾致 E4 假 FAIL）；品牌词/碎片化探针停顿须 ≥0.5s 跨 VAD min_silence 门，0.3s 测的是接缝解码不是拆轮。
- **KV-cache/缓存诊断（2026-09-06 起）**：`BOK_LLM_MSG_DEBUG=1` 打逐请求逐消息 sha1 指纹（定位前缀分叉消息）；`scripts/llm_cache_report.py <worker.log>` 出 cached 分布汇总；档案在 `.superpowers/sdd/2026-09-05-llm-cache/FINDINGS.md`（三根因+锚点封顶开放项=mlx_lm 上游 LRU 段键可查性异常，影响每轮历史增量 0.1-0.5s）。
- **回声/AEC**：浏览器采集默认已开 `echoCancellation/noiseSuppression/autoGainControl`（livekit-client 默认预设，勿关）；扬声器外放+失效输出设备会造成 AI 自闻自打断（sink 失败已自动清档回默认输出，日志 `Failed to set sink id` 出现即说明存档设备已失效）。实测/演示**建议戴耳机**；agent 侧 ai_coustics 降噪为可选后手（需授权），未接。
