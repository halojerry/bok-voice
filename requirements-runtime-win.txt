# Windows bundled runtime deps (NVIDIA CUDA path).
# - ASR: Qwen3-ASR (qwen-asr 0.0.6 pins transformers==4.57.6) -> installed by
#   build_runtime.sh after this file.
# - TTS: qwen-tts 0.1.1 pins transformers==4.57.3, which conflicts with
#   qwen-asr -> installed with `--no-deps` (works fine with 4.57.6 in practice).
# - LLM: llama.cpp CUDA binary (not a pip package).
fastapi>=0.110,<1
uvicorn[standard]>=0.29,<1
python-multipart>=0.0.9
numpy>=1.26,<3
soundfile>=0.12
librosa>=0.10
httpx>=0.27,<1
pydantic>=2.6
sqlalchemy>=2.0,<3
huggingface_hub>=0.26
hf_transfer>=0.1
transformers==4.57.6
accelerate>=1.0
einops>=0.8
gradio>=5.0
onnxruntime>=1.18
sox>=1.5
torch>=2.5
torchaudio>=2.5
