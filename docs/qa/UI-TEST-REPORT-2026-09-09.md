# 全功能用户视角测试报告（2026-09-09）

覆盖：12 页面 60+ 功能（浏览器逐页点击实测）+ 真机链路 E2E + 自动化回归。
轨 C 结果：pytest 441 ✓ / web tsc+build ✓ / B 线 npm test ✓。
已确认生效的新能力：罐头命中（`TTS_CACHE hit=1`×4，开场白秒出）、热词幻听拦截（`QWEN3_HOTWORD_ECHO_DROP`×2，零回复零落库）、预生成 key 对齐（29 条全 skip）。

## 缺陷清单

### P0（交付阻塞）

| # | 缺陷 | 复现 | 样本 |
|---|---|---|---|
| B1 | **Web 挂断不触发结算**：结算落库 0 条，结算卡空，对象主题/报告不沉淀（E2E 脚本挂断正常） | /calls/new 接通→挂断→查 settlements 表 | call-95dcd01a、call-feaf914c（均 0 条；E2E 通话有 1 条） |
| B2 | **模板编辑丢数据**：创建 5 步话术→编辑回读只剩 4 步；编辑中修改 hotwords 保存后仍是旧值 | /templates 创建→编辑→保存→API 对比 | QA-测试话术（已复现并清理） |
| B3 | **对象下拉无上限灌 300+ 选项**：大量 E2E/soak 残留对象，真实用户不可用（性能+体验） | /calls/new 打开对象下拉 | 300+ option |

### P1

| # | 缺陷 | 说明 |
|---|---|---|
| B4 | 通话状态卡死无清理：2026-09-04 的 call-82cdc639 仍「振铃」；supervisor/reports 显示 **26 路「进行中」**（历史残留） | 监控/报表数据失真，需状态超时回收 |
| B5 | 一体台「结束一体台会话」按钮持续 disabled，无法正常退出（需真麦环境复核） | /interpret 创建一体台后等 7.5s 仍不可点 |
| B6 | 知识库列表 500+ 条无分页（删除按钮 500+），页面性能与可用性差 | /knowledge |
| B7 | 人设重复数据：3 条重复「小博 · Bok 建材」出现在 /calls/new 人设下拉 | 建档无查重 |

### P2 / 专项结论

- **「拼多多→多多」漏字**：渲染音频→sidecar 回读专项 **4/4 全对**（zh/cantonese × 正常/快速，含「拼多多」完整）。结论：**不是 ASR 精度问题，是流式滑窗/轮次切分把词切碎**（VAD 微停顿碎片提交）。修复方向：普通词 join-hold / vad-pause 提交门调参（已有号码类 join-hold 先例可扩展）。
- **LLM 复读**：call-feaf914c 连续两轮回复一字不差（「对对对。是我。」后复读同一话术块）。复读锚机制在新场景失效，需另立修复。
- 知识库检索结果展示待复核（导入 PASS；检索后结果区未见明显条目渲染）。

## PASS 清单（摘要）

接通→通话 UI→暂停/恢复→挂断全流程；/calls 列表/轮询/行点击/深链 `?call=`/删除；对象新建/编辑回读/搜索/表格导入（CSV 2 行全入库）/批量勾选；人设新建；模板创建（5 步示例+hotwords+语言）；知识库导入示例；一体台创建（共享扬声器/自动半双工默认档正确）；supervisor/reports/audit 渲染。

## 受限项（浏览器测不了，需打包壳验证）

系统输出设备 CoreAudio 切换、/settings 桌面服务启停卡、真录音克隆、双端同传真说话效果（音频轨级由 e2e_interpret 覆盖）。

## 测试数据

`QA-` 前缀对象/人设/话术已全部清理；缺陷样本通话（call-95dcd01a）保留待修复验证。
