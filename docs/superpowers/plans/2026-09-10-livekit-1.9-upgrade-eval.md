# livekit-agents 1.9 升级评估计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成 livekit-agents 1.8.0 → 1.9.x 升级评估：breaking changes 影响矩阵、新版音频式 TurnDetector / adaptive interruption 的自部署可用性与粤语覆盖，产出「升/不升/何时升」决策与测试大纲。

**Architecture:** 两段式——先离线调研（changelog+源码 diff，产出 FINDINGS），再隔离 worktree 实测升级（pin 抬到 1.9 + 全量回归 + PERCEIVED_MS 对比），最后决策门决定是否合入。

**Tech Stack:** livekit-agents（现锁 `apps/agent/pyproject.toml:22-24`：`>=1.8.0,<1.9`）、git worktree 隔离、项目 E2E 套件。

## Global Constraints

- 升级实测必须在隔离 worktree + 独立端口跑，**A/B 前后 `ps aux | grep agent_runtime` 必须为 0**（殭尸 worker 污染铁律）。
- 评估期间不动 main 分支 pin；决策通过前 production 环境保持 1.8.0。
- 实测验收线：pytest 全量绿 + 三语 E2E 绿 + barge-in PASS + edge cases 绿 + PERCEIVED_MS p50 不劣化（基线 1.82s）。

---

### Task 1: 离线调研（已完成 2026-09-10）

**Files:**
- 产出: `.superpowers/sdd/2026-09-10-livekit-1.9-upgrade-eval/FINDINGS.md`

- [x] **Step 1:** 收 subagent 调研结果：**核心事实修正——1.9 不存在，PyPI 最新 GA=1.8.0（2026-09-05，无 1.8.x 补丁无 rc），现役=最新版**。main 自 1.8.0 仅 31 commits 零 breaking。
- [x] **Step 2:** 硬结论复核（源码一手证据）：音频式 `inference.TurnDetector` 部分已内置 1.8.0，LOCAL_LANGUAGES 14 语言 zh=0.3550 **无粤语**（粤语静默回落英文阈值）；`turn_detection="stt"` 下 EOT stream 根本不创建（audio_recognition.py `_update_turn_detector(None)`），对句级提交架构无意义；adaptive interruption 强制 Cloud 凭据（ValueError），自部署不可用。

### Task 2: 隔离实测（**不执行——无可升版本，前提不成立**）

~~worktree 隔离升 1.9~~：跳过。1.8.0 已全量验证（311 测试+三套 E2E），无新版本可测。

### Task 3: 决策门与收尾（已完成）

- [x] **决策：不升、零动作**——锁保持 `apps/agent/pyproject.toml` `>=1.8.0,<1.9` 不变。
- [x] **重评触发条件**（写回 FINDINGS §7.2 测试大纲）：①livekit-agents 1.9 GA 发布；或②官方 TurnDetector 校准表加入粤语（eot/languages.py）。届时按防殭尸 worker 铁律 + pytest 全量 + 三语 E2E + barge-in/edge + PERCEIVED_MS 对照重评；回退=改回锁行重装。
