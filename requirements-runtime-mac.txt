# macOS bundled runtime deps (Apple Silicon / MLX path).
# Dev venvs and build_runtime.sh both install from this file so dev and
# packaged runtimes never drift. No torch/gradio/qwen-asr/qwen-tts here:
# the MLX path only needs mlx-audio + mlx-lm.
fastapi>=0.110,<1
uvicorn[standard]>=0.29,<1
python-multipart>=0.0.9
numpy>=1.26,<3
soundfile>=0.12
librosa>=0.10
httpx>=0.27,<1
pydantic>=2.6
sqlalchemy>=2.0,<3
huggingface_hub>=0.26
hf_transfer>=0.1
mlx==0.32.2
mlx-audio==0.5.0
mlx-lm==0.31.3
# transformers 由 mlx-audio(>=5.14.0)/mlx-lm(>=5.0.0) 解析，不手动钉版本。
sentencepiece>=0.2
