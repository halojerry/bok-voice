[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "bok-voice-obs"
version = "0.1.0"
description = "Bok Voice structured logging, correlation, request middleware and audit trail"
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["bok_voice_obs"]
