# Linux bundled runtime deps (NVIDIA CUDA path，GPU 节点主力档)。
# 与 requirements-runtime-win.txt 同源：torch>=2.5 默认轮子即 cu12x（含
# Turing sm_75 → Ada sm_89 全谱），transformers 4.57.6 由 qwen-asr 钉死。
# - ASR: Qwen3-ASR (qwen-asr 0.0.6 pins transformers==4.57.6) -> installed by
#   build_runtime.sh after this file.
# - TTS: 走 MiniMax 云端（本地 qwen-tts 不进 Linux 包，需要时另行评估）。
# - LLM: llama.cpp CUDA binary 或 vLLM（sm_80+ 卡），均非本清单范畴（二进制/
#   独立安装，requirements-runtime 面只管 python 依赖）。
fastapi>=0.110,<1
uvicorn[standard]>=0.29,<1
python-multipart>=0.0.9
numpy>=1.26,<3
soundfile>=0.12
librosa>=0.10
httpx>=0.27,<1
pydantic>=2.6
sqlalchemy>=2.0,<3
huggingface_hub>=0.26
hf_transfer>=0.1
transformers==4.57.6
accelerate>=1.0
einops>=0.8
gradio>=5.0
onnxruntime>=1.18
sox>=1.5
torch>=2.5
torchaudio>=2.5
