port: 7880
bind_addresses:
  - 127.0.0.1
rtc:
  bind_addresses:
    - 127.0.0.1
  tcp_port: 7881
  udp_port: 7882
  use_external_ip: false

redis:
  # 单机本地开发可不配置；生产多副本时必须
  # address: redis:6379

keys:
  devkey: devsecret

logging:
  level: info
  json: false
